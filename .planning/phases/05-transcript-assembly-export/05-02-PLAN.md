---
phase: 05-transcript-assembly-export
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - server/src/services/transcript.ts
  - server/src/services/pdf.ts
  - server/src/services/settings.ts
  - server/src/services/student.ts
  - server/src/__tests__/helpers/testDb.ts
autonomous: true
requirements:
  - TRN-01
  - TRN-02
  - TRN-03

must_haves:
  truths:
    - "npx prisma db push exits 0 (Transcript and SchoolSettings tables exist in PostgreSQL)"
    - "npx prisma generate exits 0 (typed client updated with Transcript + SchoolSettings)"
    - "prisma.transcript.deleteMany() and prisma.schoolSettings.deleteMany() in testDb.ts no longer need @ts-expect-error"
    - "transcript.ts service exports getOrBuild, upsert, TranscriptNotFoundError"
    - "pdf.ts service exports generatePdf using puppeteer.launch with --no-sandbox, --disable-setuid-sandbox, --disable-dev-shm-usage, --disable-gpu flags"
    - "settings.ts service exports getSettings, upsertSettings"
    - "student.ts listStudents LEFT JOINs Transcript and maps transcript?.status ?? 'NONE' to transcriptStatus in response"
  artifacts:
    - path: "server/src/services/transcript.ts"
      provides: "Transcript CRUD + auto-population + HTML builder"
      exports:
        - getOrBuild
        - upsert
        - buildAutoPopulatedContent
        - computeMaxRecordTimestamp
        - buildPdfHtml
        - TranscriptNotFoundError
    - path: "server/src/services/pdf.ts"
      provides: "Puppeteer PDF generation with Windows Server flags"
      exports:
        - generatePdf
    - path: "server/src/services/settings.ts"
      provides: "SchoolSettings singleton CRUD"
      exports:
        - getSettings
        - upsertSettings
    - path: "server/src/services/student.ts"
      provides: "listStudents updated to JOIN Transcript for status"
      contains: "transcript: { select: { status: true } }"
  key_links:
    - from: "server/src/services/transcript.ts"
      to: "prisma.academicResult / prisma.activity / prisma.award / prisma.workExperience / prisma.careerGoal"
      via: "Promise.all([findMany × 5]) in buildAutoPopulatedContent"
      pattern: "Promise\\.all.*findMany"
    - from: "server/src/services/pdf.ts"
      to: "puppeteer.launch"
      via: "generatePdf → page.setContent → page.pdf"
      pattern: "puppeteer\\.launch"
    - from: "server/src/services/student.ts"
      to: "prisma.student.findMany"
      via: "include: { transcript: { select: { status: true } } }"
      pattern: "transcript.*select.*status"
---

<objective>
Run the [BLOCKING] schema push to create the Transcript and SchoolSettings tables in PostgreSQL, regenerate the typed Prisma client, then implement all three server-side services: transcript.ts (auto-population + upsert + HTML builder, per D-08/D-09/D-02/D-03), pdf.ts (Puppeteer wrapper with Windows Server flags, per D-01), and settings.ts (singleton upsert, per D-12). Also update student.ts listStudents to LEFT JOIN Transcript for status (per D-16), replacing the Student.transcriptStatus stub removed from the schema in Plan 01.

Purpose: After this plan, every API route in Plan 03 has a real service layer to call. The [BLOCKING] db push must run first — without it, the typed client is stale and all Prisma calls for Transcript/SchoolSettings will throw at runtime.

Output: Three new service files; updated student service; live Transcript + SchoolSettings tables in PostgreSQL; updated typed Prisma client; testDb.ts @ts-expect-error stubs replaced with typed calls.
</objective>

<execution_context>
@C:/@code/spcs-transcripts/.cursor/gsd-core/workflows/execute-plan.md
@C:/@code/spcs-transcripts/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-transcript-assembly-export/05-CONTEXT.md
@.planning/phases/05-transcript-assembly-export/05-RESEARCH.md
@.planning/phases/05-transcript-assembly-export/05-PATTERNS.md
@.planning/phases/05-transcript-assembly-export/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] DB push + prisma generate + remove @ts-expect-error stubs</name>
  <files>
    server/src/__tests__/helpers/testDb.ts
  </files>
  <read_first>
    - server/prisma/schema.prisma — verify Transcript and SchoolSettings models are present (from Plan 01 Task 1) before running push
    - server/src/__tests__/helpers/testDb.ts — identify the @ts-expect-error stub lines added in Plan 01 Task 2 to replace them after generate
    - .planning/STATE.md Key Decisions — "docker-compose.override.yml created to expose postgres:5432 to host for prisma db push" (Phase 04-02 decision; same docker-compose.override.yml approach applies here)
  </read_first>
  <action>
    Run commands in sequence:

    1. cd server && npx prisma db push --accept-data-loss

    The --accept-data-loss flag is REQUIRED because removing Student.transcriptStatus is a destructive change (data loss on that column). Without this flag the push will prompt interactively and block. There are no existing records in transcriptStatus with meaningful data (it was always NONE, the default) — confirmed safe.

    2. cd server && npx prisma generate

    This regenerates server/src/generated/prisma/client.ts to include PrismaClient methods for prisma.transcript and prisma.schoolSettings. The @ts-expect-error stubs in testDb.ts are no longer needed after this step.

    3. Update server/src/__tests__/helpers/testDb.ts: remove both @ts-expect-error comment lines and the `(prisma as any)` casts for transcript.deleteMany() and schoolSettings.deleteMany(). Replace them with the typed calls:
    - await prisma.transcript.deleteMany()
    - await prisma.schoolSettings.deleteMany()

    Keep the existing deletion order: transcript and schoolSettings first, then staffNote, careerGoal, workExperience, award, activity, academicResult, document, auditLog, student, user.

    Similarly, update server/src/__tests__/students.test.ts: remove the @ts-expect-error stubs added in Plan 01 Task 2 around the Transcript.create calls. Replace `(prisma as any).transcript.create(...)` with `prisma.transcript.create(...)`.
  </action>
  <verify>
    <automated>cd server && npx prisma validate && npx prisma generate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx prisma db push --accept-data-loss exits 0 with no error (tables Transcript and SchoolSettings created in PostgreSQL)
    - npx prisma generate exits 0
    - server/src/generated/prisma/client.ts or the generated index contains "transcript" (case-sensitive) as a PrismaClient property (grep check)
    - server/src/__tests__/helpers/testDb.ts does NOT contain "@ts-expect-error" comment related to transcript or schoolSettings
    - server/src/__tests__/students.test.ts does NOT contain "(prisma as any).transcript" cast
  </acceptance_criteria>
  <done>PostgreSQL has live Transcript and SchoolSettings tables; typed Prisma client regenerated; no more @ts-expect-error stubs in test helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: transcript.ts + pdf.ts + settings.ts services + listStudents update</name>
  <files>
    server/src/services/transcript.ts,
    server/src/services/pdf.ts,
    server/src/services/settings.ts,
    server/src/services/student.ts
  </files>
  <read_first>
    - server/src/services/document.ts — read FULL file; this is the primary analog for transcript.ts (IDOR guard pattern, logAudit pattern, custom Error class, PrismaClient import path from '../generated/prisma/client.js', try/finally pattern)
    - server/src/services/academicResult.ts — read FULL file; analog for settings.ts (PrismaClient import, logAudit import, service function signatures)
    - server/src/services/student.ts — read FULL file; locate listStudents function and the transcriptStatus filter logic that must be updated from Student.transcriptStatus to Transcript JOIN
    - .planning/phases/05-transcript-assembly-export/05-RESEARCH.md Patterns 1–6 and Code Examples — canonical implementations of generatePdf (Pattern 1), buildPdfHtml (Pattern 2), TipTap editor (Pattern 3 — client only, skip), Prisma Transcript schema (Pattern 4 — already done), SchoolSettings singleton (Pattern 5), buildAutoPopulatedContent (Pattern 6)
    - .planning/phases/05-transcript-assembly-export/05-PATTERNS.md — "transcript.ts", "pdf.ts", "settings.ts", "students.ts" sections for exact import paths and code adaptation notes
  </read_first>
  <behavior>
    - transcript.ts: getOrBuild(prisma, studentId) returns { transcript: TranscriptRow | null, autoPopulated: boolean, showRecordsBanner: boolean, academicsContent: string | null, activitiesContent: string | null, awardsContent: string | null, workExperienceContent: string | null, careerGoalsContent: string | null, staffEndorsementContent: string | null, academicsVisible: boolean, ... (all 6 visible booleans) }
    - transcript.ts: when no Transcript record exists for studentId, buildAutoPopulatedContent runs Promise.all of 5 findMany queries and returns autoPopulated: true
    - transcript.ts: when Transcript exists, computeMaxRecordTimestamp returns max updatedAt across 5 record tables; showRecordsBanner = maxTimestamp > transcript.updatedAt
    - transcript.ts: upsert(prisma, studentId, data, userId) runs prisma.transcript.upsert and calls logAudit with model: 'Transcript', action: 'UPDATE'
    - transcript.ts: buildPdfHtml(transcript, settings, uploadRoot) returns a complete self-contained HTML string with logoDataUri embedded as base64, letterheadHtml injected, and all visible sections rendered as <p> / <section> blocks; uses only system fonts (Arial, no Google Fonts)
    - pdf.ts: generatePdf(html) launches puppeteer with args ['--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage', '--disable-gpu']; headless: true; calls page.setContent with waitUntil: 'domcontentloaded'; calls page.emulateMediaType('print'); calls page.pdf({ format: 'A4', printBackground: true, margin: { top: '20mm', right: '20mm', bottom: '20mm', left: '20mm' } }); returns Buffer.from(pdfBuffer); always calls browser.close() in finally block
    - settings.ts: upsertSettings(prisma, data, logoFile?, uploadRoot?) writes logo to path.join(uploadRoot, 'branding', 'logo.{ext}') using fs.writeFile; calls prisma.schoolSettings.upsert({ where: { id: 'singleton' }, ... }); calls logAudit with model: 'SchoolSettings'
    - settings.ts: getSettings(prisma) returns prisma.schoolSettings.findUnique({ where: { id: 'singleton' } })
    - student.ts listStudents: passes include: { transcript: { select: { status: true } } } to findMany; maps rows as ...student, transcriptStatus: student.transcript?.status ?? 'NONE' in the response array; filter clause updated from { transcriptStatus } to { transcript: { status: transcriptStatus } } for DRAFT/FINALISED, or { transcript: { is: null } } for NONE
  </behavior>
  <action>
    Create server/src/services/transcript.ts:

    Import PrismaClient from '../generated/prisma/client.js' and TranscriptStatus type. Import logAudit from './audit.js'. Import readFile from 'node:fs/promises' and path from 'node:path'.

    Export class TranscriptNotFoundError extends Error — same pattern as DocumentNotFoundError in document.ts.

    Export function buildAutoPopulatedContent(prisma, studentId): runs Promise.all of 5 prisma findMany queries (academicResult ordered by calendarYear desc, activity by startYear desc, award by awardYear desc, workExperience by startYear desc, careerGoal by createdAt desc take 1). Maps each result to HTML sentences per D-08:
    - Academic: "<p>Achieved [grade] in [subject] ([calendarYear]).</p>"
    - Activity: "<p>[role] at [organisation] ([formatPeriod(activity)]).</p>" — include a formatPeriod helper that produces "YYYY–present" or "YYYY–YYYY" based on endYear/ongoing fields matching Phase 3 periodFormat pattern
    - Award: "<p>[title] ([level]) from [issuer] ([awardYear]).</p>"
    - WorkExperience: "<p>[role] at [employer] ([formatPeriod(work)]).</p>"
    - CareerGoal: concatenate career interests (if any) as a comma-separated list + free-text description; wrap in <p> tags; use careerGoals[0] only
    - staffEndorsementContent: always null (blank start per D-06)
    Returns joined strings or null if no records in that category.

    Export function computeMaxRecordTimestamp(prisma, studentId): runs Promise.all of 5 findFirst queries (each with orderBy updatedAt/createdAt desc, select only timestamp field). Computes Math.max of all timestamps, returns as Date or null.

    Export async function getOrBuild(prisma, studentId): findUnique transcript for studentId. If not found: call buildAutoPopulatedContent + return { transcript: null, autoPopulated: true, showRecordsBanner: false, ...contentFields, ...allVisibleTrue }. If found: call computeMaxRecordTimestamp; showRecordsBanner = maxTimestamp ? maxTimestamp > transcript.updatedAt : false. Return { transcript, autoPopulated: false, showRecordsBanner, ...transcript fields }.

    Export async function upsert(prisma, studentId, data, userId): run prisma.transcript.upsert({ where: { studentId }, update: { ...data, updatedAt: new Date() }, create: { studentId, ...data } }). Call logAudit with action: 'UPDATE', model: 'Transcript', recordId: result.id, details: { status: data.status ?? 'unchanged' }. Return result.

    Export async function buildPdfHtml(transcript, settings, uploadRoot): read logo file from disk (path.join(uploadRoot, settings.logoPath)) if settings.logoPath exists; encode as base64 data URI with correct MIME type (infer from extname). Build self-contained HTML string: DOCTYPE html, charset utf-8, no external URLs in <head> (system fonts only — Arial, Times New Roman). Body includes: logo img (if logoDataUri), schoolName h1, schoolAddress p (if present), letterheadHtml injected VERBATIM (no sanitization per D-14 Admin-only write). Then for each of the 6 sections (Academics, Activities, Awards, Work Experience, Career Goals, Staff Endorsement), if visible===true: render section h2 + transcript.[sectionName]Content as innerHTML. Hidden sections are omitted entirely from the HTML string.

    Create server/src/services/pdf.ts:

    Import puppeteer from 'puppeteer'. Export async function generatePdf(html: string): Promise<Buffer> following RESEARCH.md Pattern 1 exactly — puppeteer.launch with headless: true, args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage', '--disable-gpu']. page.setContent(html, { waitUntil: 'domcontentloaded' }). page.emulateMediaType('print'). page.pdf({ format: 'A4', printBackground: true, margin: { top: '20mm', right: '20mm', bottom: '20mm', left: '20mm' } }). Return Buffer.from(pdfBuffer). Always call browser.close() in finally block.

    Create server/src/services/settings.ts:

    Import PrismaClient. Import logAudit from './audit.js'. Import writeFile from 'node:fs/promises' and path from 'node:path'.

    Export async function getSettings(prisma): return prisma.schoolSettings.findUnique({ where: { id: 'singleton' } }).

    Export async function upsertSettings(prisma, data: { schoolName: string; schoolAddress?: string; letterheadHtml?: string }, logoFile?: { buffer: Buffer; originalname: string }, uploadRoot?: string, userId?: string): if logoFile and uploadRoot provided, determine ext from logoFile.originalname using path.extname, write buffer to path.join(uploadRoot, 'branding', 'logo.' + ext.slice(1)) using writeFile. Set logoPath = 'branding/logo.' + ext.slice(1) in the data payload. Run prisma.schoolSettings.upsert({ where: { id: 'singleton' }, update: { ...data, logoPath?, updatedAt: new Date() }, create: { id: 'singleton', ...data, logoPath? } }). Call logAudit with action: 'UPDATE', model: 'SchoolSettings', recordId: 'singleton', details: { updatedFields: Object.keys(data) }.

    Update server/src/services/student.ts listStudents function:

    In the Prisma where clause, replace the transcriptStatus filter: `...(transcriptStatus ? { transcriptStatus } : {})` becomes: if transcriptStatus is 'NONE' → `{ transcript: { is: null } }`; if transcriptStatus is 'DRAFT' or 'FINALISED' → `{ transcript: { status: transcriptStatus } }`; if undefined/null → `{}`. Add `include: { transcript: { select: { status: true } } }` to the findMany call. In the map that builds the response array, change `transcriptStatus: student.transcriptStatus` (or however it was accessed) to `transcriptStatus: student.transcript?.status ?? 'NONE'`. Remove any reference to `student.transcriptStatus` as a direct field.
  </action>
  <verify>
    <automated>cd server && npx vitest run --reporter=verbose students.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - server/src/services/transcript.ts contains "export async function getOrBuild(" (function exists)
    - server/src/services/transcript.ts contains "export async function upsert(" (function exists)
    - server/src/services/transcript.ts contains "export async function buildPdfHtml(" (function exists)
    - server/src/services/transcript.ts contains "Promise.all" (parallel batch query in buildAutoPopulatedContent)
    - server/src/services/pdf.ts contains "'--no-sandbox'" (Windows Server flag per D-01)
    - server/src/services/pdf.ts contains "'--disable-setuid-sandbox'" (required complement)
    - server/src/services/pdf.ts contains "'--disable-dev-shm-usage'" (required for constrained envs)
    - server/src/services/pdf.ts contains "'--disable-gpu'" (reduces headless crashes)
    - server/src/services/pdf.ts contains "browser.close()" in finally block
    - server/src/services/settings.ts contains "export async function upsertSettings(" (function exists)
    - server/src/services/settings.ts contains "id: 'singleton'" (singleton upsert pattern)
    - server/src/services/student.ts contains "transcript: { select: { status: true } }" (LEFT JOIN include)
    - server/src/services/student.ts does NOT contain "transcriptStatus: student.transcriptStatus" as a direct model field access (verify transition to JOIN)
    - cd server && npx vitest run --reporter=verbose students.test.ts — nav-02-status tests now use real Transcript JOIN data; if tests were already passing per Plan 01 students.test.ts update, they should still pass
  </acceptance_criteria>
  <done>All three server services implemented; student.ts listStudents uses Transcript JOIN for status; TypeScript compiler sees no errors on the service files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Express API → PostgreSQL | Prisma queries with parameterised inputs (no raw SQL injection risk) |
| server/src/services/pdf.ts → Puppeteer Chromium subprocess | HTML string from school-authored content; no user-controlled content reaches Chromium untrusted |
| server/src/services/settings.ts disk write | Logo buffer written to branding/ subfolder under UPLOAD_ROOT; no path traversal risk (ext derived from original filename, path constructed with path.join) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | transcript.ts getOrBuild / upsert | mitigate | IDOR guard implemented: getOrBuild returns null (→ 404 in route) when no Transcript for studentId; upsert checks studentId ownership via Prisma unique constraint (studentId @unique) |
| T-05-02 | Elevation of Privilege | settings.ts upsertSettings | mitigate | requireRole(ADMIN) enforced at route layer (Plan 03); service trusts that caller is Admin-authenticated |
| T-05-03 | Tampering | buildPdfHtml letterheadHtml injection | accept | Admin-only write path per D-14; no untrusted user input reaches letterheadHtml; Chromium renders school-authored HTML only |
| T-05-04 | Information Disclosure | pdf.ts generatePdf — Chromium subprocess | accept | Self-contained HTML only; no external URLs; no user-controlled HTML from untrusted sources; offline Chromium per on-premise constraint |
| T-05-SC | Tampering | puppeteer already installed (Plan 01) | mitigate | Package legitimacy audit completed in Plan 01; no additional package installs in this plan |
</threat_model>

<verification>
After Task 1: npx prisma validate exits 0; psql or docker exec shows Transcript and SchoolSettings tables exist
After Task 2: cd server && npx vitest run --reporter=verbose students.test.ts — students tests pass (nav-02-status uses Transcript JOIN)
</verification>

<success_criteria>
- npx prisma db push --accept-data-loss exits 0; Transcript + SchoolSettings tables live in PostgreSQL
- npx prisma generate exits 0; typed client includes transcript and schoolSettings
- server/src/services/transcript.ts implements getOrBuild (auto-populate on first open, records-updated banner), upsert, buildPdfHtml
- server/src/services/pdf.ts implements generatePdf with all 4 Windows Server Puppeteer flags
- server/src/services/settings.ts implements getSettings + upsertSettings (singleton pattern)
- server/src/services/student.ts listStudents JOINs Transcript for status; response field transcriptStatus still present (backward compat)
- @ts-expect-error stubs removed from testDb.ts and students.test.ts
</success_criteria>

<output>
Create `.planning/phases/05-transcript-assembly-export/05-02-SUMMARY.md` when done.
</output>

---

## Artifacts This Phase Produces (Plan 02)

**New files:**
- `server/src/services/transcript.ts` — exports: `TranscriptNotFoundError` (Error class), `buildAutoPopulatedContent(prisma, studentId)`, `computeMaxRecordTimestamp(prisma, studentId)`, `getOrBuild(prisma, studentId)`, `upsert(prisma, studentId, data, userId)`, `buildPdfHtml(transcript, settings, uploadRoot)`
- `server/src/services/pdf.ts` — exports: `generatePdf(html: string): Promise<Buffer>`
- `server/src/services/settings.ts` — exports: `getSettings(prisma)`, `upsertSettings(prisma, data, logoFile?, uploadRoot?, userId?)`

**Modified files:**
- `server/src/services/student.ts` — changed: listStudents adds `include: { transcript: { select: { status: true } } }` to findMany; transcriptStatus filter uses Transcript relation; response maps `transcript?.status ?? 'NONE'` to `transcriptStatus` field
- `server/src/__tests__/helpers/testDb.ts` — changed: @ts-expect-error stubs replaced with typed `prisma.transcript.deleteMany()` and `prisma.schoolSettings.deleteMany()`
- `server/src/__tests__/students.test.ts` — changed: (prisma as any).transcript.create() replaced with typed `prisma.transcript.create()`

**Database:**
- Table `Transcript` created in PostgreSQL with all columns per schema
- Table `SchoolSettings` created in PostgreSQL with all columns per schema
- Column `Student.transcriptStatus` dropped from PostgreSQL (--accept-data-loss)
</output>
