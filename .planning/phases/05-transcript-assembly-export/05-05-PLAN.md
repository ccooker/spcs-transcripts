---
phase: 05-transcript-assembly-export
plan: 05
type: execute
wave: 5
depends_on:
  - 05-03
  - 05-04
files_modified:
  - client/src/pages/SettingsPage.tsx
  - client/src/components/layout/AppShell.tsx
  - client/src/App.tsx
autonomous: true
requirements:
  - TRN-03

must_haves:
  truths:
    - "Admin sees Settings nav link in AppShell and can open /settings"
    - "Admin can save school name, address, letterhead HTML, and logo"
    - "Logo preview displays from /api/settings/logo after save"
    - "Staff navigating to /settings are redirected to /unauthorized"
  artifacts:
    - path: "client/src/pages/SettingsPage.tsx"
      provides: "Admin branding settings form"
      min_lines: 60
    - path: "client/src/components/layout/AppShell.tsx"
      provides: "Active Settings nav for Admin"
      contains: "settings"
  key_links:
    - from: "client/src/pages/SettingsPage.tsx"
      to: "/api/settings"
      via: "GET on load, PUT on save (JSON or FormData)"
      pattern: "/settings"
    - from: "client/src/components/layout/AppShell.tsx"
      to: "/settings"
      via: "Admin-only nav Link"
      pattern: "to=\"/settings\""
    - from: "client/src/App.tsx"
      to: "SettingsPage"
      via: "Route /settings"
      pattern: "SettingsPage"

---

## Phase Goal

**As a** careers staff member, **I want to** open any student's transcript, edit the narrative sections, set the status to Draft or Finalised, and export a branded PDF, **so that** I can produce a complete, professional transcript in a single session without hunting through spreadsheets or paper records.

<objective>
Deliver the Admin branding settings client slice: Admin can configure school name, address, logo, and letterhead HTML at /settings (per D-11–D-15), completing TRN-03 school branding configuration. Wire AppShell Settings nav and protect the route from Staff access.

Purpose: PDF export from Plan 04 uses SchoolSettings from the server — without this page, branding cannot be configured. Completes the phase goal success criterion #5.

Output: SettingsPage, live AppShell Settings nav, /settings route with Admin guard.
</objective>

<execution_context>
@C:/@code/spcs-transcripts/.cursor/gsd-core/workflows/execute-plan.md
@C:/@code/spcs-transcripts/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-transcript-assembly-export/05-UI-SPEC.md
@.planning/phases/05-transcript-assembly-export/05-PATTERNS.md
@.planning/phases/05-transcript-assembly-export/05-03-SUMMARY.md
@client/src/components/layout/AppShell.tsx
@client/src/pages/StudentDetailPage.tsx
@client/src/api/apiClient.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsPage branding form</name>
  <files>
    client/src/pages/SettingsPage.tsx
  </files>
  <read_first>
    - .planning/phases/05-transcript-assembly-export/05-UI-SPEC.md — Screen 5 Settings layout, form fields, copywriting, logo preview spec, save flow
    - client/src/pages/StudentDetailPage.tsx — AppShell usage, toast feedback, loading skeleton pattern
    - server/src/__tests__/settings.test.ts — PUT multipart .attach + .field pattern the client must mirror
    - client/src/api/apiClient.ts — apiFetch for FormData (must NOT set Content-Type application/json when sending multipart)
  </read_first>
  <action>
    Create client/src/pages/SettingsPage.tsx per D-11/D-12/D-13/D-14/D-15:

    Wrap in AppShell with activeNav="settings" (extend AppShell activeNav union type in Task 2 if needed — can use type assertion temporarily).

    On mount: apiGet Settings | null from /settings. If 403 (Staff), navigate to /unauthorized. Loading skeleton for form fields.

    Single Card titled "School branding" containing:
    - School name Input required (maxLength 100) with Label and inline validation "School name is required"
    - School address Textarea min-h-[80px] maxLength 300
    - Logo: hidden input type file accept image/*; Button outline "Choose image" triggers click; show selected filename; if settings.logoPath exists show img src="/api/settings/logo" h-20 w-20 object-contain rounded-md border with alt "Current school logo"; when new file selected show URL.createObjectURL preview
    - Letterhead HTML Textarea min-h-[240px] font-mono text-sm with hint below per UI-SPEC

    Save button primary "Save settings" at card foot. On submit: if logo file selected build FormData with schoolName, schoolAddress, letterheadHtml fields + logo file; apiFetch PUT /settings without JSON Content-Type. If no logo file, apiFetch PUT with JSON body. Disabled + "Saving…" while in flight. Success toast "Settings saved"; error toast per UI-SPEC. Re-fetch settings after save.

    Do NOT add upload Progress bar per D-15.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - client/src/pages/SettingsPage.tsx exports SettingsPage
    - SettingsPage contains "School branding" card title
    - SettingsPage contains "Save settings" button label
    - cd client && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Admin branding form page compiles and implements UI-SPEC field contract.</done>
</task>

<task type="auto">
  <name>Task 2: AppShell Settings nav + App route + Staff guard</name>
  <files>
    client/src/components/layout/AppShell.tsx,
    client/src/App.tsx
  </files>
  <read_first>
    - client/src/components/layout/AppShell.tsx — existing disabled Settings link stub (lines 58–68) to replace with live nav
    - .planning/phases/05-transcript-assembly-export/05-UI-SPEC.md — AppShell nav addition spec, Admin guard behavior
    - client/src/App.tsx — existing Route patterns; Plan 04 added transcript route — add settings route alongside
  </read_first>
  <action>
    Update client/src/components/layout/AppShell.tsx per D-11:

    Extend AppShellProps activeNav union to include 'settings'.

    Replace the disabled Settings link (aria-disabled, preventDefault) with a live Link to="/settings" using navLinkClass(activeNav === 'settings'). Add Lucide Settings icon before label. aria-label="Admin settings". Only render when userInfo?.role === 'ADMIN'.

    Update client/src/App.tsx: import SettingsPage. Add Route path="/settings" inside ProtectedRoute with SettingsPage userInfo={userInfo}. Place after transcript route.

    In SettingsPage (if not done in Task 1): on initial load if apiGet returns 403, useNavigate to /unauthorized — server enforces Admin but client should redirect gracefully per UI-SPEC.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - AppShell.tsx Settings link no longer has aria-disabled or preventDefault on settings link
    - AppShell.tsx contains to="/settings"
    - App.tsx contains path="/settings" and SettingsPage
    - cd client && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Admin can reach /settings from nav; Staff get /unauthorized; route wired in App.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → PUT /api/settings | Admin JWT required; letterhead HTML stored verbatim per D-14 |
| Logo file picker → server disk | image/* only; server validates MIME in Plan 03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Elevation of Privilege | /settings route | mitigate | Server requireRole(ADMIN); client redirects 403 to /unauthorized; nav hidden for Staff |
| T-05-05-02 | Tampering | letterheadHtml textarea | accept | Admin-only write per D-14; injected only into Puppeteer PDF not browser DOM |
| T-05-05-SC | Tampering | No new npm installs | accept | Reuses existing shadcn form primitives |

</threat_model>

<verification>
cd client && npx tsc --noEmit — no type errors
Manual: Admin login → Settings nav → save school name + logo → reload shows persisted values; Staff login → /settings redirects unauthorized
</verification>

<success_criteria>
- Admin configures school branding at /settings per TRN-03 and D-11–D-15
- Logo preview and letterhead HTML persist across reload
- Staff cannot access settings UI
- Phase 5 roadmap success criteria #5 satisfied
</success_criteria>

<output>
Create `.planning/phases/05-transcript-assembly-export/05-05-SUMMARY.md` when done.
</output>
