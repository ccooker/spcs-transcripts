---
phase: 05-transcript-assembly-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/prisma/schema.prisma
  - ecosystem.config.js
  - server/src/__tests__/transcript.test.ts
  - server/src/__tests__/settings.test.ts
  - server/src/__tests__/helpers/testDb.ts
  - server/src/__tests__/students.test.ts
autonomous: true
requirements:
  - TRN-01
  - TRN-02
  - TRN-03

must_haves:
  truths:
    - "npx prisma validate exits 0 after schema changes"
    - "schema.prisma does NOT contain transcriptStatus on Student"
    - "puppeteer is in server/node_modules; @tiptap/react and @tiptap/starter-kit are in client/node_modules"
    - "transcript.test.ts exists and all its tests fail RED (routes not yet mounted)"
    - "settings.test.ts exists and all its tests fail RED (routes not yet mounted)"
    - "students.test.ts no longer references prisma.student.update({ data: { transcriptStatus: 'DRAFT' } })"
  artifacts:
    - path: "server/prisma/schema.prisma"
      provides: "Transcript model + SchoolSettings model"
      contains: "model Transcript {"
    - path: "server/prisma/schema.prisma"
      provides: "SchoolSettings singleton schema"
      contains: "model SchoolSettings {"
    - path: "server/src/__tests__/transcript.test.ts"
      provides: "RED integration test stubs for TRN-01, TRN-02, TRN-03"
      min_lines: 60
    - path: "server/src/__tests__/settings.test.ts"
      provides: "RED integration test stubs for TRN-03 settings API"
      min_lines: 30
  key_links:
    - from: "server/src/__tests__/transcript.test.ts"
      to: "server/src/services/pdf.ts"
      via: "vi.mock('../services/pdf.js', ...)"
      pattern: "vi\\.mock.*services/pdf"
    - from: "server/src/__tests__/helpers/testDb.ts"
      to: "prisma.transcript"
      via: "@ts-expect-error stub (until Plan 02 prisma generate)"
      pattern: "transcript\\.deleteMany"
---

## Phase Goal

**As a** careers staff member, **I want to** open any student's transcript, edit the narrative sections, set the status to Draft or Finalised, and export a branded PDF, **so that** I can produce a complete, professional transcript in a single session without hunting through spreadsheets or paper records.

<objective>
Lay the foundation for Phase 5: extend the Prisma schema with the Transcript and SchoolSettings models (per D-12, D-16), remove the Student.transcriptStatus stub field (per D-16), install required packages (puppeteer, @tiptap/react, @tiptap/starter-kit, shadcn switch), configure PUPPETEER_CACHE_DIR in ecosystem.config.js, and write all RED integration test stubs so Wave 0 is complete before any implementation begins.

Purpose: Schema-first approach ensures type safety across the plan set. RED tests define the contract before implementation — every subsequent plan has a clear GREEN target.

Output: Updated schema.prisma (2 new models, 1 removed field), 2 new test files (transcript.test.ts, settings.test.ts), updated testDb.ts + students.test.ts, installed npm packages, ecosystem.config.js PUPPETEER_CACHE_DIR entry.
</objective>

<execution_context>
@C:/@code/spcs-transcripts/.cursor/gsd-core/workflows/execute-plan.md
@C:/@code/spcs-transcripts/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-transcript-assembly-export/05-CONTEXT.md
@.planning/phases/05-transcript-assembly-export/05-RESEARCH.md
@.planning/phases/05-transcript-assembly-export/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prisma schema + npm installs + ecosystem.config.js</name>
  <files>
    server/prisma/schema.prisma,
    ecosystem.config.js
  </files>
  <read_first>
    - server/prisma/schema.prisma — read the full file to understand current Student model structure (transcriptStatus field location, index locations, relation block, TranscriptStatus enum usage)
    - ecosystem.config.js — read to understand env_production block structure before adding PUPPETEER_CACHE_DIR
    - .planning/phases/05-transcript-assembly-export/05-PATTERNS.md sections "server/prisma/schema.prisma" and "server/src/app.ts" — exact field names, index names, FK patterns, and Student model modification points
    - .planning/phases/05-transcript-assembly-export/05-RESEARCH.md Pattern 4 — canonical Prisma schema for Transcript and SchoolSettings models
  </read_first>
  <behavior>
    - After schema edit: npx prisma validate exits 0 (no syntax errors)
    - schema.prisma contains `model Transcript {` with fields: id, studentId @unique, student relation onDelete: Cascade, status TranscriptStatus @default(DRAFT), 6×Content String? @db.Text, 6×Visible Boolean @default(true), createdAt, updatedAt, @@index([studentId]), @@index([status])
    - schema.prisma contains `model SchoolSettings {` with fields: id @default("singleton"), schoolName, schoolAddress @db.Text, letterheadHtml @db.Text, logoPath, updatedAt
    - schema.prisma Student model has `transcript Transcript?` relation and does NOT contain `transcriptStatus TranscriptStatus @default(NONE)` or `@@index([transcriptStatus])`
    - ecosystem.config.js env_production block contains PUPPETEER_CACHE_DIR: 'C:/ProgramData/puppeteer'
    - server/node_modules/puppeteer/ directory exists after npm install
    - client/node_modules/@tiptap/react/ and client/node_modules/@tiptap/starter-kit/ exist after npm install
  </behavior>
  <action>
    In server/prisma/schema.prisma:

    1. Locate the Student model block. Remove the field `transcriptStatus TranscriptStatus @default(NONE)` and its corresponding index `@@index([transcriptStatus])`. Add `transcript Transcript?` to the relations block at the bottom of the Student model (after the existing `documents Document[]` line).

    2. Append the Transcript model after the existing models (before or after SchoolSettings, order does not matter). Use these exact field names — academicsContent, activitiesContent, awardsContent, workExperienceContent, careerGoalsContent, staffEndorsementContent (all String? @db.Text), academicsVisible, activitiesVisible, awardsVisible, workExperienceVisible, careerGoalsVisible, staffEndorsementVisible (all Boolean @default(true)), status TranscriptStatus @default(DRAFT), studentId String @unique with Student relation (onDelete: Cascade), createdAt DateTime @default(now()), updatedAt DateTime @updatedAt. Include @@index([studentId]) and @@index([status]).

    3. Append the SchoolSettings model with id String @id @default("singleton"), schoolName String, schoolAddress String? @db.Text, letterheadHtml String? @db.Text, logoPath String?, updatedAt DateTime @updatedAt. No createdAt — this is a singleton.

    4. Do NOT remove the TranscriptStatus enum — it is still used by Transcript.status. The enum values NONE/DRAFT/FINALISED remain valid.

    In ecosystem.config.js: inside the env_production object, add PUPPETEER_CACHE_DIR: 'C:/ProgramData/puppeteer' after the BOOTSTRAP_ADMIN_EMAIL entry. This sets the Chromium cache path for the Windows SYSTEM account (PM2 runs as SYSTEM via pm2-service — same pattern as PM2_HOME from Phase 1 per D-01 RESEARCH.md Pitfall 1).

    Run (in sequence, not parallel):
    - cd server && npm install puppeteer
    - cd client && npx shadcn@latest add switch (answer any prompts with default/yes)
    - cd client && npm install @tiptap/react@3.26.1 @tiptap/starter-kit@3.26.1

    After installs: cd server && npx prisma validate
  </action>
  <verify>
    <automated>cd server && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - npx prisma validate exits 0 with no errors
    - server/prisma/schema.prisma contains the string "model Transcript {" (grep check)
    - server/prisma/schema.prisma contains the string "model SchoolSettings {" (grep check)
    - server/prisma/schema.prisma does NOT contain the string "transcriptStatus TranscriptStatus" (grep check — destructive removal confirmed)
    - server/prisma/schema.prisma does NOT contain "@@index([transcriptStatus])" (grep check)
    - server/prisma/schema.prisma contains "transcript  Transcript?" in Student model block
    - ecosystem.config.js contains "PUPPETEER_CACHE_DIR" string
    - server/package.json dependencies contains "puppeteer" key
    - client/package.json dependencies contains "@tiptap/react" key
    - client/package.json dependencies contains "@tiptap/starter-kit" key
  </acceptance_criteria>
  <done>Prisma schema validates cleanly with 2 new models; packages installed; ecosystem.config.js updated with Puppeteer cache path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED test stubs — transcript.test.ts, settings.test.ts, testDb.ts, students.test.ts</name>
  <files>
    server/src/__tests__/transcript.test.ts,
    server/src/__tests__/settings.test.ts,
    server/src/__tests__/helpers/testDb.ts,
    server/src/__tests__/students.test.ts
  </files>
  <read_first>
    - server/src/__tests__/documents.test.ts — read FULL file; this is the exact structural analog for transcript.test.ts and settings.test.ts (test file header, vi mock pattern, beforeEach, createTestStudent helper, describe block, it block shape, audit log assertion pattern)
    - server/src/__tests__/helpers/testDb.ts — read full file; understand clearDb() deletion order (FK constraints); Transcript has FK to Student so must be deleted before Student.deleteMany()
    - server/src/__tests__/students.test.ts — read full file; identify all 7 transcriptStatus references at lines 40, 61, 131, 335, 372, 377, 383 and understand what each assertion/seeding step does
    - server/src/__tests__/helpers/makeTestToken.ts — read to understand whether it supports a 'roles' override claim for Admin token generation in settings.test.ts
    - .planning/phases/05-transcript-assembly-export/05-RESEARCH.md "Validation Architecture" and "Wave 0 Gaps" sections — exact list of tests to stub
    - .planning/phases/05-transcript-assembly-export/05-PATTERNS.md sections "transcript.test.ts", "settings.test.ts", "helpers/testDb.ts", "students.test.ts" — exact code patterns to apply
  </read_first>
  <behavior>
    - transcript.test.ts: npx vitest run --reporter=verbose transcript.test.ts shows ALL tests FAILING (RED); no compile errors; vi.mock for pdf service is at file top before any imports
    - settings.test.ts: npx vitest run --reporter=verbose settings.test.ts shows ALL tests FAILING (RED); Admin 403 test, GET settings test, PUT settings test, logo GET test all RED
    - testDb.ts clearDb(): contains transcript.deleteMany() BEFORE auditLog.deleteMany() and BEFORE student.deleteMany() (FK order); uses @ts-expect-error because prisma generate has not run yet
    - students.test.ts line 335: uses prisma.transcript.create (not prisma.student.update with transcriptStatus); uses @ts-expect-error or (prisma as any) cast until prisma generate runs in Plan 02
    - students.test.ts lines 61, 131: transcriptStatus: 'NONE' response assertions UNCHANGED (API response still returns field named transcriptStatus — backward compat)
    - students.test.ts line 40 type assertion: UNCHANGED (keep transcriptStatus: string in type)
    - students.test.ts lines 372–383 nav-02-status test filter query params (transcriptStatus=NONE etc.) and response assertions: UNCHANGED — only the seed data creation changes (from direct Student.transcriptStatus update to Transcript.create)
  </behavior>
  <action>
    Create server/src/__tests__/transcript.test.ts:

    File structure: vi.mock('../services/pdf.js', ...) placed BEFORE all other imports at the very top of the file (Vitest requires mock declarations before the mocked module is imported). Mock returns Buffer.from('%PDF-1.4'). Then normal imports: process.env.UPLOAD_ROOT (set to os.tmpdir()), describe/it/expect/beforeEach from vitest, request from supertest, app from '../app.js', makeTestToken, clearDb/prisma from testDb.js.

    Include a staffToken() helper and a createTestStudent() helper (identical pattern to documents.test.ts).

    Write these test stubs (all should fail with 404/500 initially since routes don't exist):
    - trn-01-auto-populate: GET /api/students/:id/transcript with no saved narrative → expect 200 with autoPopulated: true and non-null academicsContent (requires student with academic results)
    - trn-01-put-save: PUT /api/students/:id/transcript with { academicsContent: '<p>Test</p>', status: 'DRAFT' } → expect 200; subsequent GET returns saved academicsContent
    - trn-01-visibility: PUT /api/students/:id/transcript with { academicsVisible: false } → expect 200; GET returns academicsVisible: false
    - trn-01-records-banner: seed a Transcript with updatedAt in the past; update an AcademicResult; GET transcript → showRecordsBanner: true
    - trn-02-status: PUT /api/students/:id/transcript { status: 'FINALISED' } → expect 200 with status: 'FINALISED'
    - trn-03-export: POST /api/students/:id/transcript/export → expect 200, Content-Type: application/pdf, response body starts with %PDF (mocked pdfService returns Buffer.from('%PDF-1.4'))
    - trn-idor-get: GET /api/students/:otherId/transcript with valid token but otherId not belonging to authenticated user → expect 404
    - trn-idor-put: PUT /api/students/:nonExistentId/transcript → expect 404

    Create server/src/__tests__/settings.test.ts:

    Include adminToken() helper (using makeTestToken with roles: ['ADMIN'] OR manually update user to ADMIN role via prisma after the first /api/auth/me call — check makeTestToken.ts first and use whichever approach works).

    Write these test stubs:
    - set-01-staff-forbidden: PUT /api/settings with staffToken → expect 403
    - set-02-get-empty: GET /api/settings (Admin) → expect 200 with null or empty object (no settings yet)
    - set-03-put-upsert: PUT /api/settings with { schoolName: 'SPCS', schoolAddress: '123 School Rd' } (Admin) → expect 200; second PUT with different schoolName → expect 200; GET returns updated schoolName (no duplicate row)
    - set-04-logo-get: PUT /api/settings with a small PNG file in logo field (Admin) → GET /api/settings/logo → expect 200 with Content-Type: image/png

    Update server/src/__tests__/helpers/testDb.ts:

    Add at the VERY START of clearDb() function body (before all other deleteMany calls, because even though Transcript CASCADE deletes from Student, schoolSettings has no FK and transcript must be explicit):
    - Line 1 of clearDb(): // @ts-expect-error — transcript not in generated client until Plan 02 prisma generate
    - Line 2: await (prisma as any).transcript.deleteMany()
    - Line 3: // @ts-expect-error — schoolSettings not in generated client until Plan 02 prisma generate
    - Line 4: await (prisma as any).schoolSettings.deleteMany()

    Update server/src/__tests__/students.test.ts:

    Line 335 area — replace `prisma.student.update({ where: { id: lee.id }, data: { transcriptStatus: 'DRAFT' } })` with:
    // @ts-expect-error — transcript model not in generated client until Plan 02
    await (prisma as any).transcript.create({ data: { studentId: lee.id, status: 'DRAFT' } })

    Lines 372–383 nav-02-status test: if similar NONE/FINALISED seeding exists using prisma.student.update with transcriptStatus, change them to (prisma as any).transcript.create with { studentId: ..., status: '...' } using the same @ts-expect-error pattern. The filter query params (?transcriptStatus=NONE etc.) and response body assertions (row.transcriptStatus) MUST remain unchanged — the API contract keeps the field name transcriptStatus in the response.

    Lines 40, 61, 131: No changes required — these are type assertion and response assertion using the field name transcriptStatus, which the API will continue to return.

    After all changes: run npx vitest run --reporter=verbose transcript.test.ts to confirm RED. Run npx vitest run --reporter=verbose settings.test.ts to confirm RED.
  </action>
  <verify>
    <automated>cd server && npx vitest run --reporter=verbose transcript.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - cd server && npx vitest run --reporter=verbose transcript.test.ts exits non-zero (tests FAIL as expected); output shows at least 8 test names; no TypeScript compilation errors in output
    - cd server && npx vitest run --reporter=verbose settings.test.ts exits non-zero (tests FAIL as expected); output shows at least 4 test names
    - server/src/__tests__/transcript.test.ts line 1 contains "vi.mock" (mock must be first before imports)
    - server/src/__tests__/helpers/testDb.ts contains "transcript.deleteMany" string
    - server/src/__tests__/helpers/testDb.ts contains "schoolSettings.deleteMany" string
    - server/src/__tests__/students.test.ts does NOT contain "data: { transcriptStatus: 'DRAFT' }" string (the student.update seed removed)
    - server/src/__tests__/students.test.ts contains "transcript" string (new Transcript.create seeding)
  </acceptance_criteria>
  <done>Wave 0 complete: all RED test stubs exist, testDb.ts and students.test.ts updated with @ts-expect-error stubs for new Prisma models; full suite may show new failures in students.test.ts (expected until Plan 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → node_modules | Untrusted packages installed from npm registry |
| schema.prisma → PostgreSQL | Schema changes applied to production DB in Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | npm install puppeteer / @tiptap/react / @tiptap/starter-kit | mitigate | Package legitimacy audit performed in 05-RESEARCH.md: all 3 packages approved (official orgs, 5+ yrs history, 9M+ wkly downloads, MIT license); no checkpoint required |
| T-05-01 | Information Disclosure | Transcript data (per-student) | mitigate | IDOR guard: service checks transcript.studentId === studentId before serving; RED test trn-idor-get/trn-idor-put covers this path |
</threat_model>

<verification>
After Task 1: cd server && npx prisma validate exits 0
After Task 2: cd server && npx vitest run --reporter=verbose transcript.test.ts fails RED; npx vitest run --reporter=verbose settings.test.ts fails RED
</verification>

<success_criteria>
- Prisma schema validates cleanly with Transcript and SchoolSettings models, Student.transcriptStatus removed
- puppeteer, @tiptap/react@3.26.1, @tiptap/starter-kit@3.26.1 installed; shadcn switch added
- ecosystem.config.js contains PUPPETEER_CACHE_DIR: 'C:/ProgramData/puppeteer'
- transcript.test.ts: 8+ RED integration test stubs covering TRN-01, TRN-02, TRN-03
- settings.test.ts: 4+ RED integration test stubs for Admin guard, GET, PUT, logo
- testDb.ts clearDb() deletes transcript and schoolSettings with @ts-expect-error stubs
- students.test.ts line 335 area seeds Transcript record instead of updating Student.transcriptStatus
</success_criteria>

<output>
Create `.planning/phases/05-transcript-assembly-export/05-01-SUMMARY.md` when done.
</output>

---

## Artifacts This Phase Produces (Plan 01)

**New files:**
- `server/src/__tests__/transcript.test.ts` — exports nothing; integration test file; vi.mock for ../services/pdf.js; describes: trn-01-auto-populate, trn-01-put-save, trn-01-visibility, trn-01-records-banner, trn-02-status, trn-03-export, trn-idor-get, trn-idor-put
- `server/src/__tests__/settings.test.ts` — exports nothing; integration test file; describes: set-01-staff-forbidden, set-02-get-empty, set-03-put-upsert, set-04-logo-get

**Modified files:**
- `server/prisma/schema.prisma` — added: model Transcript, model SchoolSettings, Student.transcript relation; removed: Student.transcriptStatus, Student.@@index([transcriptStatus])
- `ecosystem.config.js` — added: env_production.PUPPETEER_CACHE_DIR = 'C:/ProgramData/puppeteer'
- `server/src/__tests__/helpers/testDb.ts` — added: prisma.transcript.deleteMany() + prisma.schoolSettings.deleteMany() (with @ts-expect-error) at start of clearDb()
- `server/src/__tests__/students.test.ts` — changed: line 335 area now uses (prisma as any).transcript.create instead of student.update(transcriptStatus); lines 40/61/131 response assertions unchanged

**New Prisma models (not yet in DB until Plan 02):**
- `Transcript`: id (uuid), studentId (unique FK → Student cascade), status (TranscriptStatus default DRAFT), academicsContent (Text?), activitiesContent (Text?), awardsContent (Text?), workExperienceContent (Text?), careerGoalsContent (Text?), staffEndorsementContent (Text?), academicsVisible (bool default true), activitiesVisible (bool), awardsVisible (bool), workExperienceVisible (bool), careerGoalsVisible (bool), staffEndorsementVisible (bool), createdAt, updatedAt
- `SchoolSettings`: id (default "singleton"), schoolName (String), schoolAddress (Text?), letterheadHtml (Text?), logoPath (String?), updatedAt
</output>
