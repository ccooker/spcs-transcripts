---
phase: 03-student-records-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - server/prisma/schema.prisma
  - server/src/routes/students.ts
  - server/src/routes/awards.ts
  - server/src/routes/workExperience.ts
  - server/src/services/award.ts
  - server/src/services/workExperience.ts
  - server/src/schemas/award.ts
  - server/src/schemas/workExperience.ts
  - server/src/__tests__/records.test.ts
  - server/src/__tests__/helpers/testDb.ts
  - client/src/components/records/AwardsSection.tsx
  - client/src/components/records/WorkExperienceSection.tsx
  - client/src/pages/StudentDetailPage.tsx
autonomous: true
requirements:
  - STU-05
  - STU-06

must_haves:
  truths:
    - "RED integration tests for stu-05 (awards) and stu-06 (work experience) exist in records.test.ts and fail (no DB tables yet)"
    - "GET/POST/PATCH/:awardId/DELETE/:awardId routes registered under /api/students/:studentId/awards"
    - "GET/POST/PATCH/:workExpId/DELETE/:workExpId routes registered under /api/students/:studentId/work-experience"
    - "Award service: awardLevel validated as AwardLevel enum (SCHOOL/REGIONAL/STATE/NATIONAL/INTERNATIONAL)"
    - "WorkExperience service: null-end-date entries sort before dated entries (nulls:'first')"
    - "IDOR guard on both services: PATCH/DELETE with wrong studentId returns 404"
    - "AwardsSection rendered in StudentDetailPage below ActivitiesSection; shows award level Badge per UI-SPEC color tokens"
    - "WorkExperienceSection rendered below AwardsSection; formatPeriod used for period column"
    - "Empty states display correct copy: 'No awards yet.' / 'No work experience yet.'"
  artifacts:
    - path: "server/src/routes/awards.ts"
      provides: "CRUD router with mergeParams:true — GET / POST / PATCH /:awardId / DELETE /:awardId"
      exports: ["default router"]
    - path: "server/src/routes/workExperience.ts"
      provides: "CRUD router with mergeParams:true — GET / POST / PATCH /:workExpId / DELETE /:workExpId"
      exports: ["default router"]
    - path: "server/src/services/award.ts"
      provides: "CRUD + IDOR guard + logAudit"
      exports: ["AwardNotFoundError", "listAwards", "createAward", "updateAward", "deleteAward"]
    - path: "server/src/services/workExperience.ts"
      provides: "CRUD + IDOR guard + logAudit + null-first end-date sort"
      exports: ["WorkExperienceNotFoundError", "listWorkExperiences", "createWorkExperience", "updateWorkExperience", "deleteWorkExperience"]
    - path: "server/src/schemas/award.ts"
      provides: "Zod create + update schemas with AwardLevel enum and monthSchema/yearSchema"
      exports: ["createAwardSchema", "updateAwardSchema"]
    - path: "server/src/schemas/workExperience.ts"
      provides: "Zod create + update schemas with monthSchema/yearSchema"
      exports: ["createWorkExperienceSchema", "updateWorkExperienceSchema"]
    - path: "client/src/components/records/AwardsSection.tsx"
      provides: "Table+Dialog CRUD UI for awards; AwardLevel Badge display; MonthYearPicker for award date"
      exports: ["AwardsSection"]
    - path: "client/src/components/records/WorkExperienceSection.tsx"
      provides: "Table+Dialog CRUD UI for work experience; MonthYearPicker + Ongoing checkbox; period formatting"
      exports: ["WorkExperienceSection"]
  key_links:
    - from: "server/src/routes/students.ts"
      to: "server/src/routes/awards.ts"
      via: "router.use('/:studentId/awards', awardsRouter)"
      pattern: "router\\.use.*awards"
    - from: "server/src/routes/students.ts"
      to: "server/src/routes/workExperience.ts"
      via: "router.use('/:studentId/work-experience', workExperienceRouter)"
      pattern: "router\\.use.*work-experience"
    - from: "client/src/components/records/AwardsSection.tsx"
      to: "/api/students/:id/awards"
      via: "useQuery({ queryKey: ['student', studentId, 'awards'] })"
      pattern: "useQuery.*awards"
---

<objective>
Awards & Work Experience vertical slice: write RED integration tests for both record types, add Award and WorkExperience Prisma models to the existing schema (D-13 Award.level enum already declared in Plan 01), implement all backend layers (Zod schemas with AwardLevel enum and month+year fields, services with IDOR guard + audit, nested routes), and build AwardsSection + WorkExperienceSection components including the award level Badge mapping.

Purpose: Completes the four "list-style" record types (academics + activities already done in Plan 01; awards + work experience complete the set). Tests remain RED until Plan 03 DB push. UI sections are functional code-layer complete.

Output: RED test stubs (stu-05, stu-06), Award + WorkExperience backend (schema models, Zod, services, routes), AwardsSection + WorkExperienceSection components added to StudentDetailPage.
</objective>

<execution_context>
@C:/@code/spcs-transcripts/.cursor/gsd-core/workflows/execute-plan.md
@C:/@code/spcs-transcripts/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-student-records-ui/03-CONTEXT.md
@.planning/phases/03-student-records-ui/03-RESEARCH.md
@.planning/phases/03-student-records-ui/03-UI-SPEC.md
@.planning/phases/03-student-records-ui/03-PATTERNS.md
@.planning/phases/03-student-records-ui/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED integration tests for awards (stu-05) and work experience (stu-06)</name>
  <files>
    server/src/__tests__/records.test.ts
    server/src/__tests__/helpers/testDb.ts
  </files>
  <read_first>
    - server/src/__tests__/records.test.ts — current state from Plan 01; append stu-05 and stu-06 describe blocks after existing stu-03/stu-04 blocks
    - server/src/__tests__/helpers/testDb.ts — current clearDb(); extend to add award + workExperience deletes after activity but before academicResult
    - server/src/__tests__/students.test.ts — createTestStudent helper pattern (already in records.test.ts — no need to duplicate)
    - server/prisma/schema.prisma — confirm Award and WorkExperience models do NOT yet exist
  </read_first>
  <behavior>
    - stu-05-create: POST /api/students/:id/awards with title, issuer, awardMonth:3, awardYear:2024, level:'NATIONAL', description returns 201 with all fields
    - stu-05-list: GET /api/students/:id/awards returns 200 with array sorted by awardYear desc, awardMonth desc
    - stu-05-update: PATCH /api/students/:id/awards/:awardId with level:'INTERNATIONAL' returns 200
    - stu-05-delete: DELETE /api/students/:id/awards/:awardId returns 204
    - stu-05-invalid-level: POST with level:'OLYMPIC' returns 400 (invalid enum)
    - stu-05-idor: PATCH with award belonging to different student returns 404
    - stu-05-audit: POST creates AuditLog row model:'Award' action:'CREATE'
    - stu-06-create: POST /api/students/:id/work-experience with employer, role, startMonth:6, startYear:2023 (no endMonth/endYear) returns 201
    - stu-06-ongoing-sort: work experience with null endYear sorts before dated entry in GET response
    - stu-06-idor: PATCH with work experience belonging to different student returns 404
  </behavior>
  <action>
    In testDb.ts: extend clearDb() — add prisma.award.deleteMany() and prisma.workExperience.deleteMany() at the correct FK-safe positions. Full order should now be: staffNote → careerGoal → workExperience → award → activity → academicResult → auditLog → student → user. Add workExperience and award now; staffNote + careerGoal stubs will be added in Plan 03. Use @ts-expect-error stubs for any types not yet generated.

    In records.test.ts: append stu-05 describe block after stu-04 block. Valid award payload: { title:'Best Science Project', issuer:'HKSAR Education Bureau', awardMonth:3, awardYear:2024, level:'NATIONAL', description:'Regional winner' }. Include stu-05-invalid-level test: POST with level:'OLYMPIC' → expect 400. Include stu-05-audit test: check prisma.auditLog.findMany({ where:{ model:'Award', action:'CREATE', recordId:res.body.id } }) has length 1.

    Append stu-06 describe block. Valid work experience payload: { employer:'HSBC Hong Kong', role:'Summer Intern', startMonth:6, startYear:2023 } (no endMonth/endYear). Include stu-06-ongoing-sort test: create two entries — one with endYear:2022, one without endYear — GET → verify null-endYear entry appears first in response array.

    Tests MUST FAIL (routes return 404 — expected RED state).
  </action>
  <acceptance_criteria>
    - records.test.ts contains describe blocks named with 'stu-05' and 'stu-06'
    - stu-05-invalid-level test: POST with level:'OLYMPIC' expects status 400
    - stu-06-ongoing-sort test: checks first element of GET response has null endYear
    - testDb.ts clearDb() contains workExperience and award deletes
    - `cd server && npm test -- --reporter=verbose 2>&1 | grep -E "stu-05|stu-06"` shows FAIL (not compile error)
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test -- --reporter=verbose 2>&1 | grep -E "(stu-05|stu-06|FAIL|PASS)" | head -20</automated>
  </verify>
  <done>stu-05 and stu-06 describe blocks in records.test.ts failing as expected; testDb extended with award + workExperience deletes</done>
</task>

<task type="auto">
  <name>Task 2: Award + WorkExperience — schema models, Prisma generate, Zod schemas, services, routes, students.ts mounts</name>
  <files>
    server/prisma/schema.prisma
    server/src/schemas/award.ts
    server/src/schemas/workExperience.ts
    server/src/services/award.ts
    server/src/services/workExperience.ts
    server/src/routes/awards.ts
    server/src/routes/workExperience.ts
    server/src/routes/students.ts
    server/src/__tests__/helpers/testDb.ts
  </files>
  <read_first>
    - server/prisma/schema.prisma — current state after Plan 01 (has AcademicResult, Activity, AwardLevel enum, CareerInterest enum); append Award and WorkExperience models
    - server/src/services/academicResult.ts — exact IDOR guard + logAudit pattern to replicate for Award and WorkExperience
    - server/src/services/activity.ts — null-first orderBy pattern to replicate for WorkExperience (same null-end-date sort requirement)
    - server/src/schemas/activity.ts — monthSchema/yearSchema helpers to replicate
    - server/src/routes/academicResults.ts — full file; replicate this structure exactly for awards.ts and workExperience.ts (only names change)
    - server/src/routes/students.ts — current mounts at bottom; add awards and work-experience mounts
    - .planning/phases/03-student-records-ui/03-RESEARCH.md — Pattern 1 (Award + WorkExperience schema definitions), Pattern 3 (service IDOR guard)
  </read_first>
  <action>
    SCHEMA (server/prisma/schema.prisma): Append Award model: id String @id @default(uuid()), studentId String, student Student @relation(fields:[studentId], references:[id], onDelete:Cascade), title String, issuer String, awardMonth Int, awardYear Int, level AwardLevel, description String? @db.VarChar(500), createdAt DateTime @default(now()), updatedAt DateTime @updatedAt, @@index([studentId]), @@index([studentId, awardYear(sort:Desc), awardMonth(sort:Desc)]).

    Append WorkExperience model: id String @id @default(uuid()), studentId String, student Student @relation(fields:[studentId], references:[id], onDelete:Cascade), employer String, role String, description String? @db.VarChar(500), startMonth Int, startYear Int, endMonth Int?, endYear Int?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt, @@index([studentId]).

    Add back-relations to Student model: awards Award[] and workExperiences WorkExperience[].

    RUN: cd server && npx prisma generate

    Update testDb.ts: replace @ts-expect-error stubs for award and workExperience with real deleteMany calls now that types exist.

    SCHEMAS: Create server/src/schemas/award.ts — reuse monthSchema and yearSchema helpers (copy from activity.ts or define locally). createAwardSchema: z.object({ title:z.string().trim().min(1).max(200), issuer:z.string().trim().min(1).max(200), awardMonth:monthSchema, awardYear:yearSchema, level:z.enum(['SCHOOL','REGIONAL','STATE','NATIONAL','INTERNATIONAL']), description:z.string().trim().max(500).optional() }).strict(). updateAwardSchema: createAwardSchema.partial().

    Create server/src/schemas/workExperience.ts — copy createActivitySchema structure replacing organisation/role fields with employer/role and omitting description difference. createWorkExperienceSchema: z.object({ employer:z.string().trim().min(1).max(200), role:z.string().trim().min(1).max(200), description:z.string().trim().max(500).optional(), startMonth:monthSchema, startYear:yearSchema, endMonth:monthSchema.optional().nullable(), endYear:yearSchema.optional().nullable() }).strict(). updateWorkExperienceSchema: createWorkExperienceSchema.partial().

    SERVICES: Create server/src/services/award.ts — AwardNotFoundError class; listAwards(prisma, studentId): findMany where studentId, orderBy [awardYear desc, awardMonth desc]; createAward(prisma, studentId, data, userId): create + logAudit model:'Award' action:'CREATE'; updateAward(prisma, id, studentId, data, userId): IDOR check (findUnique → check studentId → throw if mismatch) + update + logAudit model:'Award' action:'UPDATE'; deleteAward(prisma, id, studentId, userId): IDOR check + delete + logAudit model:'Award' action:'DELETE'.

    Create server/src/services/workExperience.ts — WorkExperienceNotFoundError; listWorkExperiences with same null-first orderBy as activity.ts: [{ endYear:{ sort:'desc', nulls:'first' } }, { endMonth:{ sort:'desc', nulls:'first' } }, { startYear:'desc' }, { startMonth:'desc' }]; createWorkExperience + logAudit model:'WorkExperience'; updateWorkExperience with IDOR check + logAudit; deleteWorkExperience with IDOR check + logAudit.

    ROUTES: Create server/src/routes/awards.ts — Router({ mergeParams:true }), parseStudentId helper reading req.params.studentId, GET/POST/PATCH/:awardId/DELETE/:awardId following the exact academicResults.ts pattern. AwardNotFoundError caught → 404.

    Create server/src/routes/workExperience.ts — same pattern; WorkExperienceNotFoundError.

    MOUNT: In server/src/routes/students.ts, append: import awardsRouter from './awards.js'; import workExperienceRouter from './workExperience.js'; router.use('/:studentId/awards', awardsRouter); router.use('/:studentId/work-experience', workExperienceRouter).
  </action>
  <acceptance_criteria>
    - server/prisma/schema.prisma contains model Award with level AwardLevel field and model WorkExperience; Student back-relations include awards and workExperiences
    - server/src/schemas/award.ts: createAwardSchema level field is z.enum(['SCHOOL','REGIONAL','STATE','NATIONAL','INTERNATIONAL'])
    - server/src/services/award.ts: updateAward contains IDOR check `existing.studentId !== studentId`
    - server/src/services/workExperience.ts: listWorkExperiences orderBy contains `nulls: 'first'` on endYear
    - server/src/routes/awards.ts: Router({ mergeParams: true }) present; DELETE returns 204
    - server/src/routes/students.ts: contains `router.use('/:studentId/awards'` and `router.use('/:studentId/work-experience'`
    - `cd server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | tail -5 && echo "TypeScript OK"</automated>
  </verify>
  <done>Award + WorkExperience models in schema; Zod schemas, services, routes created; mounted in students.ts; server compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 3: AwardsSection + WorkExperienceSection components; add to StudentDetailPage</name>
  <files>
    client/src/components/records/AwardsSection.tsx
    client/src/components/records/WorkExperienceSection.tsx
    client/src/pages/StudentDetailPage.tsx
  </files>
  <read_first>
    - client/src/components/records/AcademicResultsSection.tsx — full file from Plan 01; this is the exact analog to replicate for AwardsSection (same useQuery/useMutation structure, same Dialog/Form pattern, same table+actions layout)
    - client/src/components/records/ActivitiesSection.tsx — full file from Plan 01; WorkExperienceSection is structurally identical (employer/role instead of organisation/role, same MonthYearPicker + ongoing pattern)
    - client/src/components/records/MonthYearPicker.tsx — import and usage pattern
    - client/src/lib/periodFormat.ts — formatPeriod usage for WorkExperience period column
    - client/src/pages/StudentDetailPage.tsx — current state from Plan 01 (has AcademicResultsSection + ActivitiesSection in flex-col gap-8 div); add AwardsSection + WorkExperienceSection after ActivitiesSection
    - .planning/phases/03-student-records-ui/03-UI-SPEC.md — Section 3 (Awards columns/dialog/award level badge variants), Section 4 (Work Experience columns/dialog), Copywriting Contract (add award/work experience dialog titles, toast copy, empty state copy)
  </read_first>
  <action>
    client/src/components/records/AwardsSection.tsx (NEW): Props: studentId:string. Type Award: { id:string, title:string, issuer:string, awardMonth:number, awardYear:number, level:'SCHOOL'|'REGIONAL'|'STATE'|'NATIONAL'|'INTERNATIONAL', description:string|null }. useQuery queryKey=['student',studentId,'awards'], queryFn=apiGet('/students/${studentId}/awards'). useMutation for create (POST), update (PATCH), delete (DELETE) with invalidateQueries on success. Toasts per 03-UI-SPEC Copywriting: 'Award added', 'Award updated', 'Entry deleted', error: "Couldn't save changes. Please try again." / "Couldn't delete entry. Please try again.".

    RecordSectionCard: title="Awards", count={data.length}, addLabel="Add award", emptyHeading="No awards yet.", emptyBody="Add awards and achievements to recognise this student's accomplishments.". Table columns: Title, Issuer, Date (formatMonthYear(awardMonth, awardYear)), Level (Badge), Actions (Pencil + Trash ghost icon buttons).

    Award Level Badge mapping (per 03-UI-SPEC color table): SCHOOL → Badge variant="secondary"; REGIONAL → Badge variant="outline"; STATE → Badge variant="default"; NATIONAL → Badge variant="default"; INTERNATIONAL → Badge variant="default" with className containing font-semibold. Helper function awardLevelBadge(level): returns {variant, className?} object.

    Dialog title: "Add award" / "Edit award". Form fields per 03-UI-SPEC Section 3: Title (Input required placeholder="e.g. Best Science Project"), Issuer (Input required placeholder="e.g. HKSAR Education Bureau"), Award month/year (MonthYearPicker label="Award date" required), Level (Select required options: School/Regional/State/National/International mapping enum values to display labels), Description (Textarea placeholder="Brief description" maxLength=500 — use a basic textarea element styled to match if shadcn textarea not yet installed; shadcn textarea is installed in Plan 03). Dialog footer: "Discard" + submit "Add award"/"Save award", isPending?"Saving…". RecordDeleteDialog for delete confirmation.

    client/src/components/records/WorkExperienceSection.tsx (NEW): Structurally identical to ActivitiesSection. Props: studentId:string. Type WorkExperience: { id:string, employer:string, role:string, description:string|null, startMonth:number, startYear:number, endMonth:number|null, endYear:number|null }. useQuery queryKey=['student',studentId,'work-experience']. RecordSectionCard: title="Work experience", addLabel="Add work experience", emptyHeading="No work experience yet.", emptyBody="Add work experience entries to document this student's employment history.". Table columns: Employer, Role, Period (formatPeriod), Actions. Dialog: Employer (Input required placeholder="e.g. HSBC Hong Kong"), Role (Input required placeholder="e.g. Summer Intern"), Description (Textarea/textarea placeholder="Brief description of role and responsibilities" maxLength=500), Start date (MonthYearPicker required), End date (MonthYearPicker) + Ongoing/Present Checkbox. On Ongoing checked: set endMonth/endYear null, disable end MonthYearPicker. Dialog footer: "Discard" + "Add work experience"/"Save work experience".

    client/src/pages/StudentDetailPage.tsx: In the flex flex-col gap-8 div, add AwardsSection and WorkExperienceSection after ActivitiesSection:
    &lt;AwardsSection studentId={student.id} /&gt;
    &lt;WorkExperienceSection studentId={student.id} /&gt;
    {/* CareerGoals and Notes sections added in Plan 03 */}
    Add imports for AwardsSection and WorkExperienceSection.
  </action>
  <acceptance_criteria>
    - client/src/components/records/AwardsSection.tsx: awardLevelBadge function maps INTERNATIONAL to variant="default" with font-semibold class; useQuery queryKey contains 'awards' segment
    - client/src/components/records/WorkExperienceSection.tsx: uses formatPeriod for Period column; Ongoing Checkbox sets endMonth and endYear to null; useQuery queryKey contains 'work-experience' segment
    - client/src/pages/StudentDetailPage.tsx: contains AwardsSection and WorkExperienceSection JSX within the flex-col div
    - `cd client && npx tsc --noEmit` exits 0
    - The section order in StudentDetailPage JSX is: AcademicResultsSection → ActivitiesSection → AwardsSection → WorkExperienceSection (per D-02)
  </acceptance_criteria>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | tail -5 && echo "Client TypeScript OK"</automated>
  </verify>
  <done>AwardsSection (with level Badge mapping) and WorkExperienceSection created; both added to StudentDetailPage in correct order; client compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/students/:id/awards | Untrusted: awardId and body from browser; JWT auth inherited |
| client → /api/students/:id/work-experience | Untrusted: workExpId and body from browser; JWT auth inherited |
| service → Prisma | Internal: parameterized queries; AwardLevel validated via Zod enum before reaching service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering / Info Disclosure | award + workExperience service update/delete | mitigate | IDOR guard: `if (!existing \|\| existing.studentId !== studentId) throw XNotFoundError` — always 404, never 403 (ASVS V4.2) |
| T-03-07 | Tampering | award + workExperience POST/PATCH routes | mitigate | Zod `.strict()` on createAwardSchema and createWorkExperienceSchema — unknown fields rejected with 400 |
| T-03-08 | Tampering | award POST — invalid level value | mitigate | z.enum(['SCHOOL','REGIONAL','STATE','NATIONAL','INTERNATIONAL']) in createAwardSchema; invalid level returns 400 (validated in stu-05-invalid-level test) |
| T-03-09 | Repudiation | award + workExperience services | mitigate | logAudit() called after every write; model:'Award' and model:'WorkExperience' respectively |
| T-03-10 | Spoofing | all /awards and /work-experience routes | accept | validateJwt + resolveUser global middleware inherited from Phase 1; req.user!.id available in all handlers |
| T-03-SC | Tampering | npm/shadcn installs | mitigate | No new npm packages in this plan; shadcn textarea referenced but installed in Plan 03 (shadcn official registry, pre-audited) |
</threat_model>

<verification>
- `cd server && npm test -- --reporter=verbose 2>&1 | grep -E "stu-05|stu-06"` shows RED (failing) test output
- `cd server && npx tsc --noEmit` exits 0
- `cd client && npx tsc --noEmit` exits 0
- server/src/routes/students.ts contains `router.use('/:studentId/awards'` and `router.use('/:studentId/work-experience'`
- server/src/services/award.ts contains IDOR check `existing.studentId !== studentId`
- server/src/services/workExperience.ts contains `nulls: 'first'` in orderBy
- client/src/pages/StudentDetailPage.tsx section order: AcademicResultsSection → ActivitiesSection → AwardsSection → WorkExperienceSection
</verification>

<success_criteria>
- Award and WorkExperience Prisma models in schema.prisma with AwardLevel enum, onDelete:Cascade, month+year integer fields, and correct indexes
- Award Zod schema validates level against AwardLevel enum; returns 400 for invalid values
- WorkExperience service uses null-first orderBy matching Activity service pattern (D-11)
- CRUD services for both types: IDOR guard, logAudit on every write, NotFoundError thrown → 404 in route
- routes/awards.ts and routes/workExperience.ts: Router({mergeParams:true}), parseStudentId, Zod safeParse, all 4 verbs
- AwardsSection: level column renders Badge with correct variant per 03-UI-SPEC Award Level Variants table; D-13 INTERNATIONAL uses font-semibold
- WorkExperienceSection: mirrors ActivitiesSection with employer/role fields; formatPeriod for period column; Ongoing checkbox nulls end-date
- StudentDetailPage: all four list-type sections (academics, activities, awards, work exp) rendered in D-02 order
- RED tests exist and fail as expected for STU-05 and STU-06
</success_criteria>

<output>
Create `.planning/phases/03-student-records-ui/03-02-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

**Server — new files:**
- `server/src/routes/awards.ts` — exports default Express Router with GET/POST/PATCH/:awardId/DELETE/:awardId
- `server/src/routes/workExperience.ts` — exports default Express Router with GET/POST/PATCH/:workExpId/DELETE/:workExpId
- `server/src/services/award.ts` — exports AwardNotFoundError, listAwards, createAward, updateAward, deleteAward
- `server/src/services/workExperience.ts` — exports WorkExperienceNotFoundError, listWorkExperiences, createWorkExperience, updateWorkExperience, deleteWorkExperience
- `server/src/schemas/award.ts` — exports createAwardSchema, updateAwardSchema
- `server/src/schemas/workExperience.ts` — exports createWorkExperienceSchema, updateWorkExperienceSchema

**Server — modified files:**
- `server/prisma/schema.prisma` — adds models Award, WorkExperience; back-relations awards[] + workExperiences[] on Student
- `server/src/routes/students.ts` — adds awardsRouter + workExperienceRouter mounts
- `server/src/__tests__/records.test.ts` — extended with stu-05 + stu-06 RED test groups
- `server/src/__tests__/helpers/testDb.ts` — clearDb() extended with award + workExperience deletes

**Client — new files:**
- `client/src/components/records/AwardsSection.tsx` — exports AwardsSection; awardLevelBadge helper mapping SCHOOL→secondary, REGIONAL→outline, STATE/NATIONAL/INTERNATIONAL→default
- `client/src/components/records/WorkExperienceSection.tsx` — exports WorkExperienceSection

**Client — modified files:**
- `client/src/pages/StudentDetailPage.tsx` — adds AwardsSection + WorkExperienceSection (now 4 of 6 sections present)
