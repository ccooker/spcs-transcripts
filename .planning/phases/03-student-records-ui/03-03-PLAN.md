---
phase: 03-student-records-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - server/prisma/schema.prisma
  - server/src/routes/students.ts
  - server/src/routes/careerGoals.ts
  - server/src/routes/notes.ts
  - server/src/services/careerGoal.ts
  - server/src/services/staffNote.ts
  - server/src/schemas/careerGoal.ts
  - server/src/schemas/staffNote.ts
  - server/src/__tests__/records.test.ts
  - server/src/__tests__/helpers/testDb.ts
  - server/prisma/migrations/20260612_add_records/migration.sql
  - client/src/components/records/CareerGoalsSection.tsx
  - client/src/components/records/NotesSection.tsx
  - client/src/components/records/CareerInterestsChecklist.tsx
  - client/src/pages/StudentDetailPage.tsx
autonomous: true
requirements:
  - STU-07
  - STU-08

must_haves:
  truths:
    - "GET /api/students/:id/career-goals returns all versions newest-first with author.displayName included"
    - "POST /api/students/:id/career-goals creates a new version (new row) — NOT an update to the existing version (D-16)"
    - "No PATCH or DELETE routes exist on /career-goals — any attempt returns 404 or 405 (append-only versioning per D-16)"
    - "GET /api/students/:id/notes returns notes newest-first with author.displayName"
    - "POST /api/students/:id/notes creates a note — no PATCH or DELETE routes registered (D-17)"
    - "All RED tests from Plans 01-03 (stu-03 through stu-08) are GREEN after schema push"
    - "npx prisma db push completes without error; all 6 new tables exist in PostgreSQL"
    - "CareerGoalsSection shows current version interests as Badge chips + description; version history collapsible toggle"
    - "NotesSection: inline textarea with Add note button + {n}/500 character count; notes listed newest-first with attribution (D-18, D-19)"
    - "CareerInterestsChecklist: 12-item grid (2 columns on md+); each item min 44px touch area (D-14)"
    - "All 6 section types visible on /students/:id in D-02 order (Academics → Activities → Awards → Work Exp → Career Goals → Notes)"
  artifacts:
    - path: "server/src/routes/careerGoals.ts"
      provides: "GET / and POST / only — no PATCH or DELETE (versioned pattern per D-16)"
      exports: ["default router"]
    - path: "server/src/routes/notes.ts"
      provides: "GET / and POST / only — no PATCH or DELETE (append-only per D-17)"
      exports: ["default router"]
    - path: "server/src/services/careerGoal.ts"
      provides: "listCareerGoals (with author.displayName), createCareerGoal (always new row) — no update or delete"
      exports: ["listCareerGoals", "createCareerGoal"]
    - path: "server/src/services/staffNote.ts"
      provides: "listStaffNotes (with author.displayName, newest-first), createStaffNote — no update or delete"
      exports: ["listStaffNotes", "createStaffNote"]
    - path: "server/src/schemas/careerGoal.ts"
      provides: "createCareerGoalSchema with interests array min(1) and description optional; no update schema"
      exports: ["CAREER_INTERESTS", "createCareerGoalSchema"]
    - path: "server/src/schemas/staffNote.ts"
      provides: "createStaffNoteSchema with content min(1) max(500); no update schema"
      exports: ["createStaffNoteSchema"]
    - path: "server/prisma/migrations/20260612_add_records/migration.sql"
      provides: "SQL for all 6 new tables + 2 new enums; used for documentation and manual apply"
    - path: "client/src/components/records/CareerGoalsSection.tsx"
      provides: "Versioned career goals display (current + collapsible history) + Update dialog with CareerInterestsChecklist"
      exports: ["CareerGoalsSection"]
    - path: "client/src/components/records/NotesSection.tsx"
      provides: "Append-only inline textarea + Add note button + character counter + notes list"
      exports: ["NotesSection"]
    - path: "client/src/components/records/CareerInterestsChecklist.tsx"
      provides: "12-item 2-col checkbox grid for career interests multi-select"
      exports: ["CareerInterestsChecklist", "CAREER_INTEREST_LABELS"]
  key_links:
    - from: "server/src/routes/students.ts"
      to: "server/src/routes/careerGoals.ts"
      via: "router.use('/:studentId/career-goals', careerGoalsRouter)"
      pattern: "router\\.use.*career-goals"
    - from: "server/src/routes/careerGoals.ts"
      to: "server/src/services/careerGoal.ts"
      via: "createCareerGoal — always creates new row, no update path"
      pattern: "createCareerGoal"
    - from: "client/src/components/records/CareerGoalsSection.tsx"
      to: "/api/students/:id/career-goals"
      via: "useQuery({ queryKey: ['student', studentId, 'career-goals'] })"
      pattern: "useQuery.*career-goals"
    - from: "client/src/components/records/NotesSection.tsx"
      to: "/api/students/:id/notes"
      via: "useMutation POST; useQuery queryKey ['student', studentId, 'notes']"
      pattern: "useQuery.*notes"
---

<objective>
Career Goals & Staff Notes vertical slice + schema push + full GREEN: add CareerGoal and StaffNote Prisma models (POST-only versioned and append-only patterns respectively), write RED tests for stu-07 and stu-08, push all 6 models to the database (BLOCKING), install remaining shadcn components (checkbox/textarea/tooltip/scroll-area), build CareerGoalsSection and NotesSection with CareerInterestsChecklist, complete the StudentDetailPage with all 6 sections, and confirm all stu-03 through stu-08 integration tests pass GREEN.

Purpose: Closes the phase — after this plan the database has all 6 tables, all 6 API endpoints are live, all section components are rendered, and the full test suite is green. Staff can immediately use the student detail page to manage all six record types.

Output: All schema models deployed to PostgreSQL, full API coverage for STU-03 through STU-08, CareerGoalsSection + NotesSection + CareerInterestsChecklist, all 6 sections on StudentDetailPage, 100% test GREEN.
</objective>

<execution_context>
@C:/@code/spcs-transcripts/.cursor/gsd-core/workflows/execute-plan.md
@C:/@code/spcs-transcripts/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-student-records-ui/03-CONTEXT.md
@.planning/phases/03-student-records-ui/03-RESEARCH.md
@.planning/phases/03-student-records-ui/03-UI-SPEC.md
@.planning/phases/03-student-records-ui/03-PATTERNS.md
@.planning/phases/03-student-records-ui/03-01-SUMMARY.md
@.planning/phases/03-student-records-ui/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CareerGoal + StaffNote — schema models, Prisma generate, Zod schemas, services, routes, students.ts mounts, RED tests for stu-07 and stu-08, clearDb extension</name>
  <files>
    server/prisma/schema.prisma
    server/src/schemas/careerGoal.ts
    server/src/schemas/staffNote.ts
    server/src/services/careerGoal.ts
    server/src/services/staffNote.ts
    server/src/routes/careerGoals.ts
    server/src/routes/notes.ts
    server/src/routes/students.ts
    server/src/__tests__/records.test.ts
    server/src/__tests__/helpers/testDb.ts
  </files>
  <read_first>
    - server/prisma/schema.prisma — current state after Plan 02 (has AcademicResult, Activity, Award, WorkExperience, User model); append CareerGoal + StaffNote; check User model for existing back-relations (auditLogs) to add careerGoalsAuthored + staffNotesAuthored named relations
    - server/src/services/activity.ts or server/src/services/academicResult.ts — import/export pattern to replicate for careerGoal.ts and staffNote.ts (same PrismaClient type, same logAudit import)
    - server/src/routes/academicResults.ts — full file; careerGoals.ts and notes.ts use identical structure minus PATCH/DELETE verbs
    - server/src/routes/students.ts — current bottom of file; add career-goals + notes mounts
    - server/src/__tests__/records.test.ts — current state; append stu-07 + stu-08 describe blocks
    - server/src/__tests__/helpers/testDb.ts — current clearDb(); complete the full FK-safe order by adding staffNote + careerGoal at top
    - .planning/phases/03-student-records-ui/03-RESEARCH.md — Pattern 1 (CareerGoal + StaffNote schema), Pattern 4 (versioned create service), Pattern 5 (append-only service), Pattern 7 (careerGoal + staffNote Zod schemas)
    - .planning/phases/03-student-records-ui/03-PATTERNS.md — careerGoal.ts and staffNote.ts service patterns; careerGoals.ts route pattern (GET+POST only)
  </read_first>
  <behavior>
    - stu-07-create: POST /api/students/:id/career-goals with interests:['ENGINEERING','IT_TECHNOLOGY'], description:'Targeting HKU' returns 201 with id, interests, description, author.displayName
    - stu-07-list: GET /api/students/:id/career-goals returns array sorted newest-first (index 0 is most recent)
    - stu-07-versioning: POST twice → GET returns 2 items, newest first; does NOT overwrite first entry
    - stu-07-no-patch: PATCH /api/students/:id/career-goals/:goalId returns 404 (route not registered)
    - stu-07-no-delete: DELETE /api/students/:id/career-goals/:goalId returns 404
    - stu-07-empty-interests: POST with interests:[] returns 400 (min(1) validation)
    - stu-08-create: POST /api/students/:id/notes with content:'Test note' returns 201 with id, content, author.displayName, createdAt
    - stu-08-list: GET /api/students/:id/notes returns notes newest-first
    - stu-08-append-only-no-patch: PATCH /api/students/:id/notes/:noteId returns 404
    - stu-08-append-only-no-delete: DELETE /api/students/:id/notes/:noteId returns 404
    - stu-08-max-length: POST with content of 501 characters returns 400
    - stu-08-audit: POST /notes creates AuditLog row model:'StaffNote' action:'CREATE'
  </behavior>
  <action>
    SCHEMA (server/prisma/schema.prisma): Append CareerGoal model: id String @id @default(uuid()), studentId String, student Student @relation(fields:[studentId], references:[id], onDelete:Cascade), interests CareerInterest[], description String? @db.VarChar(500), authorId String, author User @relation("CareerGoalAuthor", fields:[authorId], references:[id]), createdAt DateTime @default(now()), // NO updatedAt — immutable once created (D-16), @@index([studentId, createdAt(sort:Desc)]). Note: NO updatedAt field on CareerGoal — this signals immutability to maintainers.

    Append StaffNote model: id String @id @default(uuid()), studentId String, student Student @relation(fields:[studentId], references:[id], onDelete:Cascade), content String @db.VarChar(500), authorId String, author User @relation("StaffNoteAuthor", fields:[authorId], references:[id]), createdAt DateTime @default(now()), // NO updatedAt — append-only (D-17), @@index([studentId, createdAt(sort:Desc)]).

    Add back-relations to Student model: careerGoals CareerGoal[] and staffNotes StaffNote[].

    Add back-relations to User model: careerGoalsAuthored CareerGoal[] @relation("CareerGoalAuthor") and staffNotesAuthored StaffNote[] @relation("StaffNoteAuthor"). Read the existing User model first to see if auditLogs relation is there; add the two new named relations alongside it.

    RUN: cd server && npx prisma generate

    testDb.ts: complete the FK-safe clearDb order. Final order (top to bottom): prisma.staffNote.deleteMany(), prisma.careerGoal.deleteMany(), prisma.workExperience.deleteMany(), prisma.award.deleteMany(), prisma.activity.deleteMany(), prisma.academicResult.deleteMany(), prisma.auditLog.deleteMany(), prisma.student.deleteMany(), prisma.user.deleteMany(). Replace any remaining @ts-expect-error stubs.

    SCHEMAS: Create server/src/schemas/careerGoal.ts — export const CAREER_INTERESTS = ['MEDICINE_HEALTH','LAW','ENGINEERING','BUSINESS_FINANCE','EDUCATION','ARTS_DESIGN','SCIENCE_RESEARCH','IT_TECHNOLOGY','HOSPITALITY','SOCIAL_SERVICES','SPORTS','UNDECIDED'] as const. createCareerGoalSchema: z.object({ interests: z.array(z.enum(CAREER_INTERESTS)).min(1, 'Select at least one interest area'), description: z.string().trim().max(500).optional() }).strict(). NO update schema — career goals are POST-only.

    Create server/src/schemas/staffNote.ts — createStaffNoteSchema: z.object({ content: z.string().trim().min(1).max(500) }).strict(). NO update schema — staff notes are append-only.

    SERVICES: Create server/src/services/careerGoal.ts — import PrismaClient from '../generated/prisma/client.js'; import logAudit from './audit.js'; import type CareerInterest from client. listCareerGoals(prisma, studentId): findMany where studentId, include:{ author:{ select:{ displayName:true } } }, orderBy:{ createdAt:'desc' }. createCareerGoal(prisma, studentId, data:{ interests:CareerInterest[], description?:string }, userId): prisma.careerGoal.create({ data:{ studentId, authorId:userId, interests:data.interests, description:data.description??null }, include:{ author:{ select:{ displayName:true } } } }), then logAudit model:'CareerGoal' action:'CREATE'. NO updateCareerGoal or deleteCareerGoal functions — their absence is the enforcement.

    Create server/src/services/staffNote.ts — listStaffNotes(prisma, studentId): findMany where studentId, include:{ author:{ select:{ displayName:true } } }, orderBy:{ createdAt:'desc' }. createStaffNote(prisma, studentId, content:string, userId): create note, logAudit model:'StaffNote' action:'CREATE'. NO update or delete functions.

    ROUTES: Create server/src/routes/careerGoals.ts — Router({ mergeParams:true }). Copy parseStudentId helper. Register ONLY: router.get('/', ...) and router.post('/', ...). Do NOT register router.patch() or router.delete(). GET: list career goals, return array. POST: safeParse createCareerGoalSchema, call createCareerGoal(prisma, studentId, parsed.data, req.user!.id), res.status(201).json(goal).

    Create server/src/routes/notes.ts — identical structure. Router({ mergeParams:true }). ONLY GET and POST. POST: safeParse createStaffNoteSchema, call createStaffNote(prisma, studentId, parsed.data.content, req.user!.id), res.status(201).json(note).

    MOUNT: In students.ts append: import careerGoalsRouter from './careerGoals.js'; import notesRouter from './notes.js'; router.use('/:studentId/career-goals', careerGoalsRouter); router.use('/:studentId/notes', notesRouter).

    TESTS: Append stu-07 and stu-08 describe blocks to records.test.ts. For stu-07-versioning test: POST career-goals twice with different interests arrays, then GET and assert response[0].interests matches the second POST, and response.length === 2. For stu-07-no-patch: request.patch('/api/students/${student.id}/career-goals/fake-id').set(auth).expect(404). For stu-08-max-length: construct a string of 501 'a' characters, POST → expect 400. Tests FAIL at this stage (DB tables don't exist yet).
  </action>
  <acceptance_criteria>
    - server/prisma/schema.prisma: model CareerGoal has NO updatedAt field; model StaffNote has NO updatedAt field
    - server/prisma/schema.prisma: CareerGoal has interests CareerInterest[] and authorId+author User relation named "CareerGoalAuthor"; User model has careerGoalsAuthored relation
    - server/src/services/careerGoal.ts: does NOT export updateCareerGoal or deleteCareerGoal
    - server/src/services/staffNote.ts: does NOT export updateStaffNote or deleteStaffNote
    - server/src/routes/careerGoals.ts: does NOT contain router.patch or router.delete
    - server/src/routes/notes.ts: does NOT contain router.patch or router.delete
    - server/src/schemas/careerGoal.ts: interests field has .min(1, ...) validation
    - server/src/routes/students.ts: contains `router.use('/:studentId/career-goals'` and `router.use('/:studentId/notes'`
    - `cd server && npx tsc --noEmit` exits 0
    - stu-07 and stu-08 describe blocks visible in `cd server && npm test -- --reporter=verbose 2>&1 | grep -E "stu-07|stu-08"`; tests show FAIL (not error)
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | tail -5 && npm test -- --reporter=verbose 2>&1 | grep -cE "(FAIL|stu-0[3-8])" | head -5</automated>
  </verify>
  <done>CareerGoal + StaffNote models in schema; services export only list+create; routes have only GET+POST; students.ts mounts both; clearDb has all 6 models; stu-07 + stu-08 RED tests added; server TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate migration SQL + schema push (npx prisma db push) — deploys all 6 new models to PostgreSQL</name>
  <files>
    server/prisma/migrations/20260612_add_records/migration.sql
  </files>
  <read_first>
    - server/prisma/schema.prisma — final state with all 6 new models; verify all models present before generating SQL
    - server/prisma/migrations/ — list existing migrations to confirm naming convention (existing: 20260612094000_add_student)
    - .planning/STATE.md (Key Decisions) — "Migration SQL generated via prisma migrate diff — No PostgreSQL/Docker available in dev; used --from-empty --to-schema to generate SQL without live DB"
    - .planning/phases/03-student-records-ui/03-RESEARCH.md Pattern 10 — migration strategy for no-live-DB environment
  </read_first>
  <action>
    STEP 1 — Generate migration SQL (documentation only; not required for db push):
    Run from server/: `npx prisma migrate diff --from-migrations prisma/migrations --to-schema-datamodel prisma/schema.prisma --script` and save output to `prisma/migrations/20260612_add_records/migration.sql`. Create the directory first. The generated SQL creates: AwardLevel enum, CareerInterest enum, academic_results table, activities table, awards table, work_experience table, career_goals table with interests as _CareerInterest[] array column, staff_notes table. If prisma migrate diff requires a live database for --from-migrations and fails, fall back to: `npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script > prisma/migrations/20260612_add_records/migration.sql` (generates full schema SQL from scratch). Inspect the generated SQL to confirm CareerInterest[] array type generates correctly (should produce `_career_interest[]` or `"CareerInterest"[]` column per Prisma 7 PostgreSQL array syntax). If array type output is incorrect (generates plain text or unsupported type), note it in the SUMMARY.md for follow-up — the db push is the authoritative apply step regardless.

    STEP 2 — Push schema to database ([BLOCKING]):
    Run: `cd server && npx prisma db push`
    This command syncs schema.prisma to the PostgreSQL database (the DATABASE_URL from .env or ecosystem.config.js). Expected output: "Your database is now in sync with your Prisma schema." If the command prompts about data loss (unexpected since we are only ADDING tables), run with --accept-data-loss flag. If the command fails due to missing DATABASE_URL: check server/.env for DATABASE_URL; if .env is not present, check ecosystem.config.js env block for DATABASE_URL.

    STEP 3 — Verify tables created:
    Run: `cd server && node -e "const {PrismaClient}=require('./src/generated/prisma/client.js'); const p=new PrismaClient(); p.academicResult.count().then(c=>console.log('academicResult count:',c)).catch(e=>console.error(e)).finally(()=>p.\$disconnect())"` — if this returns "academicResult count: 0" the table exists. Also verify careerGoal and staffNote tables with same pattern.

    STEP 4 — Regenerate client after push (push regenerates automatically, but confirm):
    `cd server && npx prisma generate` if the client types seem stale.
  </action>
  <acceptance_criteria>
    - `npx prisma db push` exits 0 with "Your database is now in sync" message (or equivalent success output)
    - `cd server && npm test` — at minimum stu-03 and stu-04 tests now PASS (DB tables exist for AcademicResult + Activity)
    - migration.sql file exists at server/prisma/migrations/20260612_add_records/migration.sql with CREATE TABLE statements for all 6 models
    - `cd server && node -e "require('./src/generated/prisma/client.js').PrismaClient" 2>&1` exits without 'Module not found' error
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx prisma db push 2>&1 | tail -3</automated>
  </verify>
  <done>All 6 Prisma models deployed to PostgreSQL; migration SQL documented; stu-03 and stu-04 tests begin passing</done>
</task>

<task type="auto">
  <name>Task 3: Install shadcn components, CareerGoalsSection + NotesSection + CareerInterestsChecklist, complete StudentDetailPage, all tests GREEN</name>
  <files>
    client/src/components/records/CareerGoalsSection.tsx
    client/src/components/records/NotesSection.tsx
    client/src/components/records/CareerInterestsChecklist.tsx
    client/src/pages/StudentDetailPage.tsx
  </files>
  <read_first>
    - client/src/components/records/AcademicResultsSection.tsx — full file; copy useQuery/useMutation/queryClient.invalidateQueries pattern for CareerGoalsSection and NotesSection
    - client/src/components/records/RecordSectionCard.tsx — full file; CareerGoalsSection uses this wrapper; NotesSection uses it with hideAddButton=true
    - client/src/pages/StudentDetailPage.tsx — current state (4 sections already rendered); add CareerGoalsSection + NotesSection
    - .planning/phases/03-student-records-ui/03-UI-SPEC.md — Section 5 (Career Goals section layout + version history toggle + Update dialog), Section 6 (Notes section layout + textarea + attribution + character count), Appendix B (CareerInterestsChecklist 12 items), Copywriting Contract (career goals + notes toast copy, validation messages, version history labels)
    - .planning/phases/03-student-records-ui/03-RESEARCH.md — Pattern 4 (career goals API response shape: versions[0] is current, versions.slice(1) is history), Pattern 5 (staff notes response with author.displayName), Pattern 6 (TanStack Query section fetching pattern)
    - .planning/phases/03-student-records-ui/03-PATTERNS.md — CareerGoalsSection version history toggle pattern, CareerInterestsChecklist pattern with Checkbox imports
    - server/src/__tests__/records.test.ts — read the failing tests to understand what behavior is expected for GREEN; ensure implementation matches
  </read_first>
  <action>
    INSTALL shadcn components:
    cd client && npx shadcn@latest add checkbox textarea tooltip scroll-area
    These are official shadcn registry components (pre-audited in 03-RESEARCH.md Package Legitimacy table). If the CLI prompts to overwrite existing files, answer Y. This installs: client/src/components/ui/checkbox.tsx, client/src/components/ui/textarea.tsx, client/src/components/ui/tooltip.tsx, client/src/components/ui/scroll-area.tsx.

    client/src/components/records/CareerInterestsChecklist.tsx (NEW): Props: value:string[], onChange:(v:string[])=>void. Export CAREER_INTEREST_LABELS: Record&lt;string,string&gt; mapping enum values to display labels per 03-UI-SPEC Appendix B: { MEDICINE_HEALTH:'Medicine / Health', LAW:'Law', ENGINEERING:'Engineering', BUSINESS_FINANCE:'Business / Finance', EDUCATION:'Education', ARTS_DESIGN:'Arts / Design', SCIENCE_RESEARCH:'Science / Research', IT_TECHNOLOGY:'IT / Technology', HOSPITALITY:'Hospitality', SOCIAL_SERVICES:'Social Services', SPORTS:'Sports', UNDECIDED:'Undecided / Exploring' }. Render &lt;fieldset&gt;&lt;legend className="sr-only"&gt;Career interests&lt;/legend&gt;&lt;div className="grid grid-cols-1 md:grid-cols-2 gap-3"&gt; mapping the 12 interest keys in the order from CAREER_INTERESTS array. Each item: &lt;div className="flex items-center gap-2 min-h-[44px]"&gt;&lt;Checkbox id={key} checked={value.includes(key)} onCheckedChange={(checked)=>onChange(checked ? [...value,key] : value.filter(v=>v!==key))} /&gt;&lt;Label htmlFor={key}&gt;{CAREER_INTEREST_LABELS[key]}&lt;/Label&gt;&lt;/div&gt;. Import Checkbox from @/components/ui/checkbox (just installed); Label from @/components/ui/label.

    client/src/components/records/CareerGoalsSection.tsx (NEW): Props: studentId:string. Type CareerGoalVersion: { id:string, interests:string[], description:string|null, author:{ displayName:string }, createdAt:string }. useQuery queryKey=['student',studentId,'career-goals'], queryFn=apiGet&lt;CareerGoalVersion[]&gt;(`/students/${studentId}/career-goals`). useMutation for create (POST to /students/${studentId}/career-goals, on success: invalidateQueries + toast.success('Career goals saved'), on error: toast.error("Couldn't save changes. Please try again.")). const versions = data ?? []; const current = versions[0]; const history = versions.slice(1).

    RecordSectionCard: title="Career goals", count={versions.length}, addLabel="Update career goals", onAdd=openUpdateDialog, isLoading, isError, onRetry=refetch, isEmpty=versions.length===0, emptyHeading="No career goals recorded yet.", emptyBody="Add the first entry to begin building this student's career profile.". Note: D-05 says section header shows count — show total version count.

    Current version display (when versions.length > 0): inside CardContent, show current version block with: sub-heading "Interests" (text-sm font-medium), interest chips as &lt;Badge variant="secondary" className="mr-1 mb-1"&gt;{CAREER_INTEREST_LABELS[interest]}&lt;/Badge&gt; in a flex flex-wrap gap-2 div. Sub-heading "Goals & description" if current.description. Description text as text-sm font-normal leading-relaxed. Attribution: "Updated {formatted date} by {current.author.displayName}" as text-sm text-muted-foreground.

    Version history: if history.length > 0, show a Button variant="ghost" size="sm" with ChevronRight icon (rotates 90° when expanded) labeled "Version history ({history.length} previous)" / "Hide version history". [historyExpanded] state via useState(false). When expanded, map history: each entry is a div with bg-muted/30 rounded p-3 mb-2, shows "{version.author.displayName} · {format date as '11 Jun 2026'}" as text-sm text-muted-foreground, then a ChevronRight expand toggle for full content (use secondary expand state per version). For accessible version history entries use aria-expanded on each expand toggle.

    Update dialog (Dialog component): title "Update career goals". Form: useForm({ resolver: zodResolver(careerGoalFormSchema) }) with interests field (CareerInterestsChecklist as controlled component) and description (Textarea from @/components/ui/textarea, placeholder="Describe this student's goals, university targets, or career aspirations", maxLength=500, min-h-[120px], character count {n}/500 as text-sm text-muted-foreground text-right). Pre-populate from current version when dialog opens (setValues with current?.interests and current?.description). On submit: POST new version (does NOT patch). Footer: "Discard" + "Save career goals" / "Saving…".

    client/src/components/records/NotesSection.tsx (NEW): Props: studentId:string. Type StaffNote: { id:string, content:string, author:{ displayName:string }, createdAt:string }. useQuery queryKey=['student',studentId,'notes'], queryFn=apiGet&lt;StaffNote[]&gt;(`/students/${studentId}/notes`). useMutation for create (POST, on success: invalidateQueries + toast.success('Note added') + clear textarea, on error: toast.error("Couldn't save changes. Please try again.")). State: noteContent:string='', isPending from mutation.

    RecordSectionCard with hideAddButton=true: title="Notes", count={notes.length}. Card body structure: 1) Textarea input area at top: &lt;div className="mb-4"&gt;&lt;Label htmlFor="new-note"&gt;Add a note&lt;/Label&gt;&lt;Textarea id="new-note" placeholder="Add a note about this student…" value={noteContent} onChange={(e)=>setNoteContent(e.target.value)} maxLength={500} className="min-h-[80px] mt-1.5" aria-describedby="note-char-count" /&gt;&lt;div className="flex items-center justify-end mt-1 gap-2"&gt;&lt;span id="note-char-count" className="text-sm text-muted-foreground"&gt;{noteContent.length}/500&lt;/span&gt;&lt;Button variant="ghost" onClick={()=>setNoteContent('')} disabled={isPending||!noteContent}&gt;Clear&lt;/Button&gt;&lt;Button variant="default" onClick={handleSubmit} disabled={isPending||!noteContent.trim()||noteContent.length>500}&gt;{isPending?'Adding…':'Add note'}&lt;/Button&gt;&lt;/div&gt;&lt;/div&gt;. 2) Notes list: if notes.length===0 show "No notes yet. Add the first note above." as text-sm text-muted-foreground text-center py-4. Else render &lt;ul&gt; with each note as &lt;li key={note.id}&gt;&lt;div className="flex items-center gap-1 text-sm text-muted-foreground"&gt;&lt;span&gt;{note.author.displayName}&lt;/span&gt;&lt;span&gt;·&lt;/span&gt;&lt;time dateTime={note.createdAt}&gt;{formatNoteDate(note.createdAt)}&lt;/time&gt;&lt;/div&gt;&lt;p className="text-sm font-normal leading-relaxed mt-1"&gt;{note.content}&lt;/p&gt;&lt;Separator className="mt-3 mb-3" /&gt;&lt;/li&gt;. formatNoteDate helper: new Date(createdAt).toLocaleString('en-HK',{ day:'numeric', month:'short', year:'numeric', hour:'2-digit', minute:'2-digit' }) or similar producing "12 Jun 2026, 2:14 PM" format. No edit or delete controls on note items (D-17).

    client/src/pages/StudentDetailPage.tsx: In the flex flex-col gap-8 div, add after WorkExperienceSection:
    &lt;CareerGoalsSection studentId={student.id} /&gt;
    &lt;NotesSection studentId={student.id} /&gt;
    Add imports for CareerGoalsSection and NotesSection.

    RUN TESTS — make all RED tests GREEN:
    cd server && npm test
    All stu-03 through stu-08 tests should now pass (DB tables exist after Task 2). If any test fails unexpectedly, debug and fix. Common issues: testDb clearDb order (FK violations), missing include clause in service (author not returned), wrong orderBy (notes not newest-first). Fix the implementation until `npm test` passes fully green.
  </action>
  <acceptance_criteria>
    - client/src/components/records/CareerInterestsChecklist.tsx: renders 12 Checkbox+Label pairs; CAREER_INTEREST_LABELS maps all 12 enum values; each item has min-h-[44px] on wrapper div
    - client/src/components/records/CareerGoalsSection.tsx: POST mutation has no PATCH — creates new version; historyExpanded toggle button present; Textarea from @/components/ui/textarea used for description field
    - client/src/components/records/NotesSection.tsx: noteContent character counter shows {n}/500; Add note button disabled when noteContent.trim() is empty; no edit/delete icons on note list items
    - client/src/pages/StudentDetailPage.tsx: contains all 6 section component imports and JSX in D-02 order (AcademicResultsSection → ActivitiesSection → AwardsSection → WorkExperienceSection → CareerGoalsSection → NotesSection)
    - `cd client && npx tsc --noEmit` exits 0
    - `cd server && npm test` exits 0 with all stu-03 through stu-08 tests PASSING
    - `cd server && npm test 2>&1 | grep -E "Tests.*passed|passed"` shows total passing count including all record tests
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test 2>&1 | tail -15</automated>
  </verify>
  <done>shadcn checkbox/textarea/tooltip/scroll-area installed; CareerGoalsSection (versioned history + update dialog) + NotesSection (inline append-only) + CareerInterestsChecklist created; all 6 sections on StudentDetailPage in D-02 order; all stu-03 through stu-08 integration tests GREEN; client TypeScript clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/students/:id/career-goals | Untrusted POST only; no mutation routes; JWT auth inherited |
| client → /api/students/:id/notes | Untrusted POST only; no mutation routes; append-only enforced by route omission |
| service layer → Prisma (CareerGoal create) | Server-internal: always creates new row; no update path exists in service or route |
| npx prisma db push | Trusted admin operation; run with DATABASE_URL from secure environment config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | careerGoals route — attempt to PATCH/DELETE career goal version | mitigate | No PATCH or DELETE routes registered on careerGoalsRouter; any attempt returns 404 (verified by stu-07-no-patch and stu-07-no-delete tests) |
| T-03-12 | Tampering | notes route — attempt to PATCH/DELETE a note | mitigate | No PATCH or DELETE routes registered on notesRouter; append-only enforced by omission (verified by stu-08-append-only tests) |
| T-03-13 | Tampering | career goals POST — mass assignment on interests array | mitigate | Zod createCareerGoalSchema with .strict(); interests validated as z.array(z.enum(CAREER_INTERESTS)); unknown fields rejected |
| T-03-14 | Repudiation | careerGoal + staffNote create | mitigate | logAudit() called in service after every create; model:'CareerGoal' and model:'StaffNote'; authorId recorded on row (not just in audit log) |
| T-03-15 | Info Disclosure | career goals response includes author.displayName | accept | DisplayName is not sensitive PII; required for attribution display (D-16, D-19); no email or user ID leaked |
| T-03-16 | Tampering | npx prisma db push — schema migration | mitigate | Run only in controlled environment with verified DATABASE_URL; output inspected before confirming |
| T-03-SC | Tampering | npm/shadcn installs — checkbox, textarea, tooltip, scroll-area | mitigate | All four are from shadcn official registry (github.com/shadcn-ui/ui); pre-audited [OK] in 03-RESEARCH.md Package Legitimacy table |
</threat_model>

<verification>
- `cd server && npm test` exits 0 — all stu-03 through stu-08 tests PASS
- `cd server && npx tsc --noEmit` exits 0
- `cd client && npx tsc --noEmit` exits 0
- server/src/routes/careerGoals.ts: grep for 'router.patch\|router.delete' returns no results
- server/src/routes/notes.ts: grep for 'router.patch\|router.delete' returns no results
- server/src/services/careerGoal.ts: grep for 'updateCareerGoal\|deleteCareerGoal' returns no results
- server/prisma/migrations/20260612_add_records/migration.sql exists with CREATE TABLE statements
- client/src/pages/StudentDetailPage.tsx: section order verified by reading the flex-col div content
- `cd server && npm test 2>&1 | grep -cE "PASS"` — all passing count confirmed

<human-check>
Navigate to /students/:id for any student. Verify:
1. All six section cards are visible in order: Academic results → Activities → Awards → Work experience → Career goals → Notes
2. Each empty section shows the correct empty-state copy and an Add button (except Notes, which shows the inline textarea)
3. Click "Add result" in Academic results, fill the form, submit — result appears in the table immediately
4. Click "Update career goals", select at least one interest, add description, save — current version block shows interests as Badge chips and description text
5. Type a note in the Notes textarea, click "Add note" — note appears in the list with your name and timestamp
6. Submit approval once all 6 sections work correctly
</human-check>
</verification>

<success_criteria>
- CareerGoal model in schema: interests CareerInterest[] array field, authorId FK, no updatedAt, POST-only enforcement
- StaffNote model in schema: content VarChar(500), authorId FK, no updatedAt, append-only enforcement
- careerGoals.ts service: exports only listCareerGoals + createCareerGoal; no update or delete
- staffNote.ts service: exports only listStaffNotes + createStaffNote; no update or delete
- careerGoals.ts route: GET + POST only; no PATCH or DELETE registered
- notes.ts route: GET + POST only; no PATCH or DELETE registered
- npx prisma db push succeeds; all 6 new tables verified in database
- migration.sql documents the full schema additions
- CareerGoalsSection: shows current version with interests chips + description; collapsible version history; pre-populates dialog from current version (D-16); POST creates new version (not update)
- NotesSection: inline textarea with {n}/500 counter; Add note disabled when empty; notes listed newest-first with displayName + datetime; no edit/delete controls (D-17)
- CareerInterestsChecklist: 12 items in 2-col grid; min-h-[44px] per item; controlled toggle array (D-14)
- StudentDetailPage: all 6 sections in D-02 order; section count badges all render; section gap gap-8
- All stu-03 through stu-08 integration tests GREEN
</success_criteria>

<output>
Create `.planning/phases/03-student-records-ui/03-03-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

**Server — new files:**
- `server/src/routes/careerGoals.ts` — exports default Express Router; GET / + POST / ONLY; no PATCH/DELETE
- `server/src/routes/notes.ts` — exports default Express Router; GET / + POST / ONLY; no PATCH/DELETE
- `server/src/services/careerGoal.ts` — exports listCareerGoals, createCareerGoal (no update/delete)
- `server/src/services/staffNote.ts` — exports listStaffNotes, createStaffNote (no update/delete)
- `server/src/schemas/careerGoal.ts` — exports CAREER_INTERESTS const, createCareerGoalSchema (no update schema)
- `server/src/schemas/staffNote.ts` — exports createStaffNoteSchema (no update schema)
- `server/prisma/migrations/20260612_add_records/migration.sql` — SQL for all 6 new tables + 2 new enums

**Server — modified files:**
- `server/prisma/schema.prisma` — adds CareerGoal model (interests CareerInterest[], authorId, no updatedAt), StaffNote model (content VarChar(500), authorId, no updatedAt); back-relations careerGoals + staffNotes on Student; careerGoalsAuthored + staffNotesAuthored named relations on User
- `server/src/routes/students.ts` — adds careerGoalsRouter + notesRouter mounts
- `server/src/__tests__/records.test.ts` — extended with stu-07 + stu-08 RED→GREEN test groups
- `server/src/__tests__/helpers/testDb.ts` — clearDb() completed with all 6 models in FK-safe order

**Client — new files:**
- `client/src/components/records/CareerInterestsChecklist.tsx` — exports CareerInterestsChecklist, CAREER_INTEREST_LABELS
- `client/src/components/records/CareerGoalsSection.tsx` — exports CareerGoalsSection
- `client/src/components/records/NotesSection.tsx` — exports NotesSection

**Client — modified files:**
- `client/src/pages/StudentDetailPage.tsx` — all 6 sections now rendered in D-02 order
- `client/package.json` — adds shadcn checkbox, textarea, tooltip, scroll-area (official registry, pre-audited)

**Schema complete — all 6 Phase 3 models deployed:**
AcademicResult · Activity · Award · WorkExperience · CareerGoal · StaffNote
