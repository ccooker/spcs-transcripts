---
phase: 03-student-records-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/prisma/schema.prisma
  - server/src/routes/students.ts
  - server/src/routes/academicResults.ts
  - server/src/routes/activities.ts
  - server/src/services/academicResult.ts
  - server/src/services/activity.ts
  - server/src/schemas/academicResult.ts
  - server/src/schemas/activity.ts
  - server/src/__tests__/records.test.ts
  - server/src/__tests__/helpers/testDb.ts
  - client/src/App.tsx
  - client/src/lib/periodFormat.ts
  - client/src/components/records/RecordSectionCard.tsx
  - client/src/components/records/MonthYearPicker.tsx
  - client/src/components/records/RecordDeleteDialog.tsx
  - client/src/components/records/AcademicResultsSection.tsx
  - client/src/components/records/ActivitiesSection.tsx
  - client/src/pages/StudentDetailPage.tsx
autonomous: true
requirements:
  - STU-03
  - STU-04

must_haves:
  truths:
    - "RED integration tests for stu-03 (academics) and stu-04 (activities) exist in records.test.ts and fail as expected (no DB tables yet)"
    - "GET/POST/PATCH/:resultId/DELETE/:resultId routes are registered under /api/students/:studentId/academics"
    - "GET/POST/PATCH/:activityId/DELETE/:activityId routes are registered under /api/students/:studentId/activities"
    - "IDOR guard: PATCH or DELETE with a resultId belonging to a different studentId returns 404 — never 403"
    - "Activity service list function uses nulls:'first' on endYear/endMonth so ongoing entries sort before dated entries (D-11)"
    - "AcademicResultsSection renders below the Profile card on /students/:id; shows 'No academic results yet.' empty state with 'Add result' button (D-04)"
    - "ActivitiesSection renders below AcademicResultsSection; shows 'No activities yet.' empty state (D-04)"
    - "Section card headers show title + entry count e.g. 'Academic results (0)' per D-05"
    - "QueryClientProvider wraps the router in App.tsx so all TanStack Query hooks in this and future plans resolve without 'No QueryClient set' error"
  artifacts:
    - path: "server/src/routes/academicResults.ts"
      provides: "CRUD router with mergeParams:true — GET / POST / PATCH /:resultId / DELETE /:resultId"
      exports: ["default router"]
    - path: "server/src/routes/activities.ts"
      provides: "CRUD router with mergeParams:true — GET / POST / PATCH /:activityId / DELETE /:activityId"
      exports: ["default router"]
    - path: "server/src/services/academicResult.ts"
      provides: "CRUD functions + IDOR guard + logAudit"
      exports: ["AcademicResultNotFoundError", "listAcademicResults", "createAcademicResult", "updateAcademicResult", "deleteAcademicResult"]
    - path: "server/src/services/activity.ts"
      provides: "CRUD functions + IDOR guard + logAudit + null-first end-date sort"
      exports: ["ActivityNotFoundError", "listActivities", "createActivity", "updateActivity", "deleteActivity"]
    - path: "server/src/schemas/academicResult.ts"
      provides: "Zod create + update schemas; PRESET_SUBJECTS constant; subject/subjectOther cross-field refinement"
      exports: ["PRESET_SUBJECTS", "createAcademicResultSchema", "updateAcademicResultSchema"]
    - path: "server/src/schemas/activity.ts"
      provides: "Zod create + update schemas with monthSchema/yearSchema helpers"
      exports: ["createActivitySchema", "updateActivitySchema"]
    - path: "client/src/components/records/RecordSectionCard.tsx"
      provides: "Shared section card wrapper: title+count header, Add button, loading skeleton, error Alert, empty state"
      exports: ["RecordSectionCard"]
    - path: "client/src/components/records/MonthYearPicker.tsx"
      provides: "Month + year pair Selects inside fieldset+legend; w-24 each; null for empty (D-12)"
      exports: ["MonthYearPicker"]
    - path: "client/src/components/records/RecordDeleteDialog.tsx"
      provides: "AlertDialog: 'Delete this entry?' / 'Keep entry' / 'Delete' (D-21)"
      exports: ["RecordDeleteDialog"]
    - path: "client/src/lib/periodFormat.ts"
      provides: "MONTH_NAMES array; formatMonthYear(); formatPeriod() — 'Sep 2022 – Present'"
      exports: ["MONTH_NAMES", "formatMonthYear", "formatPeriod"]
    - path: "client/src/components/records/AcademicResultsSection.tsx"
      provides: "Table+Dialog CRUD UI for academic results; subject preset dropdown + 'Other' conditional Input; uses useQuery/useMutation"
      exports: ["AcademicResultsSection"]
    - path: "client/src/components/records/ActivitiesSection.tsx"
      provides: "Table+Dialog CRUD UI for activities; MonthYearPicker + Ongoing checkbox; uses useQuery/useMutation"
      exports: ["ActivitiesSection"]
    - path: "server/src/__tests__/records.test.ts"
      provides: "RED integration tests covering stu-03 and stu-04 scenarios"
      contains: "describe('stu-03')"
  key_links:
    - from: "server/src/routes/students.ts"
      to: "server/src/routes/academicResults.ts"
      via: "router.use('/:studentId/academics', academicResultsRouter)"
      pattern: "router\\.use.*academicResults"
    - from: "server/src/routes/students.ts"
      to: "server/src/routes/activities.ts"
      via: "router.use('/:studentId/activities', activitiesRouter)"
      pattern: "router\\.use.*activities"
    - from: "client/src/components/records/AcademicResultsSection.tsx"
      to: "/api/students/:id/academics"
      via: "useQuery({ queryKey: ['student', studentId, 'academics'] })"
      pattern: "useQuery.*academics"
    - from: "client/src/App.tsx"
      to: "QueryClientProvider"
      via: "wraps router tree with QueryClient instance"
      pattern: "QueryClientProvider"
---

## Phase Goal

**As a** careers staff member, **I want to** enter and manage all six types of student record data from within a single student profile page, **so that** I can build a complete student profile without navigating away.

<objective>
Academics & Activities vertical slice: write RED integration tests for both record types, add AcademicResult and Activity Prisma models (schema only — DB push deferred to Plan 03), implement all backend layers (Zod schemas, services with IDOR guard + audit, nested routes), install @tanstack/react-query + QueryClientProvider, and build the shared UI infrastructure plus AcademicResultsSection and ActivitiesSection components on the StudentDetailPage.

Purpose: Delivers the first two record type sections end-to-end (code-layer complete). Tests are intentionally RED until Plan 03 pushes the schema. UI sections render empty states + dialogs immediately; data will flow once DB push runs.

Output: RED test stubs (stu-03, stu-04), AcademicResult + Activity backend (schema models, Zod, services, routes), shared UI components (RecordSectionCard, MonthYearPicker, RecordDeleteDialog, periodFormat), AcademicResultsSection + ActivitiesSection replacing the StudentDetailPage placeholder.
</objective>

<execution_context>
@C:/@code/spcs-transcripts/.cursor/gsd-core/workflows/execute-plan.md
@C:/@code/spcs-transcripts/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-student-records-ui/03-CONTEXT.md
@.planning/phases/03-student-records-ui/03-RESEARCH.md
@.planning/phases/03-student-records-ui/03-UI-SPEC.md
@.planning/phases/03-student-records-ui/03-PATTERNS.md
@.planning/phases/02-student-profiles-search/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED integration tests for academics (stu-03) and activities (stu-04)</name>
  <files>
    server/src/__tests__/records.test.ts
    server/src/__tests__/helpers/testDb.ts
  </files>
  <read_first>
    - server/src/__tests__/students.test.ts — full file; copy createTestStudent helper, makeTestToken usage, describe block structure, and audit log assertion pattern
    - server/src/__tests__/helpers/testDb.ts — current clearDb() to extend
    - server/src/__tests__/helpers/makeTestToken.ts — token helper signature
    - server/prisma/schema.prisma — confirm AcademicResult and Activity models do NOT yet exist (tests are intentionally pre-implementation)
  </read_first>
  <behavior>
    - stu-03-list: GET /api/students/:id/academics returns 200 with array (after implementation)
    - stu-03-create: POST /api/students/:id/academics with valid body returns 201; body contains id, subject, grade, calendarYear, formLevel
    - stu-03-update: PATCH /api/students/:id/academics/:resultId with grade change returns 200
    - stu-03-delete: DELETE /api/students/:id/academics/:resultId returns 204
    - stu-03-idor: PATCH /api/students/OTHER/academics/:resultId where resultId belongs to :id returns 404
    - stu-03-audit: successful POST /academics creates AuditLog row with model:'AcademicResult' action:'CREATE'
    - stu-04-list: GET /api/students/:id/activities returns 200 with array
    - stu-04-create: POST /api/students/:id/activities with organisation/role/startMonth/startYear returns 201
    - stu-04-ongoing-sort: activities with null endYear sort before activities with dated endYear in GET response
    - stu-04-idor: PATCH with wrong studentId returns 404
  </behavior>
  <action>
    In testDb.ts: extend clearDb() to delete 6 new models BEFORE student/auditLog. Add prisma.academicResult.deleteMany(), prisma.activity.deleteMany() at the top of clearDb — after Award, WorkExperience, CareerGoal, StaffNote slots are added by later plans, this file will be extended again; for now add academicResult + activity only. NOTE: TypeScript will error until after Task 2 runs `npx prisma generate` — leave clearDb stubs as `// @ts-expect-error — model added in Task 2` inline comments so the test suite compiles without the generated types yet. Alternatively, extend clearDb AFTER Task 2 runs generate (move testDb.ts edit to Task 2 instead of Task 1 if TypeScript errors would break compilation).

    In records.test.ts: create new file. Copy the import block and createTestStudent helper from students.test.ts exactly (same token pattern, same request to POST /api/students). Write describe blocks tagged 'stu-03' and 'stu-04'. Each it() call should use the test ID as the first argument string (e.g. 'stu-03-create: POST /academics with valid body returns 201'). Valid academic payload: subject:'Chinese Language', grade:'A', calendarYear:2024, formLevel:'FORM_4'. Valid activity payload: organisation:'SPCS Drama Society', role:'President', startMonth:9, startYear:2022. Include stu-03-idor test: create student A and student B, POST academic result to A, then PATCH with /students/B/academics/:resultId — expect 404.

    Tests MUST FAIL at this stage (routes don't exist → 404). That is the expected RED state — do not try to make them pass.
  </action>
  <verify>
    <automated>cd server && npm test -- --reporter=verbose 2>&1 | grep -E "(stu-03|stu-04|FAIL|PASS)" | head -30</automated>
  </verify>
  <done>records.test.ts exists with stu-03 and stu-04 describe blocks; `npm test` shows these tests FAILING (not erroring at compile time); testDb.ts clearDb extended with academicResult + activity deletes (or @ts-expect-error stubs if generate not yet run)</done>
</task>

<task type="auto">
  <name>Task 2: AcademicResult + Activity — schema models, Prisma generate, Zod schemas, services, routes, and students.ts mounts</name>
  <files>
    server/prisma/schema.prisma
    server/src/schemas/academicResult.ts
    server/src/schemas/activity.ts
    server/src/services/academicResult.ts
    server/src/services/activity.ts
    server/src/routes/academicResults.ts
    server/src/routes/activities.ts
    server/src/routes/students.ts
    server/src/__tests__/helpers/testDb.ts
  </files>
  <read_first>
    - server/prisma/schema.prisma — full file; add after the existing Student model; note existing enums (Role, AuditAction, FormLevel) and UUID pattern
    - server/src/services/student.ts — full file; copy IDOR guard pattern (findUnique → check studentId → throw NotFoundError), logAudit call placement, PrismaClient parameter type
    - server/src/schemas/student.ts — z.object().strict() pattern; FORM_LEVELS export to import in academicResult schema
    - server/src/routes/students.ts — full file; copy parseStudentId helper; note import pattern to replicate for record routers
    - server/src/services/audit.ts — logAudit() signature: (prisma, { userId, action, model, recordId, details? })
    - .planning/phases/03-student-records-ui/03-RESEARCH.md — Pattern 1 (schema), Pattern 2 (routes), Pattern 3 (services), Pattern 7 (Zod schemas)
    - .planning/phases/03-student-records-ui/03-PATTERNS.md — academicResult.ts and activity.ts service patterns with exact code excerpts
  </read_first>
  <action>
    SCHEMA (server/prisma/schema.prisma): Add the following BEFORE the Student model: enum AwardLevel { SCHOOL REGIONAL STATE NATIONAL INTERNATIONAL } and enum CareerInterest { MEDICINE_HEALTH LAW ENGINEERING BUSINESS_FINANCE EDUCATION ARTS_DESIGN SCIENCE_RESEARCH IT_TECHNOLOGY HOSPITALITY SOCIAL_SERVICES SPORTS UNDECIDED }. Add AcademicResult model (id String @id @default(uuid()), studentId String, student Student @relation(fields:[studentId], references:[id], onDelete:Cascade), subject String, subjectOther String?, grade String @db.VarChar(20), calendarYear Int, formLevel FormLevel, notes String? @db.VarChar(200), createdAt DateTime @default(now()), updatedAt DateTime @updatedAt, @@index([studentId]), @@index([studentId, calendarYear(sort:Desc)])). Add Activity model (id String @id @default(uuid()), studentId String, student Student @relation(…onDelete:Cascade), organisation String, role String, description String? @db.VarChar(500), startMonth Int, startYear Int, endMonth Int?, endYear Int?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt, @@index([studentId])). Add back-relations to Student model: academicResults AcademicResult[] and activities Activity[]. Note: AwardLevel and CareerInterest enums are added now (used by Award and CareerGoal in Plans 02+03) so schema is stable for generate.

    RUN: cd server && npx prisma generate (regenerates client types including new models and enums)

    EXTEND testDb.ts: replace any @ts-expect-error stubs with the real deleteMany calls now that types exist.

    SCHEMAS: Create server/src/schemas/academicResult.ts — import z from zod and FORM_LEVELS from ./student.js. Define PRESET_SUBJECTS as const array (26 DSE subjects from 03-UI-SPEC.md Appendix A, ending with 'OTHER'). createAcademicResultSchema: z.object({ subject: z.enum(PRESET_SUBJECTS), subjectOther: z.string().trim().min(1).max(100).optional(), grade: z.string().trim().min(1).max(20), calendarYear: z.number().int().min(2010).max(2040), formLevel: z.enum(FORM_LEVELS), notes: z.string().trim().max(200).optional() }).strict().refine(data => data.subject !== 'OTHER' || !!data.subjectOther, { message:"Subject name is required when 'Other' is selected", path:['subjectOther'] }). updateAcademicResultSchema: createAcademicResultSchema.partial() with same refine.

    Create server/src/schemas/activity.ts — monthSchema: z.number().int().min(1).max(12), yearSchema: z.number().int().min(2000).max(2040). createActivitySchema: z.object({ organisation:z.string().trim().min(1).max(200), role:z.string().trim().min(1).max(200), description:z.string().trim().max(500).optional(), startMonth:monthSchema, startYear:yearSchema, endMonth:monthSchema.optional().nullable(), endYear:yearSchema.optional().nullable() }).strict(). updateActivitySchema: createActivitySchema.partial().

    SERVICES: Create server/src/services/academicResult.ts — export class AcademicResultNotFoundError extends Error. listAcademicResults(prisma, studentId): findMany where studentId, orderBy [calendarYear desc, formLevel desc]. createAcademicResult(prisma, studentId, data, userId): create with studentId spread, then logAudit CREATE. updateAcademicResult(prisma, id, studentId, data, userId): findUnique {where:{id}}, if (!existing || existing.studentId !== studentId) throw AcademicResultNotFoundError, update, logAudit UPDATE. deleteAcademicResult(prisma, id, studentId, userId): same IDOR check, delete, logAudit DELETE. PrismaClient param: use InstanceType<typeof PrismaClient> from ../generated/prisma/client.js.

    Create server/src/services/activity.ts — same structure; ActivityNotFoundError; listActivities orderBy [{ endYear:{ sort:'desc', nulls:'first' } }, { endMonth:{ sort:'desc', nulls:'first' } }, { startYear:'desc' }, { startMonth:'desc' }] (D-11 null-first for ongoing entries).

    ROUTES: Create server/src/routes/academicResults.ts — const router = Router({ mergeParams: true }). Copy parseStudentId helper from students.ts (reads req.params.studentId). All 4 verbs (GET /, POST /, PATCH /:resultId, DELETE /:resultId) following the exact pattern from 03-PATTERNS.md academicResults route excerpt. All routes already behind global validateJwt+resolveUser — no per-router middleware needed. req.user!.id passed to service functions. AcademicResultNotFoundError caught → 404.

    Create server/src/routes/activities.ts — identical structure; ActivityNotFoundError; uses createActivitySchema/updateActivitySchema.

    MOUNT: In server/src/routes/students.ts, add at the bottom (before export default router): import academicResultsRouter from './academicResults.js'; import activitiesRouter from './activities.js'; router.use('/:studentId/academics', academicResultsRouter); router.use('/:studentId/activities', activitiesRouter).
  </action>
  <acceptance_criteria>
    - server/prisma/schema.prisma contains model AcademicResult and model Activity with all fields as specified; AwardLevel and CareerInterest enums declared
    - server/src/schemas/academicResult.ts: PRESET_SUBJECTS array contains exactly 26 entries ending with 'OTHER'; createAcademicResultSchema uses .strict(); the subject==='OTHER' refine is present
    - server/src/services/academicResult.ts: updateAcademicResult contains `if (!existing || existing.studentId !== studentId)` IDOR check
    - server/src/services/activity.ts: listActivities orderBy contains `nulls: 'first'` on endYear
    - server/src/routes/academicResults.ts: `Router({ mergeParams: true })` at top; DELETE route returns 204
    - server/src/routes/students.ts: contains `router.use('/:studentId/academics'` and `router.use('/:studentId/activities'`
    - `cd server && npx tsc --noEmit` exits 0 (no TypeScript errors across all new files)
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | tail -5 && echo "TypeScript OK"</automated>
  </verify>
  <done>All new server files compile cleanly; AcademicResult + Activity models in schema; routes mounted in students.ts; clearDb updated with new models</done>
</task>

<task type="auto">
  <name>Task 3: Install @tanstack/react-query + QueryClientProvider, shared UI components, AcademicResultsSection + ActivitiesSection, StudentDetailPage placeholder replacement</name>
  <files>
    client/src/App.tsx
    client/src/lib/periodFormat.ts
    client/src/components/records/RecordSectionCard.tsx
    client/src/components/records/MonthYearPicker.tsx
    client/src/components/records/RecordDeleteDialog.tsx
    client/src/components/records/AcademicResultsSection.tsx
    client/src/components/records/ActivitiesSection.tsx
    client/src/pages/StudentDetailPage.tsx
  </files>
  <read_first>
    - client/src/App.tsx — full file; find where to wrap router/app tree with QueryClientProvider
    - client/src/pages/StudentDetailPage.tsx — full file; locate placeholder Card block (search for "Student records" text ~line 267-277); note existing import block and Card/CardHeader usage
    - client/src/components/students/ArchiveStudentDialog.tsx — AlertDialog pattern for RecordDeleteDialog; in-flight loading state pattern
    - client/src/components/students/StudentForm.tsx — useForm+zodResolver, FormField/FormItem/FormLabel/FormControl/FormMessage pattern, Select with string↔number conversion
    - client/src/api/apiClient.ts — apiGet/apiPost/apiPatch/apiDelete signatures and return types
    - .planning/phases/03-student-records-ui/03-UI-SPEC.md — Section 1 (Academics columns/dialog), Section 2 (Activities columns/dialog), Copywriting Contract (section headers, dialog titles, toast copy, validation messages), Interaction Specifications, Accessibility Baseline
    - .planning/phases/03-student-records-ui/03-RESEARCH.md — Pattern 6 (TanStack Query section fetching pattern), Pattern 8 (MonthYearPicker), Pattern 9 (Subject Other reveal), RecordSectionCard from Pattern code examples
    - .planning/phases/03-student-records-ui/03-PATTERNS.md — RecordSectionCard, AcademicResultsSection, ActivitiesSection, MonthYearPicker, RecordDeleteDialog pattern assignments with code excerpts
  </read_first>
  <action>
    INSTALL: cd client && npm install @tanstack/react-query (adds to package.json; no shadcn installs in this plan — checkbox/textarea/tooltip/scroll-area deferred to Plan 03).

    App.tsx: import { QueryClient, QueryClientProvider } from '@tanstack/react-query'. Create const queryClient = new QueryClient() OUTSIDE the component (module-level, not inside render). Wrap the router/app JSX tree with &lt;QueryClientProvider client={queryClient}&gt;...&lt;/QueryClientProvider&gt;. This is a single wrap — read the current App.tsx structure to find the exact insertion point.

    client/src/lib/periodFormat.ts (NEW): export const MONTH_NAMES = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec']. export function formatMonthYear(month:number, year:number): string — returns MONTH_NAMES[month-1] + ' ' + year. export function formatPeriod(startMonth, startYear, endMonth:number|null, endYear:number|null): string — if endMonth===null||endYear===null returns start + ' – Present'; else returns start + ' – ' + formatMonthYear(endMonth,endYear).

    client/src/components/records/RecordSectionCard.tsx (NEW): Props interface RecordSectionCardProps: title:string, count?:number, addLabel:string, onAdd:()=>void, isLoading?:boolean, isError?:boolean, onRetry?:()=>void, isEmpty?:boolean, emptyHeading?:string, emptyBody?:string, children?:React.ReactNode, hideAddButton?:boolean. Card with aria-busy={isLoading}. CardHeader: flex flex-row items-center justify-between space-y-0 pb-4. Left: &lt;h2&gt; with text-xl font-semibold leading-tight for title + &lt;span className="text-sm font-normal text-muted-foreground"&gt; ({count??0}) &lt;/span&gt;. Right: Button variant="default" size="sm" with Plus icon + addLabel (hidden when hideAddButton). CardContent: if isLoading show 3× &lt;Skeleton className="h-11 w-full" /&gt; in space-y-3; if isError show &lt;Alert variant="destructive"&gt;&lt;AlertDescription&gt;Couldn't load {title.toLowerCase()}. Try again.{onRetry && &lt;button onClick={onRetry} className="underline ml-1"&gt;Retry&lt;/button&gt;}&lt;/AlertDescription&gt;&lt;/Alert&gt;; if isEmpty show &lt;div className="py-8 text-center"&gt; with emptyHeading in text-sm font-medium and emptyBody in text-sm text-muted-foreground mt-1; else render children. Import Card/CardContent/CardHeader/CardTitle from @/components/ui/card; Button from @/components/ui/button; Skeleton from @/components/ui/skeleton; Alert/AlertDescription from @/components/ui/alert; Plus from lucide-react.

    client/src/components/records/MonthYearPicker.tsx (NEW): Props: label:string, monthValue:number|null|undefined, yearValue:number|null|undefined, onMonthChange:(v:number|null)=>void, onYearChange:(v:number|null)=>void, required?:boolean, disabled?:boolean. Render &lt;fieldset className="space-y-1.5"&gt;&lt;legend className="text-sm font-medium leading-none"&gt;{label}{required&&&lt;span aria-hidden&gt; *&lt;/span&gt;}&lt;/legend&gt;&lt;div className="flex gap-2"&gt; with two Select components (month and year). Month Select: options 1–12 mapped from MONTHS array ({value:1,label:'Jan'}…{value:12,label:'Dec'}), SelectTrigger className="w-24" aria-label="Month", value={monthValue?.toString()??''}, onValueChange={(v)=>onMonthChange(v?Number(v):null)}. Year Select: options from yearOptions(currentYear) = Array.from({length:11},(_,i)=>currentYear-i), SelectTrigger className="w-24" aria-label="Year", value={yearValue?.toString()??''}, onValueChange={(v)=>onYearChange(v?Number(v):null)}.

    client/src/components/records/RecordDeleteDialog.tsx (NEW): Props: open:boolean, onOpenChange:(o:boolean)=>void, onConfirm:()=>Promise&lt;void&gt;, isDeleting:boolean. Uses AlertDialog (already installed). AlertDialogTitle: "Delete this entry?". AlertDialogDescription: "This action cannot be undone." AlertDialogCancel disabled={isDeleting}: "Keep entry". AlertDialogAction variant="destructive" onClick={onConfirm} disabled={isDeleting}: isDeleting ? "Deleting…" : "Delete".

    client/src/components/records/AcademicResultsSection.tsx (NEW): Props: studentId:string. Type AcademicResult: {id:string, subject:string, subjectOther:string|null, grade:string, calendarYear:number, formLevel:string, notes:string|null}. useQuery queryKey=['student',studentId,'academics'], queryFn=apiGet&lt;AcademicResult[]&gt;(`/students/${studentId}/academics`). useMutation for create (POST → invalidate + toast.success('Academic result added')), update (PATCH → invalidate + toast.success('Academic result updated')), delete (DELETE → invalidate + toast.success('Entry deleted')). onError for all mutations: toast.error("Couldn't save changes. Please try again.") for create/update; toast.error("Couldn't delete entry. Please try again.") for delete. State: dialogOpen:boolean, editTarget:AcademicResult|null, deleteTarget:AcademicResult|null. RecordSectionCard props: title="Academic results", count={data.length}, addLabel="Add result", onAdd=openAddDialog, isLoading, isError, onRetry=refetch, isEmpty=data.length===0, emptyHeading="No academic results yet.", emptyBody="Add results to track this student's academic performance.". Table inside card: columns Subject, Grade, Year, Form, Notes (truncated 60 chars), Actions (Pencil+Trash ghost icon buttons). Subject cell: result.subject==='OTHER' ? result.subjectOther : result.subject. Dialog (shadcn Dialog): title "Add academic result"/"Edit academic result". Form fields: Subject (Select from PRESET_SUBJECTS; last option 'Other' triggers conditional Input for subjectOther — watch('subject')==='OTHER'), Grade (Input placeholder="e.g. A, B+, 85%, Distinction" maxLength=20), Calendar year (Select current year – 10yr range, string→number conversion), Form level (Select FORM_1–FORM_6 → display 'Form 1'–'Form 6'), Notes (Input placeholder="Optional — short note" maxLength=200). useForm({ resolver: zodResolver(academicResultFormSchema) }) where form schema mirrors server Zod schema (define client-side schema separately using PRESET_SUBJECTS constant). Dialog footer: Button variant="outline" "Discard" + submit Button variant="default" isPending?"Saving…":"Add result"/"Save result". Pre-populate form with editTarget values when editing.

    client/src/components/records/ActivitiesSection.tsx (NEW): Props: studentId:string. Type Activity: {id:string, organisation:string, role:string, description:string|null, startMonth:number, startYear:number, endMonth:number|null, endYear:number|null}. Same useQuery/useMutation pattern as AcademicResultsSection but for /activities endpoint. RecordSectionCard: title="Activities", emptyHeading="No activities yet.", emptyBody="Add extracurricular activities to build this student's profile.", addLabel="Add activity". Table columns: Organisation, Role, Period (formatPeriod(startMonth,startYear,endMonth,endYear)), Actions. Dialog: organisation (Input placeholder="e.g. SPCS Drama Society"), role (Input placeholder="e.g. President, Member"), description (Textarea — only textarea required in this plan is the Activities description field; import directly from @/components/ui/textarea if available, else use Input with multiple lines for now — NOTE: shadcn textarea is installed in Plan 03; for Plan 01 use a basic HTML textarea styled to match or skip description field in UI until Plan 03 installs textarea component), start date (MonthYearPicker label="Start date" required), end date (MonthYearPicker label="End date") + Checkbox "Ongoing / Present" — when checked set endMonth/endYear to null and disable the MonthYearPicker. Form schema validates startMonth required, endMonth/endYear optional nullable.

    client/src/pages/StudentDetailPage.tsx: Replace the placeholder Card (the one containing "Student records" as CardTitle and "Academic results, activities, awards, and other records will be available here…" as CardContent text) with:
    &lt;div className="flex flex-col gap-8"&gt;
      &lt;AcademicResultsSection studentId={student.id} /&gt;
      &lt;ActivitiesSection studentId={student.id} /&gt;
      {/* Awards and WorkExperience sections added in Plan 02 */}
      {/* CareerGoals and Notes sections added in Plan 03 */}
    &lt;/div&gt;
    Add imports for AcademicResultsSection and ActivitiesSection at top of file. Keep all other page code unchanged.
  </action>
  <acceptance_criteria>
    - client/package.json contains "@tanstack/react-query": version matching ^5.x in dependencies
    - client/src/App.tsx contains QueryClientProvider and QueryClient instantiation
    - client/src/lib/periodFormat.ts: formatPeriod(9,2022,null,null) returns 'Sep 2022 – Present'; formatPeriod(9,2022,6,2024) returns 'Sep 2022 – Jun 2024'
    - client/src/components/records/RecordSectionCard.tsx: RecordSectionCard interface includes hideAddButton prop; Card has aria-busy={isLoading}
    - client/src/components/records/AcademicResultsSection.tsx: imports useQuery and useMutation from '@tanstack/react-query'; queryKey includes 'academics' segment
    - client/src/components/records/ActivitiesSection.tsx: imports MonthYearPicker; uses formatPeriod in table Period column
    - client/src/pages/StudentDetailPage.tsx: no longer contains "Student records will be available here" text; contains AcademicResultsSection and ActivitiesSection imports and JSX
    - cd client && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | tail -5 && echo "Client TypeScript OK"</automated>
  </verify>
  <done>@tanstack/react-query installed; QueryClientProvider in App.tsx; RecordSectionCard + MonthYearPicker + RecordDeleteDialog + periodFormat.ts created; AcademicResultsSection + ActivitiesSection replace placeholder in StudentDetailPage; client compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/students/:id/academics | Untrusted: studentId and body from browser; staff role required |
| /api route handler → service layer | Server-internal: studentId validated by parseStudentId; recordId trust deferred to IDOR check in service |
| service → Prisma | Internal: parameterized queries; no string concatenation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / Info Disclosure | academicResult service update/delete | mitigate | IDOR guard: `if (!existing \|\| existing.studentId !== studentId) throw AcademicResultNotFoundError` — always 404, never 403 (ASVS V4.2) |
| T-03-02 | Tampering | academicResult + activity POST/PATCH routes | mitigate | Zod `.strict()` on createAcademicResultSchema and createActivitySchema — unknown fields rejected with 400 |
| T-03-03 | Repudiation | academicResult + activity services (create/update/delete) | mitigate | `logAudit()` called inside service functions after every write; not in route handlers |
| T-03-04 | Spoofing | all /academics and /activities routes | accept | `validateJwt` + `resolveUser` global middleware from Phase 1 inherited by all routes; no per-router setup needed |
| T-03-05 | Tampering | npm install @tanstack/react-query | mitigate | Package approved in RESEARCH.md Package Legitimacy Audit; same TanStack org as @tanstack/react-table already in project |
| T-03-SC | Tampering | npm/shadcn installs | mitigate | slopcheck — @tanstack/react-query verified [OK] in 03-RESEARCH.md Package Legitimacy table |
</threat_model>

<verification>
- `cd server && npm test -- --reporter=verbose 2>&1 | grep -E "stu-03|stu-04"` shows RED (failing) test output
- `cd server && npx tsc --noEmit` exits 0
- `cd client && npx tsc --noEmit` exits 0
- server/src/routes/students.ts contains router.use for '/:studentId/academics' and '/:studentId/activities'
- server/src/services/academicResult.ts contains IDOR check string `existing.studentId !== studentId`
- server/src/services/activity.ts contains `nulls: 'first'` in orderBy
- client/src/App.tsx contains QueryClientProvider
- client/src/pages/StudentDetailPage.tsx does NOT contain "Student records will be available here"
</verification>

<success_criteria>
- AcademicResult and Activity Prisma models in schema.prisma with correct fields, relations, and indexes per 03-RESEARCH.md Pattern 1
- Zod schemas with .strict(), PRESET_SUBJECTS, subject/subjectOther cross-field refinement, monthSchema/yearSchema
- CRUD services with IDOR guard (findUnique → check studentId → 404 on mismatch) and logAudit on every write
- Routes with Router({mergeParams:true}), parseStudentId helper, Zod safeParse, typed error handling
- students.ts mounts academics and activities sub-routers
- @tanstack/react-query installed; QueryClientProvider wraps app
- RecordSectionCard (shared wrapper), MonthYearPicker (fieldset+2 Selects), RecordDeleteDialog (AlertDialog simple confirm), periodFormat (formatPeriod returning "Present" for null end-date)
- AcademicResultsSection: table+dialog with subject preset + Other reveal, useQuery/useMutation
- ActivitiesSection: table+dialog with MonthYearPicker + Ongoing checkbox, useQuery/useMutation
- StudentDetailPage: placeholder replaced; AcademicResultsSection + ActivitiesSection rendered
- RED tests exist and fail as expected for STU-03 and STU-04
</success_criteria>

<output>
Create `.planning/phases/03-student-records-ui/03-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

**Server — new files:**
- `server/src/routes/academicResults.ts` — exports default Express Router with GET/POST/PATCH/:resultId/DELETE/:resultId
- `server/src/routes/activities.ts` — exports default Express Router with GET/POST/PATCH/:activityId/DELETE/:activityId
- `server/src/services/academicResult.ts` — exports AcademicResultNotFoundError, listAcademicResults, createAcademicResult, updateAcademicResult, deleteAcademicResult
- `server/src/services/activity.ts` — exports ActivityNotFoundError, listActivities, createActivity, updateActivity, deleteActivity
- `server/src/schemas/academicResult.ts` — exports PRESET_SUBJECTS, createAcademicResultSchema, updateAcademicResultSchema
- `server/src/schemas/activity.ts` — exports createActivitySchema, updateActivitySchema

**Server — modified files:**
- `server/prisma/schema.prisma` — adds enums AwardLevel, CareerInterest; models AcademicResult, Activity; back-relations on Student
- `server/src/routes/students.ts` — adds academicResultsRouter + activitiesRouter mounts
- `server/src/__tests__/records.test.ts` — new file with stu-03 + stu-04 RED test groups
- `server/src/__tests__/helpers/testDb.ts` — clearDb() extended with academicResult + activity deletes

**Client — new files:**
- `client/src/lib/periodFormat.ts` — exports MONTH_NAMES, formatMonthYear, formatPeriod
- `client/src/components/records/RecordSectionCard.tsx` — exports RecordSectionCard
- `client/src/components/records/MonthYearPicker.tsx` — exports MonthYearPicker
- `client/src/components/records/RecordDeleteDialog.tsx` — exports RecordDeleteDialog
- `client/src/components/records/AcademicResultsSection.tsx` — exports AcademicResultsSection
- `client/src/components/records/ActivitiesSection.tsx` — exports ActivitiesSection

**Client — modified files:**
- `client/src/App.tsx` — adds QueryClient instantiation + QueryClientProvider wrapper
- `client/src/pages/StudentDetailPage.tsx` — replaces placeholder; adds AcademicResultsSection + ActivitiesSection

**Enums declared (Prisma + mapped to Zod):**
- AwardLevel: SCHOOL, REGIONAL, STATE, NATIONAL, INTERNATIONAL (used by Plan 02)
- CareerInterest: MEDICINE_HEALTH, LAW, ENGINEERING, BUSINESS_FINANCE, EDUCATION, ARTS_DESIGN, SCIENCE_RESEARCH, IT_TECHNOLOGY, HOSPITALITY, SOCIAL_SERVICES, SPORTS, UNDECIDED (used by Plan 03)
