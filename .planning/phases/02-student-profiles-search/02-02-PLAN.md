---
phase: 02-student-profiles-search
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - server/src/schemas/student.ts
  - server/src/services/student.ts
  - server/src/routes/students.ts
  - server/src/__tests__/students.test.ts
  - client/src/api/apiClient.ts
  - client/src/components/students/ArchiveStudentDialog.tsx
  - client/src/pages/StudentDetailPage.tsx
  - client/src/App.tsx
  - client/src/components/ui/alert-dialog.tsx
autonomous: true
requirements:
  - STU-02
  - AUTH-02
  - AUTH-03
user_setup: []

must_haves:
  truths:
    - "Staff can open /students/:id and see profile fields in read-only mode"
    - "Staff can edit profile fields and save changes via PATCH"
    - "Staff can archive a student after typing their full name in the confirmation dialog"
    - "Archived students are hidden from default API responses (archivedAt set)"
    - "Admin can restore an archived student; Staff receives 403 on restore"
    - "Student detail shows Phase 3 placeholder card for record sections (D-04)"
  artifacts:
    - path: server/src/routes/students.ts
      provides: GET/PATCH/DELETE/:id and POST/:id/restore
    - path: client/src/pages/StudentDetailPage.tsx
      provides: Profile view/edit and archive flow
      min_lines: 80
    - path: client/src/components/students/ArchiveStudentDialog.tsx
      provides: Type-name confirmation per D-15
      min_lines: 40
  key_links:
    - from: client/src/components/students/ArchiveStudentDialog.tsx
      to: DELETE /api/students/:id
      via: apiDelete on confirm
      pattern: "apiDelete.*\\/students"
    - from: server/src/routes/students.ts
      to: requireRole(Role.ADMIN)
      via: POST /:id/restore
      pattern: "requireRole.*ADMIN"
    - from: client/src/pages/StudentDetailPage.tsx
      to: PATCH /api/students/:id
      via: apiPatch on Save changes
      pattern: "apiPatch.*\\/students"
---

## Phase Goal

**As a** careers staff member, **I want to** view, update, or archive a student profile, **so that** the directory stays accurate and outdated records can be removed from active use without losing data.

<objective>
Deliver view/edit/archive/restore vertical slice: integration tests (RED→GREEN), backend GET/PATCH/DELETE/restore endpoints, and StudentDetailPage with ArchiveStudentDialog.

Purpose: Completes STU-02 lifecycle and admin restore per D-13–D-16; detail page becomes landing target after create.
Output: Student detail/edit UI, soft-archive API, admin restore with role gate.
</objective>

<execution_context>
@C:/@code/spcs-transcripts/.cursor/gsd-core/workflows/execute-plan.md
@C:/@code/spcs-transcripts/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-student-profiles-search/02-CONTEXT.md
@.planning/phases/02-student-profiles-search/02-UI-SPEC.md
@.planning/phases/02-student-profiles-search/02-RESEARCH.md
@.planning/phases/02-student-profiles-search/02-PATTERNS.md
@server/src/routes/students.ts
@server/src/middleware/requireRole.ts
@client/src/components/students/StudentForm.tsx
@client/src/pages/StudentNewPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — view, edit, archive, and restore integration tests</name>
  <read_first>
    server/src/__tests__/students.test.ts
    server/src/__tests__/auth.test.ts
    server/src/middleware/requireRole.ts
    .planning/phases/02-student-profiles-search/02-VALIDATION.md
  </read_first>
  <files>server/src/__tests__/students.test.ts</files>
  <behavior>
    - stu-02-get: GET /api/students/:id returns 200 with student fields for existing active student
    - stu-02-get: GET unknown UUID returns 404
    - stu-02-patch: PATCH /api/students/:id updates fullName and writes audit UPDATE
    - stu-02-archive: DELETE /api/students/:id sets archivedAt non-null on the student row
    - stu-02-archive: subsequent GET /api/students/:id returns 200 with archivedAt populated (detail view retains archived records)
    - stu-02-archive: DELETE writes audit action DELETE
    - stu-02-restore: Admin token POST /api/students/:id/restore clears archivedAt; audit UPDATE with restored detail
    - auth-02-restore: Staff token POST restore returns 403
    - auth-03-update: PATCH and DELETE each create audit log entries
  </behavior>
  <action>
    Add test helper createTestStudent() in students.test.ts: POST via supertest or direct prisma.student.create after auth/me upsert, returning { id, fullName, ... }.

    Use makeTestToken for staff@school.edu (STAFF) and admin via BOOTSTRAP_ADMIN_EMAIL (ADMIN) per auth.test.ts pattern.

    Tests must fail RED until Task 2 implements routes.

    Do NOT assert list exclusion in stu-02-archive — GET /api/students list is implemented in Plan 03; list-default exclusion test belongs there only.

    Seed at least one student for archive tests.
  </action>
  <acceptance_criteria>
    - students.test.ts contains tests matching -t stu-02-get, stu-02-patch, stu-02-archive, stu-02-restore, auth-02-restore
    - stu-02-archive asserts DELETE sets archivedAt and GET :id returns archivedAt; no GET / list assertion
    - Running npm test -- src/__tests__/students.test.ts -t stu-02-get exits non-zero (route missing)
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test -- src/__tests__/students.test.ts -t stu-02-get --run; test $? -ne 0</automated>
  </verify>
  <done>All stu-02 and auth-02-restore test cases exist and fail until endpoints implemented; archive tests scoped to DELETE, archivedAt, GET :id, and audit only</done>
</task>

<task type="auto">
  <name>Task 2: GET/PATCH/DELETE/restore API and service methods</name>
  <read_first>
    server/src/routes/students.ts
    server/src/services/student.ts
    server/src/schemas/student.ts
    server/src/services/audit.ts
    .planning/phases/02-student-profiles-search/02-CONTEXT.md
  </read_first>
  <files>server/src/schemas/student.ts, server/src/services/student.ts, server/src/routes/students.ts</files>
  <action>
    Extend student.ts service per D-13, D-14:
    - getStudentById(prisma, id): findUnique; 404 if not found; return student regardless of archivedAt (detail view needs archived)
    - updateStudent(prisma, id, data, userId): update where archivedAt null OR allow update on archived for admin only — prefer block PATCH on archived with 409
    - archiveStudent(prisma, id, userId): update where id and archivedAt null, set archivedAt new Date(); logAudit DELETE
    - restoreStudent(prisma, id, userId): update archivedAt null; logAudit UPDATE with details { restored: true }

    Extend routes/students.ts:
    - GET /:id — return student JSON or 404 { error: 'Student not found' }
    - PATCH /:id — updateStudentSchema safeParse; 400 invalid; 404 not found; logAudit UPDATE
    - DELETE /:id — archiveStudent; 404 if already archived or missing
    - POST /:id/restore — requireRole(Role.ADMIN) middleware; restoreStudent; 403 handled by requireRole for staff

    All mutations call logAudit (AUTH-03). Never hard-delete rows (D-13).

    No schema.prisma changes in this plan — skip db push.
  </action>
  <acceptance_criteria>
    - cd server && npm test -- src/__tests__/students.test.ts -t "stu-02|auth-02-restore|auth-03" passes all cases
    - Staff POST restore returns 403
    - Admin POST restore returns 200 and clears archivedAt
    - DELETE sets archivedAt timestamp non-null
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test -- src/__tests__/students.test.ts -t "stu-02|auth-02-restore|auth-03" -x</automated>
  </verify>
  <done>All stu-02 backend tests GREEN; archive and restore behave per D-13, D-14</done>
</task>

<task type="auto">
  <name>Task 3: Student detail page, edit mode, and archive dialog</name>
  <read_first>
    client/src/pages/StudentNewPage.tsx
    client/src/components/students/StudentForm.tsx
    client/src/App.tsx
    .planning/phases/02-student-profiles-search/02-UI-SPEC.md
  </read_first>
  <files>client/src/pages/StudentDetailPage.tsx, client/src/components/students/ArchiveStudentDialog.tsx, client/src/App.tsx, client/src/api/apiClient.ts, client/src/components/ui/alert-dialog.tsx</files>
  <action>
    Add shadcn alert-dialog: cd client && npx shadcn@latest add alert-dialog

    Create ArchiveStudentDialog.tsx per D-15: AlertDialog with title "Archive student", description explaining hidden from list, input to type full name, Confirm "Archive student" disabled until input matches fullName trim+case-insensitive (RESEARCH A3), Cancel "Keep student". On confirm apiDelete(`/students/${id}`), toast "Student archived", navigate /students.

    Create StudentDetailPage.tsx at /students/:id per 02-UI-SPEC Screen 3:
    - Fetch GET /api/students/:id on mount via apiGet
    - Header: fullName text-2xl, subtitle "{Form label} · Class of {year} · {schoolStudentId}", transcriptStatus Badge (NONE/Draft/Finalised variants per UI-SPEC)
    - Profile card: read-only label/value grid default; Edit button toggles StudentForm in edit mode; Save changes calls apiPatch; Discard changes reverts; toast "Changes saved"
    - Phase 3 placeholder card per D-04: title "Student records", body exact copy from UI-SPEC
    - Archive Button variant destructive for non-archived students (Staff+Admin per D-14)
    - Admin viewing archived student: subtitle includes "(Archived)", show Restore student outline button calling apiPost(`/students/${id}/restore`), toast "Student restored"
    - 404 state: Alert "Student not found" + Back to students link
    - Loading: Skeleton placeholders

    Wire App.tsx route /students/:id with ProtectedRoute and userInfo prop.

    Extend apiPatch and apiDelete in apiClient if not done in Plan 01.
  </action>
  <acceptance_criteria>
    - /students/:uuid loads profile after create redirect from Plan 01
    - Edit → Save changes persists via PATCH and shows "Changes saved" toast
    - Archive dialog requires typed name match before enabling confirm
    - After archive, redirect to /students and student absent from default list (verified manually or in Plan 03)
    - Admin restore button visible only when userInfo.role === 'ADMIN' and student.archivedAt set
    - Copy matches UI-SPEC for archive dialog and placeholder card
  </acceptance_criteria>
  <verify>
    <automated>cd client && npm run build</automated>
  </verify>
  <done>Staff can view, edit, and archive students on detail page; admin can restore archived students</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | PATCH/DELETE body and :id param from browser |
| Staff→Admin | Restore and includeArchived gated by role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | GET /api/students/:id | mitigate | require JWT; return 404 not 403 for missing UUID to avoid enumeration [accept minimal info leak for staff-only app] |
| T-02-03 | Tampering | PATCH /api/students/:id | mitigate | updateStudentSchema whitelist fields; reject extra keys via Zod strict or strip |
| T-02-04 | Elevation of Privilege | POST /:id/restore | mitigate | requireRole(Role.ADMIN) per D-14 |
| T-02-05 | Repudiation | archive/restore | mitigate | logAudit DELETE on archive, UPDATE with restored:true on restore |
| T-02-06 | Spoofing | All /:id routes | mitigate | validateJwt + resolveUser (Phase 1 global middleware) |

</threat_model>

<verification>
- cd server && npm test -- src/__tests__/students.test.ts -t "stu-02|auth-02-restore|auth-03" -x passes
- cd client && npm run build passes
- Manual UAT: archive flow with typed name; admin restore
</verification>

<success_criteria>
- STU-02 satisfied: view, edit, delete (archive) profiles
- D-04, D-13, D-14, D-15, D-16 implemented
- AUTH-02: staff blocked from restore
- AUTH-03: audit on update, archive, restore
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | Location |
|--------|------|----------|
| updateStudentSchema | Zod schema | server/src/schemas/student.ts |
| getStudentById | function | server/src/services/student.ts |
| updateStudent | function | server/src/services/student.ts |
| archiveStudent | function | server/src/services/student.ts |
| restoreStudent | function | server/src/services/student.ts |
| apiPatch | function | client/src/api/apiClient.ts |
| apiDelete | function | client/src/api/apiClient.ts |
| ArchiveStudentDialog | component | client/src/components/students/ArchiveStudentDialog.tsx |
| StudentDetailPage | page | client/src/pages/StudentDetailPage.tsx |

<output>
Create `.planning/phases/02-student-profiles-search/02-02-SUMMARY.md` when done
</output>
