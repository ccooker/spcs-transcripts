---
phase: 02-student-profiles-search
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - server/src/schemas/student.ts
  - server/src/services/student.ts
  - server/src/routes/students.ts
  - server/src/__tests__/students.test.ts
  - client/src/pages/StudentsListPage.tsx
  - client/src/components/students/StudentsDataTable.tsx
  - client/src/components/students/StudentColumns.tsx
  - client/src/components/students/CohortGroupHeader.tsx
  - client/src/App.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - STU-02
user_setup: []

must_haves:
  truths:
    - "D-09: Staff see paginated student list at /students as a data table with sortable columns (server-side sort via TanStack manualSorting)"
    - "Clicking Search students applies name filter via server q param (not on every keystroke)"
    - "Form level and transcript status filters refetch from server immediately"
    - "Table shows cohort group header rows with per-form Draft/Finalised/None counts when sorted by form"
    - "Each student row shows transcript status badge"
    - "Admin can toggle Show archived students and restore from list row"
    - "Row click navigates to /students/:id"
  artifacts:
    - path: server/src/services/student.ts
      provides: listStudents with pagination, search, filter, sort
    - path: client/src/pages/StudentsListPage.tsx
      provides: Search, filters, table, pagination UI
      min_lines: 80
    - path: client/src/components/students/StudentsDataTable.tsx
      provides: TanStack manualPagination/Sorting table
      min_lines: 60
    - path: client/src/components/students/CohortGroupHeader.tsx
      provides: Per-form summary row (NAV-03, D-10)
      min_lines: 20
  key_links:
    - from: client/src/pages/StudentsListPage.tsx
      to: GET /api/students
      via: apiGet with query string built from appliedQuery, filters, pagination, sort
      pattern: "apiGet.*\\/students\\?"
    - from: server/src/routes/students.ts
      to: listStudentsQuerySchema
      via: safeParse req.query
      pattern: "listStudentsQuerySchema"
    - from: client/src/components/students/StudentsDataTable.tsx
      to: CohortGroupHeader
      via: inject row when formLevel changes
      pattern: "CohortGroupHeader"
---

## Phase Goal

**As a** careers staff member, **I want to** search, filter, and browse students grouped by form with status indicators, **so that** I can quickly find any student or survey cohort transcript progress.

<objective>
Complete the student directory navigation surface: list API with server-side search/filter/sort/pagination (RED→GREEN), and StudentsListPage with TanStack Data Table, cohort group headers, and admin archived toggle.

Purpose: Delivers NAV-01, NAV-02, NAV-03 and replaces Plan 01 /students stub with full list experience.
Output: GET /api/students, StudentsListPage, StudentsDataTable, CohortGroupHeader, activated nav.
</objective>

<execution_context>
@C:/@code/spcs-transcripts/.cursor/gsd-core/workflows/execute-plan.md
@C:/@code/spcs-transcripts/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-student-profiles-search/02-CONTEXT.md
@.planning/phases/02-student-profiles-search/02-UI-SPEC.md
@.planning/phases/02-student-profiles-search/02-RESEARCH.md
@.planning/phases/02-student-profiles-search/02-PATTERNS.md
@client/src/components/layout/AppShell.tsx
@server/src/routes/students.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — list, search, and filter integration tests</name>
  <read_first>
    server/src/__tests__/students.test.ts
    .planning/phases/02-student-profiles-search/02-RESEARCH.md
    .planning/phases/02-student-profiles-search/02-VALIDATION.md
  </read_first>
  <files>server/src/__tests__/students.test.ts</files>
  <behavior>
    - nav-01: GET /api/students?q=Chan returns only students whose fullName contains Chan case-insensitive
    - nav-02-form: GET /api/students?formLevel=FORM_4 returns only FORM_4 students
    - nav-02-status: GET /api/students?transcriptStatus=NONE returns only NONE status students
    - list-default: GET /api/students excludes archived students (archivedAt not null) by default
    - list-admin-archived: GET /api/students?includeArchived=true with ADMIN token includes archived; STAFF token gets 403 on includeArchived=true
    - list-pagination: GET /api/students?page=1&pageSize=2 returns meta { page, pageSize, total, totalPages } and at most 2 data rows
    - list-sort: GET /api/students?sort=fullName&order=asc returns ascending fullName order
  </behavior>
  <action>
    Extend students.test.ts with nav-01, nav-02-form, nav-02-status, and list-* tests.

    Seed helper: create 4+ students across FORM_1 and FORM_4 with varied fullNames; archive one for exclusion test.

    Tests fail RED until GET / list handler exists.
  </action>
  <acceptance_criteria>
    - Tests match -t nav-01, nav-02-form, nav-02-status
    - npm test -- src/__tests__/students.test.ts -t nav-01 fails before implementation
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test -- src/__tests__/students.test.ts -t nav-01 --run; test $? -ne 0</automated>
  </verify>
  <done>nav-01, nav-02, and list behavior tests exist and fail until GET / implemented</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/students list API with query validation</name>
  <read_first>
    server/src/schemas/student.ts
    server/src/services/student.ts
    server/src/routes/students.ts
    server/src/middleware/requireRole.ts
    .planning/phases/02-student-profiles-search/02-RESEARCH.md
  </read_first>
  <files>server/src/schemas/student.ts, server/src/services/student.ts, server/src/routes/students.ts</files>
  <action>
    Add listStudentsQuerySchema to student.ts schemas per RESEARCH Pattern 2:
    - q optional string
    - formLevel optional enum FORM_1..FORM_6
    - transcriptStatus optional DRAFT|FINALISED|NONE
    - page default 1, pageSize default 50 max 100
    - sort whitelist fullName|formLevel|graduationYear|transcriptStatus default formLevel
    - order asc|desc default asc
    - includeArchived boolean default false

    Implement listStudents(prisma, query, user) in student.ts:
    - where archivedAt: includeArchived ? undefined : null (D-13)
    - If includeArchived true and user.role !== ADMIN return 403 from route (D-14)
    - ILIKE search on fullName when q present
    - orderBy from sort/order; default formLevel asc then fullName asc for cohort grouping
    - skip/take pagination; return { data, meta: { page, pageSize, total, totalPages } }

    Add GET '/' to students router before GET '/:id' (order matters): parse query, call listStudents, 400 on invalid query.

    No schema.prisma changes — no db push needed.
  </action>
  <acceptance_criteria>
    - cd server && npm test -- src/__tests__/students.test.ts -t "nav-01|nav-02|list-" -x all pass
    - Default list excludes archived students
    - includeArchived=true returns 403 for STAFF token
    - Response shape includes data array and meta object with totalPages
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test -- src/__tests__/students.test.ts -t "nav-01|nav-02|list-" -x</automated>
  </verify>
  <done>GET /api/students supports search, filter, sort, pagination per NAV-01 and NAV-02</done>
</task>

<task type="auto">
  <name>Task 3: Students list page with data table and cohort headers</name>
  <read_first>
    client/src/pages/StudentDetailPage.tsx
    client/src/components/layout/AppShell.tsx
    .planning/phases/02-student-profiles-search/02-UI-SPEC.md
    .planning/phases/02-student-profiles-search/02-PATTERNS.md
  </read_first>
  <files>client/src/pages/StudentsListPage.tsx, client/src/components/students/StudentsDataTable.tsx, client/src/components/students/StudentColumns.tsx, client/src/components/students/CohortGroupHeader.tsx, client/src/App.tsx</files>
  <action>
    Replace Plan 01 /students stub with full StudentsListPage per 02-UI-SPEC Screen 1.

    Follow PATTERNS.md analogs explicitly:
    - StudentsListPage: mirror HomePage loading skeleton (PATTERNS.md "StudentsListPage" — Skeleton h-8/h-6 pattern) + App.tsx data-fetch useEffect pattern; wrap in AppShell activeNav="students"
    - StudentColumns: map transcriptStatus to Badge variants per PATTERNS.md "StudentColumns" (FINALISED→default, DRAFT→secondary, NONE→outline) and 02-UI-SPEC
    - StudentsDataTable: TanStack manualPagination/Sorting/Filtering setup from PATTERNS.md "StudentsDataTable" + RESEARCH Pattern 4; loading rows use Skeleton h-12 per UI-SPEC
    - CohortGroupHeader: inject TableRow between student rows per PATTERNS.md "CohortGroupHeader" + RESEARCH Pattern 5 (bg-muted/50, colSpan, Draft/Finalised/None counts from current filtered page data per RESOLVED open question 1)

    StudentsListPage state:
    - searchInput (local, not sent until Search clicked) vs appliedQuery (sent as q) per D-11
    - formLevelFilter, transcriptStatusFilter — immediate refetch on change (D-08 filter UI)
    - includeArchived toggle — admin only (D-14), hidden for Staff
    - pagination { pageIndex, pageSize: 50 }, sorting state
    - Fetch apiGet(`/students?${params}`) when appliedQuery, filters, pagination, sorting change — NOT on searchInput onChange

    StudentColumns.tsx: columns fullName, formLevel (Form N label), graduationYear (Class of YYYY), schoolStudentId, transcriptStatus Badge per UI-SPEC variants, actions chevron/link to /students/:id. Sortable headers with aria-sort.

    D-09: StudentsDataTable.tsx uses useReactTable with manualPagination, manualSorting, manualFiltering true; column headers clickable for server-side sort (asc/desc toggle); render shadcn Table suited to 200–600 students; loading skeleton 5 rows; empty states per UI-SPEC copy ("No students yet" vs "No students found").

    CohortGroupHeader.tsx per D-10, NAV-03: when sort column is formLevel (default), inject header row before each form group change in rendered rows. Counts Draft/Finalised/None from current page data only (RESEARCH A1). Suppress headers when sort is not form-based.

    Admin archived rows: bg-muted/30, "(Archived)" label, Restore button in actions calling apiPost restore then refetch.

    Page header: h1 "Students", Button "Add student" → /students/new.

    Toolbar: Input placeholder "Search by name", Button "Search students", Select "All forms"/Form 1-6, Select "All statuses"/Draft/Finalised/None, admin checkbox "Show archived students".

    Pagination footer: "Showing {start}–{end} of {total} students", Prev/Next min-h 44px.

    Update App.tsx /students route to StudentsListPage with userInfo.

    Error state: Alert destructive "Couldn't load students" + Try again button.
  </action>
  <acceptance_criteria>
    - /students renders table inside AppShell with Students nav active
    - Search students button sends q param; typing alone does not refetch (verify no API call on keystroke-only)
    - Form and status filters trigger immediate refetch
    - Cohort group headers visible with default formLevel sort showing "Form N" and "Draft: n · Finalised: n · None: n"
    - Row click navigates to /students/:id
    - cd client && npm run build passes
    - cd server && npm test -- src/__tests__/students.test.ts -x full file passes
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test -- src/__tests__/students.test.ts -x && cd ../client && npm run build</automated>
  </verify>
  <done>Full student directory with search, filter, cohort overview, and pagination live at /students</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Query params q, formLevel, sort — untrusted strings |
| Admin list | includeArchived exposes archived PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | GET /api/students?q | mitigate | Prisma contains/mode insensitive — parameterized; never raw SQL interpolate q |
| T-02-07 | Tampering | sort query param | mitigate | Zod enum whitelist for sort column names only |
| T-02-08 | Elevation of Privilege | includeArchived | mitigate | 403 unless req.user.role === ADMIN (D-14) |
| T-02-09 | Information Disclosure | List response | accept | All staff see all active students by design (RESEARCH V4) |
| T-02-10 | Denial of Service | pageSize | mitigate | Cap pageSize at 100 in Zod schema |

</threat_model>

<verification>
- cd server && npm test -- src/__tests__/students.test.ts -x all green
- cd client && npm run build passes
- Manual UAT per 02-VALIDATION.md: cohort headers, search button behavior, admin archived toggle
</verification>

<success_criteria>
- NAV-01, NAV-02, NAV-03 satisfied
- D-09, D-10, D-11 implemented
- Phase 2 ROADMAP success criteria 1–5 achievable after all three plans execute
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | Location |
|--------|------|----------|
| listStudentsQuerySchema | Zod schema | server/src/schemas/student.ts |
| listStudents | function | server/src/services/student.ts |
| StudentsListPage | page | client/src/pages/StudentsListPage.tsx |
| StudentsDataTable | component | client/src/components/students/StudentsDataTable.tsx |
| StudentColumns | column defs | client/src/components/students/StudentColumns.tsx |
| CohortGroupHeader | component | client/src/components/students/CohortGroupHeader.tsx |

<output>
Create `.planning/phases/02-student-profiles-search/02-03-SUMMARY.md` when done
</output>
