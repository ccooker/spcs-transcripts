---
phase: 02-student-profiles-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/prisma/schema.prisma
  - server/src/schemas/student.ts
  - server/src/services/student.ts
  - server/src/routes/students.ts
  - server/src/app.ts
  - server/src/__tests__/students.test.ts
  - server/src/__tests__/helpers/testDb.ts
  - client/package.json
  - client/src/api/apiClient.ts
  - client/src/components/layout/AppShell.tsx
  - client/src/components/students/StudentForm.tsx
  - client/src/lib/formLevels.ts
  - client/src/pages/HomePage.tsx
  - client/src/pages/StudentNewPage.tsx
  - client/src/App.tsx
  - client/src/main.tsx
autonomous: true
requirements:
  - STU-01
  - AUTH-03
user_setup: []

must_haves:
  truths:
    - "Staff can submit a create-student form with full name, form level, graduation year, and school student ID"
    - "A newly created student is persisted in PostgreSQL with UUID id and transcriptStatus NONE"
    - "Creating a student writes an AuditLog entry with action CREATE and model Student"
    - "After create, staff are redirected to the new student's detail route placeholder or list with success feedback"
  artifacts:
    - path: server/prisma/schema.prisma
      provides: Student model with FormLevel and TranscriptStatus enums
      contains: "model Student"
    - path: server/src/routes/students.ts
      provides: POST /api/students
      exports: ["default"]
    - path: client/src/pages/StudentNewPage.tsx
      provides: Full-page create form at /students/new
      min_lines: 40
    - path: client/src/components/layout/AppShell.tsx
      provides: Shared authenticated layout with active Students nav
      min_lines: 50
  key_links:
    - from: client/src/pages/StudentNewPage.tsx
      to: POST /api/students
      via: apiPost via StudentForm submit
      pattern: "apiPost.*\\/students"
    - from: server/src/routes/students.ts
      to: server/src/services/audit.ts
      via: logAudit on CREATE
      pattern: "logAudit.*CREATE"
    - from: server/src/routes/students.ts
      to: server/prisma/schema.prisma
      via: prisma.student.create
      pattern: "prisma\\.student\\.create"
---

## Phase Goal

**As a** careers staff member, **I want to** add a new student profile with name, form level, and school ID, **so that** their record exists before I enter academic and activity data in Phase 3.

<objective>
Deliver the thinnest end-to-end create-student vertical slice: failing integration tests (RED), Prisma Student schema + POST API (GREEN), and `/students/new` UI wired through AppShell.

Purpose: Establishes the Student entity and first staff-facing workflow — the foundation for all later Phase 2 slices.
Output: Student model in DB, POST /api/students, StudentNewPage, AppShell, apiPost helper.
</objective>

<execution_context>
@C:/@code/spcs-transcripts/.cursor/gsd-core/workflows/execute-plan.md
@C:/@code/spcs-transcripts/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-student-profiles-search/02-CONTEXT.md
@.planning/phases/02-student-profiles-search/02-RESEARCH.md
@.planning/phases/02-student-profiles-search/02-PATTERNS.md
@.planning/phases/02-student-profiles-search/02-UI-SPEC.md
@server/src/__tests__/auth.test.ts
@server/src/services/audit.ts
@server/src/app.ts
@client/src/pages/HomePage.tsx
@client/src/api/apiClient.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — create-student integration tests and test DB scaffold</name>
  <read_first>
    server/src/__tests__/auth.test.ts
    server/src/__tests__/helpers/testDb.ts
    server/src/__tests__/helpers/makeTestToken.ts
    server/src/services/audit.ts
    .planning/phases/02-student-profiles-search/02-VALIDATION.md
  </read_first>
  <files>server/src/__tests__/students.test.ts, server/src/__tests__/helpers/testDb.ts, server/package.json</files>
  <behavior>
    - stu-01-create: POST /api/students with valid body returns 201, body contains id (UUID), fullName, formLevel, graduationYear, schoolStudentId, transcriptStatus NONE
    - stu-01-create: POST without Authorization returns 401
    - stu-01-create: POST missing required fullName returns 400
    - stu-01-create: duplicate schoolStudentId returns 409 with error message containing "already exists"
    - auth-03-create: successful create writes exactly one AuditLog with action CREATE, model Student, recordId matching response id
  </behavior>
  <action>
    Install server dependency: cd server && npm install zod@4.4.3 (per RESEARCH.md Package Legitimacy Audit — all packages OK, no checkpoint needed).

    Extend testDb.ts clearDb(): add await prisma.student.deleteMany() as the FIRST delete (before auditLog.deleteMany and user.deleteMany).

    Create server/src/__tests__/students.test.ts following auth.test.ts structure (describe blocks, beforeEach(clearDb), makeTestToken with preferred_username staff@school.edu).

    Test payload for happy path: { fullName: "Chan Tai Man", formLevel: "FORM_4", graduationYear: 2027, schoolStudentId: "S2024001" }.

    Import app from ../app.js — tests MUST fail initially because students router does not exist yet (RED state).

    Do NOT implement routes in this task — tests only.
  </action>
  <acceptance_criteria>
    - File server/src/__tests__/students.test.ts exists with test names matching -t stu-01 and -t auth-03-create
    - cd server && npm test -- src/__tests__/students.test.ts -t stu-01 returns failing tests (not "no tests found")
    - testDb.ts contains prisma.student.deleteMany before prisma.auditLog.deleteMany
    - server/package.json lists zod dependency
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test -- src/__tests__/students.test.ts -t stu-01 --run; test $? -ne 0</automated>
  </verify>
  <done>students.test.ts exists with stu-01 and auth-03-create cases; running them fails because POST /api/students is not implemented</done>
</task>

<task type="auto">
  <name>Task 2: Student schema, Zod validation, POST API, and schema push</name>
  <read_first>
    server/prisma/schema.prisma
    server/src/services/audit.ts
    server/src/routes/auth.ts
    server/src/app.ts
    .planning/phases/02-student-profiles-search/02-RESEARCH.md
    .planning/phases/02-student-profiles-search/02-CONTEXT.md
  </read_first>
  <files>server/prisma/schema.prisma, server/src/schemas/student.ts, server/src/services/student.ts, server/src/routes/students.ts, server/src/app.ts</files>
  <action>
    Add to schema.prisma per D-01, D-02, D-05, D-07, D-08:
    - enum FormLevel { FORM_1 FORM_2 FORM_3 FORM_4 FORM_5 FORM_6 }
    - enum TranscriptStatus { DRAFT FINALISED NONE }
    - model Student with fields: id String @id @default(uuid()), fullName String, formLevel FormLevel, graduationYear Int, schoolStudentId String @unique, studentEmail String?, studentPhone String?, parentEmail String?, parentPhone String?, transcriptStatus TranscriptStatus @default(NONE), archivedAt DateTime?, createdAt, updatedAt
    - Indexes: @@index([fullName]), @@index([formLevel, archivedAt]), @@index([transcriptStatus]), @@index([archivedAt])

    Create server/src/schemas/student.ts exporting FORM_LEVELS const, createStudentSchema (per RESEARCH.md Zod template with fullName min 1 max 200, formLevel enum, graduationYear int 2020-2040, schoolStudentId min 1 max 50, optional contact emails/phones), and updateStudentSchema as createStudentSchema.partial() excluding schoolStudentId changes if desired or allow patch of all except id.

    Create server/src/services/student.ts with createStudent(prisma, data, userId) calling prisma.student.create and logAudit with action CREATE, model 'Student'.

    Create server/src/routes/students.ts with Router, POST '/' handler: safeParse body with createStudentSchema, on failure 400 { error: 'Invalid request body' }, on P2002 409 { error: 'A student with this ID already exists.' }, on success 201 with student JSON. Use req.user!.id for audit.

    Register in app.ts: import studentsRouter from './routes/students.js'; app.use('/api/students', studentsRouter) after auth router.

    Generate migration: cd server && npx prisma migrate dev --name add_student --create-only then apply OR use migrate dev if DB available.

    Per D-03: server validation must require fullName, formLevel, graduationYear, schoolStudentId; contact fields optional.

    [BLOCKING] After all schema.prisma edits, ensure PostgreSQL is running (docker compose up -d), then from server/: npx prisma db push (use --accept-data-loss if non-TTY prompt), then npx prisma generate. Do NOT mark task done until push exits 0.
  </action>
  <acceptance_criteria>
    - schema.prisma contains model Student and enums FormLevel, TranscriptStatus
    - POST /api/students returns 201 for valid payload in integration test
    - stu-01-create and auth-03-create tests pass
    - Duplicate schoolStudentId returns 409
    - npx prisma db push exits 0 and npx prisma generate exits 0
    - PostgreSQL Student table exists with columns matching schema.prisma
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx prisma db push && npx prisma generate && npm test -- src/__tests__/students.test.ts -t "stu-01|auth-03-create" -x</automated>
  </verify>
  <done>POST /api/students creates Student rows with audit log; schema pushed to DB; stu-01 and auth-03-create tests GREEN</done>
</task>

<task type="auto">
  <name>Task 3: Client packages and shadcn component bootstrap</name>
  <read_first>
    client/package.json
    client/src/main.tsx
    client/src/components/ui/button.tsx
    .planning/phases/02-student-profiles-search/02-PATTERNS.md
    .planning/phases/02-student-profiles-search/02-UI-SPEC.md
  </read_first>
  <files>client/package.json, client/src/main.tsx, client/src/components/ui/table.tsx, client/src/components/ui/input.tsx, client/src/components/ui/select.tsx, client/src/components/ui/form.tsx, client/src/components/ui/label.tsx</files>
  <action>
    Install client deps per 02-UI-SPEC.md and RESEARCH.md Package Legitimacy Audit (all OK): cd client && npm install @tanstack/react-table@8.21.3 react-hook-form@7.78.0 zod@4.4.3 @hookform/resolvers@5.4.0 sonner@2.0.7

    Add shadcn components per PATTERNS.md "client/src/components/ui/*": cd client && npx shadcn@latest add table input select form label (non-interactive flags if supported; otherwise accept defaults). Match existing button.tsx conventions (@/ aliases, forwardRef, cva).

    Mount Toaster from sonner in main.tsx root alongside existing providers.

    Do NOT implement AppShell, StudentForm, or routes in this task — prep only so Task 4 focuses on UI wiring.
  </action>
  <acceptance_criteria>
    - client/package.json lists @tanstack/react-table, react-hook-form, zod, @hookform/resolvers, sonner
    - client/src/components/ui/table.tsx, input.tsx, select.tsx, form.tsx, label.tsx exist
    - Sonner Toaster mounted in main.tsx
    - cd client && npm run build passes with new components imported nowhere yet (or minimal smoke import)
  </acceptance_criteria>
  <verify>
    <automated>cd client && npm run build</automated>
  </verify>
  <done>Client table/form dependencies installed; shadcn primitives ready for StudentForm and list UI in Task 4</done>
</task>

<task type="auto">
  <name>Task 4: AppShell, create-student UI, and routing</name>
  <read_first>
    client/src/pages/HomePage.tsx
    client/src/App.tsx
    client/src/api/apiClient.ts
    .planning/phases/02-student-profiles-search/02-UI-SPEC.md
    .planning/phases/02-student-profiles-search/02-PATTERNS.md
  </read_first>
  <files>client/src/api/apiClient.ts, client/src/components/layout/AppShell.tsx, client/src/components/students/StudentForm.tsx, client/src/lib/formLevels.ts, client/src/pages/HomePage.tsx, client/src/pages/StudentNewPage.tsx, client/src/App.tsx</files>
  <action>
    Extend apiClient.ts with apiPost, apiPatch, apiDelete helpers mirroring apiGet error pattern (throw Error with status property). Follow PATTERNS.md "apiClient.ts" extension pattern.

    Create client/src/lib/formLevels.ts: FORM_LEVELS array and formLevelLabel mapping FORM_1 to "Form 1" through FORM_6 (per D-05, D-06).

    Extract AppShell from HomePage.tsx per PATTERNS.md "AppShell" analog: props { userInfo: UserInfo | null, activeNav?: 'home' | 'students', children }. Remove aria-disabled/preventDefault on Students link; apply active styling when activeNav='students'. Refactor HomePage to wrap content in AppShell activeNav="home".

    Create StudentForm.tsx: React Hook Form + zodResolver mirroring server createStudentSchema fields per D-01, D-02, D-03. Use shadcn Form/Input/Select from Task 3. Required labels with asterisk: Full name, Form level, Graduation year (Select dropdown current year through current+6, display "Class of {year}" per D-07), School student ID. Optional section "Contact details (optional)" with four contact fields. Inline FormMessage on validation errors.

    Create StudentNewPage.tsx at route /students/new per D-12 (full page, not modal): AppShell, "Back to students" link, h1 "Add student", StudentForm, actions "Back without saving" (outline, navigates /students) and "Create student" (primary). On submit call apiPost('/students', data); on 409 show toast "A student with this ID already exists." and field error; on success sonner toast "Student created" and navigate to `/students/${id}`.

    Add App.tsx routes: /students/new wrapped in ProtectedRoute with userInfo prop. Add /students route rendering minimal stub: AppShell + h1 "Students" + Button "Add student" linking to /students/new — enough for nav until Plan 03 ships full list.
  </action>
  <acceptance_criteria>
    - Navigating to /students/new renders create form inside AppShell with Students nav active
    - Submitting valid required fields calls POST /api/students and redirects to /students/:uuid
    - HomePage still renders inside AppShell without regression
    - apiPost exported from apiClient.ts
    - Copy matches 02-UI-SPEC: page title "Add student", CTA "Create student", success toast "Student created"
  </acceptance_criteria>
  <verify>
    <automated>cd client && npm run build</automated>
  </verify>
  <done>Staff can open /students/new, create a student end-to-end, and land on detail route UUID URL with success toast</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted form input crosses to POST /api/students |
| API→PostgreSQL | Prisma parameterized queries; schoolStudentId uniqueness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | POST /api/students | mitigate | Zod createStudentSchema safeParse; reject unknown fields; 400 on invalid body |
| T-02-02 | Information Disclosure | Error handlers | mitigate | Log studentId only in server errors; never log fullName (RESEARCH security) |
| T-02-03 | Elevation of Privilege | Student.id | mitigate | UUID @default(uuid()) per D-01/Pitfall 4 — no sequential IDs |
| T-02-04 | Repudiation | createStudent | mitigate | logAudit CREATE on every successful create (AUTH-03) |
| T-02-05 | Spoofing | POST /api/students | mitigate | validateJwt + resolveUser global middleware on /api prefix (Phase 1) |
| T-02-SC | Tampering | npm install zod | mitigate | Package Legitimacy Audit marks zod OK; no [SUS] packages |

</threat_model>

<verification>
- cd server && npm test -- src/__tests__/students.test.ts -t "stu-01|auth-03-create" -x passes
- cd server && npx prisma db push succeeds
- cd client && npm run build passes
- Manual: create student via /students/new; verify row in DB and audit log entry
</verification>

<success_criteria>
- STU-01 satisfied: staff create profile with name, form level, contact details (optional), school ID
- AUTH-03 partial: audit log on create
- All locked decisions D-01, D-02, D-03, D-05, D-06, D-07, D-08, D-12 implemented in this slice
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | Location |
|--------|------|----------|
| FormLevel | enum | server/prisma/schema.prisma |
| TranscriptStatus | enum | server/prisma/schema.prisma |
| Student | model | server/prisma/schema.prisma |
| FORM_LEVELS | const | server/src/schemas/student.ts |
| createStudentSchema | Zod schema | server/src/schemas/student.ts |
| updateStudentSchema | Zod schema | server/src/schemas/student.ts |
| createStudent | function | server/src/services/student.ts |
| studentsRouter | Express Router | server/src/routes/students.ts |
| apiPost | function | client/src/api/apiClient.ts |
| AppShell | component | client/src/components/layout/AppShell.tsx |
| StudentForm | component | client/src/components/students/StudentForm.tsx |
| formLevelLabel | function | client/src/lib/formLevels.ts |
| StudentNewPage | page | client/src/pages/StudentNewPage.tsx |

<output>
Create `.planning/phases/02-student-profiles-search/02-01-SUMMARY.md` when done
</output>
