---
phase: 04-document-management
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - server/src/generated/prisma/
  - server/src/__tests__/documents.test.ts
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "DocumentType enum exists in the live PostgreSQL database (per D-01 through D-04)"
    - "Document table exists in the live PostgreSQL database with all columns from schema.prisma (per D-03)"
    - "Prisma generated client exposes prisma.document and the DocumentType enum to TypeScript (per DOC-01 through DOC-04)"
    - "All 11 doc- integration tests are GREEN (per DOC-01 through DOC-04)"
    - "Full prior test suite (62+) is also GREEN — schema push did not break existing tables"
    - "testDb.ts document.deleteMany() @ts-expect-error stub removed and replaced with real typed call"
  artifacts:
    - path: "server/src/generated/prisma/client.d.ts"
      provides: "TypeScript types for Document model and DocumentType enum"
      contains: "DocumentType"
    - path: "server/src/generated/prisma/client.js"
      provides: "PrismaClient with document accessor"
      contains: "document"
  key_links:
    - from: "server/src/__tests__/documents.test.ts"
      to: "POST /api/students/:id/documents"
      via: "supertest request(app).post(…).attach(…).field(…)"
      pattern: "attach.*field"
    - from: "server/src/services/document.ts"
      to: "prisma.document"
      via: "prisma.document.create / findMany / findUnique / update"
      pattern: "prisma\\.document"
---

<objective>
Push the Document schema to the live PostgreSQL database (docker-compose postgres), regenerate the Prisma client so TypeScript has full Document types, then verify all 11 document integration tests turn GREEN. Also clean up the @ts-expect-error stub in testDb.ts that was added in 04-01 to work around the absent generated client.

After this plan the full server API is live: upload, list, download, and soft-delete of PDF documents all work end-to-end against the real database. The client slice (Plan 04-03) can now be built against a fully functional API.

Purpose: Activate the Document model in the database and confirm every DOC-01 through DOC-04 behavior passes automated tests.
Output: Document table in PostgreSQL, regenerated Prisma client, 11 GREEN tests, 1 stub removed from testDb.ts.
</objective>

<execution_context>
@C:/@code/spcs-transcripts/.cursor/gsd-core/workflows/execute-plan.md
@C:/@code/spcs-transcripts/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-document-management/04-RESEARCH.md
@.planning/phases/04-document-management/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] DB push — push Document schema to PostgreSQL + regenerate Prisma client</name>
  <files>server/src/generated/prisma/, server/src/__tests__/helpers/testDb.ts</files>

  <read_first>
    - server/prisma/schema.prisma — confirm DocumentType enum and Document model are present (written in 04-01 Task 2)
    - server/src/__tests__/helpers/testDb.ts — locate the `@ts-expect-error` line above document.deleteMany() to remove it after generate
    - .planning/phases/04-document-management/04-RESEARCH.md — Schema Push Approach section (authoritative commands)
  </read_first>

  <action>
    Step 1 — Ensure PostgreSQL is running. From the workspace root (not server/): `docker-compose up -d postgres`. Wait 5 seconds for the container to be ready. Confirm with `docker-compose ps` — the postgres service should show status "running" or "Up".

    Step 2 — Push schema. From server/ directory: `npx prisma db push`. This applies the DocumentType enum and Document table to the live PostgreSQL instance. Since this is an additive change (new enum + new table, no drops or renames), no --accept-data-loss flag is required. Expected output: "Your database is now in sync with your Prisma schema." If the command prompts for confirmation in a non-TTY environment, run `npx prisma db push --accept-data-loss` instead (safe — the change is purely additive).

    Step 3 — Regenerate Prisma client. From server/ directory: `npx prisma generate`. Expected output includes "Generated Prisma Client". The generated client in server/src/generated/prisma/ now exposes the Document model and DocumentType enum to TypeScript.

    Step 4 — Remove the @ts-expect-error stub from testDb.ts. Read server/src/__tests__/helpers/testDb.ts and remove the `// @ts-expect-error — Document model not yet in client; remove after prisma generate in 04-02` comment line above `await prisma.document.deleteMany()`. The call is now type-safe after prisma generate.

    Step 5 — Confirm TypeScript is clean: `cd server && npx tsc --noEmit`. The previously @ts-ignored/expect-errored document lines should now compile cleanly.
  </action>

  <verify>
    <automated>cd server &amp;&amp; npx prisma db push 2&gt;&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `docker-compose ps` shows postgres service as "Up" / running
    - `npx prisma db push` output contains "Your database is now in sync with your Prisma schema."
    - `npx prisma generate` output contains "Generated Prisma Client"
    - server/src/generated/prisma/client.d.ts contains the string "DocumentType" (enum exported from generated client)
    - server/src/__tests__/helpers/testDb.ts no longer contains the `@ts-expect-error` comment line above document.deleteMany()
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>

  <done>DB push successful; Prisma client regenerated with Document model; testDb.ts stub removed; tsc clean</done>
</task>

<task type="auto">
  <name>Task 2: Verify all 11 document tests GREEN + full suite passes</name>
  <files>server/src/__tests__/documents.test.ts</files>

  <read_first>
    - server/src/__tests__/documents.test.ts — read the test file to confirm the test names match exactly what VALIDATION.md specifies; check that the test body now makes real requests rather than the placeholder `expect(true).toBe(false)` stubs. If any test still has the placeholder body, replace it with the real supertest request from RESEARCH.md Pattern 9 and the VALIDATION.md behavior table.
    - .planning/phases/04-document-management/04-VALIDATION.md — Per-Task Verification Map (11 rows) for exact expected behaviors and test name patterns
    - .planning/phases/04-document-management/04-RESEARCH.md — Pattern 9 (supertest multipart .attach() pattern) and Code Examples section
  </read_first>

  <action>
    Replace all 11 placeholder stubs (`expect(true).toBe(false)`) in documents.test.ts with real test implementations using the supertest patterns from RESEARCH.md Pattern 9 and PATTERNS.md test file section.

    For each test, use the createTestStudent() helper and staffToken(). Multipart upload uses `.attach('file', buffer, {filename, contentType}).field('typeTag', value)`. The UPLOAD_ROOT env var is already set to os.tmpdir() at the top of the file from 04-01.

    Implement each test to match its exact expected behavior from VALIDATION.md:
    - doc-01-reject-mime: attach a text buffer with contentType:'text/plain' → expect res.status 400
    - doc-01-reject-magic: attach a buffer starting with 0x00 bytes (not %PDF) with contentType:'application/pdf' → expect res.status 400
    - doc-01-size-limit: attach a Buffer.alloc(26*1024*1024, 0x25) (26 MB) with contentType:'application/pdf' → expect res.status 400
    - doc-01-idor: create studentA and studentB; POST valid PDF to studentB's URL; expect 404 (or 403)
    - doc-01: POST validPdfBuffer + REPORT_CARD → expect 201 + body.originalFilename === 'test.pdf' + body.typeTag === 'REPORT_CARD'
    - doc-02: POST validPdfBuffer, then DELETE it (soft-delete), then GET /documents → expect response body array to have length 0
    - doc-02-download: POST validPdfBuffer, then GET /documents/:docId/download → expect status 200 + header Content-Disposition to include 'attachment'
    - doc-03: POST validPdfBuffer, DELETE it → expect DELETE status 200; GET /documents → body array excludes docId
    - doc-03-audit: POST validPdfBuffer, DELETE it → prisma.auditLog.findMany({where:{model:'Document', action:'DELETE', recordId:doc.id}}) → expect length 1
    - doc-04-invalid-type: POST validPdfBuffer + typeTag:'INVALID_TAG' → expect 400
    - doc-04-all-types: for each of ['REPORT_CARD','CERTIFICATE','AWARD_LETTER','WORK_EXPERIENCE_LETTER','REFERENCE_LETTER','OTHER'], POST validPdfBuffer + that typeTag → expect 201

    Run the quick suite first to get immediate feedback: `cd server && npm test -- --testNamePattern "doc-"`. If any test fails for unexpected reasons (route not found, 500 error), diagnose by reading the error body and cross-checking the route mount in students.ts and documents.ts. Common cause: UPLOAD_ROOT not set before app import — confirm the env assignment is the first executed line in documents.test.ts before any other import that transitively loads app.ts.

    Once the doc- suite is GREEN, run the full suite to ensure no regression.
  </action>

  <verify>
    <automated>cd server &amp;&amp; npm test 2&gt;&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `npm test -- --testNamePattern "doc-"` reports 11 passed, 0 failed
    - `npm test` (full suite) reports all prior tests passing + 11 new doc tests passing; total failed = 0
    - No test uses placeholder `expect(true).toBe(false)` — all 11 stubs replaced with real assertions
    - doc-03-audit test asserts `prisma.auditLog.findMany` returns exactly 1 row with model='Document' action='DELETE'
    - doc-02-download test asserts response header `content-disposition` contains 'attachment'
    - doc-04-all-types test POSTs all 6 DocumentType enum values and each returns 201
  </acceptance_criteria>

  <done>All 11 doc- tests GREEN; full test suite GREEN (73+ tests passing, 0 failing)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx prisma db push → PostgreSQL | DDL executed against live DB; only additive changes expected |
| Generated Prisma client → TypeScript | Type-safe after generation; @ts-expect-error stubs must be removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-DB-01 | Tampering | npx prisma db push (additive DDL) | accept | Schema adds DocumentType enum + Document table only; no existing table modified; no --accept-data-loss data risk |
| T-04-DB-02 | Denial of Service | test UPLOAD_ROOT in os.tmpdir() | accept | Tests write real PDF buffers to tmpdir; os.tmpdir() is ephemeral and not the production mount; acceptable for test isolation |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new npm installs in this plan; multer already installed in 04-01 |
</threat_model>

<verification>
After both tasks complete:

1. `cd server && npm test -- --testNamePattern "doc-"` → 11 passed, 0 failed
2. `cd server && npm test` → all tests pass (0 failed), count ≥ 73 (62 prior + 11 new)
3. `cd server && npx tsc --noEmit` → exit code 0
4. `grep -c "DocumentType" server/src/generated/prisma/client.d.ts` → output ≥ 1
5. `grep -c "@ts-expect-error" server/src/__tests__/helpers/testDb.ts` → output is 0 (stub removed)
</verification>

<success_criteria>
- `npx prisma db push` completes successfully ("Your database is now in sync with your Prisma schema.")
- Prisma client regenerated with Document model and DocumentType enum
- All 11 doc- test stubs replaced with real implementations
- `npm test` full suite: 0 failures
- testDb.ts @ts-expect-error stub removed
- `npx tsc --noEmit` clean
</success_criteria>

<output>
Create `.planning/phases/04-document-management/04-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol | Type | File |
|--------|------|------|
| `prisma.document` | PrismaClient accessor (generated) | `server/src/generated/prisma/client.js` |
| `DocumentType` | TypeScript enum (generated) | `server/src/generated/prisma/client.d.ts` |
| `Document` table | PostgreSQL table | live database (via db push) |
| `DocumentType` | PostgreSQL enum | live database (via db push) |
| 11 document integration tests | test implementations (previously stubs) | `server/src/__tests__/documents.test.ts` |
