---
phase: 01-infrastructure-auth
plan: 01
slug: server-scaffold
type: execute
wave: 1
depends_on: []
files_modified:
  - server/package.json
  - server/tsconfig.json
  - server/.env.example
  - server/prisma/schema.prisma
  - server/src/lib/prisma.ts
  - server/src/types/express.d.ts
  - server/src/app.ts
  - server/vitest.config.ts
  - server/src/__tests__/helpers/makeTestToken.ts
  - server/src/__tests__/helpers/testDb.ts
  - server/src/__tests__/auth.test.ts
  - server/src/__tests__/requireRole.test.ts
  - server/src/__tests__/audit.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "server/package.json lists all Standard Stack dependencies from RESEARCH.md — express, express-jwt, jwks-rsa, prisma, @prisma/client, cors, helmet, dotenv (production) plus vitest, supertest, jsonwebtoken, nock (devDependencies)"
    - "server/prisma/schema.prisma defines User (Role enum) and AuditLog (AuditAction enum) models exactly as Pattern 7 in RESEARCH.md"
    - "npx prisma migrate dev --name init completes successfully and server/prisma/migrations/ contains at least one migration SQL file"
    - "server/vitest.config.ts configures test environment: node, globals: true, include pattern server/src/__tests__/**/*.test.ts"
    - "server/src/__tests__/auth.test.ts contains at least 5 test cases for AUTH-01 and AUTH-02 behaviors"
    - "server/src/__tests__/audit.test.ts contains at least 1 test case for AUTH-03 (logAudit writes AuditLog record)"
    - "npx vitest run exits non-zero — tests are RED until Plan 03 implements the middleware"
  artifacts:
    - path: "server/package.json"
      provides: "npm project manifest with all required dependencies"
      contains: "\"express\":"
    - path: "server/prisma/schema.prisma"
      provides: "Prisma data model — User + AuditLog"
      contains: "model User"
    - path: "server/prisma/migrations/"
      provides: "Initial migration SQL generated by prisma migrate dev"
    - path: "server/src/lib/prisma.ts"
      provides: "PrismaClient singleton"
      exports: ["prisma"]
    - path: "server/src/app.ts"
      provides: "Minimal Express app stub (expanded in Plan 03)"
      exports: ["app"]
    - path: "server/vitest.config.ts"
      provides: "Vitest test runner configuration"
    - path: "server/src/__tests__/auth.test.ts"
      provides: "Failing integration tests for AUTH-01 + AUTH-02"
      min_lines: 50
    - path: "server/src/__tests__/audit.test.ts"
      provides: "Failing unit test for AUTH-03 logAudit"
      min_lines: 20
  key_links:
    - from: "server/src/__tests__/auth.test.ts"
      to: "server/src/app.ts"
      via: "import { app } from '../../app'"
      pattern: "import.*app.*from.*app"
    - from: "server/src/__tests__/helpers/makeTestToken.ts"
      to: "jsonwebtoken"
      via: "jwt.sign(payload, secret, { algorithm: 'HS256' })"
      pattern: "jwt\\.sign"
    - from: "server/src/lib/prisma.ts"
      to: "@prisma/client"
      via: "new PrismaClient()"
      pattern: "new PrismaClient"
---

## Phase Goal

**As a** careers staff member, **I want to** sign in with my school Microsoft account and reach the SPCS Student Transcript System, **so that** I can manage student records with my role-based permissions enforced and every data operation logged.

<objective>
Establish the server-side Node.js + Express project scaffold: npm project, TypeScript config, Prisma schema defining User + AuditLog models, first database migration, and a failing test suite (RED phase) that declares the auth contract for Plans 03 and beyond.

Purpose: The test suite IS the spec — it defines what /api/auth/me must return, what requireRole must enforce, and what logAudit must write before any implementation exists. Tests fail until Plan 03 provides the middleware.

Output: server/ npm project with all dependencies installed; Prisma schema + first migration; vitest config; test helpers; 3 failing test files covering AUTH-01, AUTH-02, AUTH-03.
</objective>

<execution_context>
@C:/@code/spcs-transcripts/.cursor/gsd-core/workflows/execute-plan.md
@C:/@code/spcs-transcripts/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-auth/01-CONTEXT.md
@.planning/phases/01-infrastructure-auth/01-RESEARCH.md
@.planning/phases/01-infrastructure-auth/01-VALIDATION.md
@.planning/phases/01-infrastructure-auth/01-SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialise server npm project, Prisma schema, and shared contracts</name>
  <read_first>
    - .planning/phases/01-infrastructure-auth/01-RESEARCH.md — read: Standard Stack table (exact versions), Pattern 7 (Prisma schema), Pitfall 6 (127.0.0.1 not localhost in DATABASE_URL), Anti-Patterns (no app.listen() in app.ts), Environment Availability table (Docker fallback for PostgreSQL)
    - .planning/phases/01-infrastructure-auth/01-CONTEXT.md — read: D-01 through D-09 locked decisions (all locked; none have discretion)
    - .planning/phases/01-infrastructure-auth/01-SKELETON.md — read: Architectural Decisions table and Key Contract section
  </read_first>
  <files>
    server/package.json,
    server/tsconfig.json,
    server/.env.example,
    server/prisma/schema.prisma,
    server/src/lib/prisma.ts,
    server/src/types/express.d.ts,
    server/src/app.ts
  </files>
  <action>
    Create the `server/` directory as a standalone npm project (do NOT use npm workspaces or monorepo tooling).

    1. **server/package.json** — set `"type": "module"` (ESM), `"main": "dist/index.js"`. Scripts: `"dev": "tsx watch src/index.ts"`, `"build": "tsc"`, `"start": "node dist/index.js"`, `"test": "vitest run"`, `"test:watch": "vitest"`. Production dependencies (exact versions from RESEARCH.md Standard Stack): `express@5.2.1`, `express-jwt@8.5.1`, `jwks-rsa@4.0.1`, `prisma@7.8.0`, `@prisma/client@7.8.0`, `cors@2.8.6`, `helmet@8.2.0`, `dotenv@17.4.2`. DevDependencies: `typescript`, `tsx`, `@types/node`, `@types/express@5`, `vitest`, `@vitest/coverage-v8`, `supertest@7.2.2`, `@types/supertest`, `jsonwebtoken@9.0.3`, `@types/jsonwebtoken`, `nock@14.0.15`. Run `npm install` inside `server/` after writing package.json.

    2. **server/tsconfig.json** — compiler options: `"target": "ES2022"`, `"module": "NodeNext"`, `"moduleResolution": "NodeNext"`, `"strict": true`, `"outDir": "dist"`, `"rootDir": "src"`, `"esModuleInterop": true`, `"skipLibCheck": true`. Include: `["src/**/*"]`. Exclude: `["node_modules", "dist"]`.

    3. **server/.env.example** — one env var per line, placeholder values only (no real secrets). Include: `DATABASE_URL=postgresql://spcs:password@127.0.0.1:5432/spcs_transcripts` (use 127.0.0.1, not localhost — per Pitfall 6 in RESEARCH.md to avoid Windows PostgreSQL connection issues), `AZURE_TENANT_ID=your-azure-tenant-id`, `AZURE_CLIENT_ID=your-azure-client-id`, `BOOTSTRAP_ADMIN_EMAIL=admin@school.edu`, `PORT=3001`, `CORS_ORIGIN=http://localhost:5173`, `NODE_ENV=development`, `TEST_JWT_SECRET=test-secret-for-vitest-only-never-production`. Add a comment on the TEST_JWT_SECRET line: `# Used by makeTestToken.ts in test environment only — never set in production`. Also create `.gitignore` in `server/` containing: `node_modules/`, `dist/`, `.env`, `*.env.local`.

    4. **server/prisma/schema.prisma** — implement exactly as Pattern 7 in RESEARCH.md: datasource block with `provider = "postgresql"` and `url = env("DATABASE_URL")`; generator block with `provider = "prisma-client-js"`; `enum Role { ADMIN STAFF }`; `enum AuditAction { CREATE UPDATE DELETE }`; `model User` with fields: `id String @id @default(uuid())`, `email String @unique`, `displayName String`, `role Role @default(STAFF)`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`, `auditLogs AuditLog[] @relation("ActingUser")`; `model AuditLog` with fields: `id String @id @default(uuid())`, `actingUserId String`, `actingUser User @relation("ActingUser", fields: [actingUserId], references: [id])`, `action AuditAction`, `model String`, `recordId String`, `details String?`, `timestamp DateTime @default(now())`, indexes: `@@index([actingUserId])`, `@@index([model, recordId])`, `@@index([timestamp])`. After writing the file, copy `server/.env.example` to `server/.env` with real local values for DATABASE_URL, then run `npx prisma migrate dev --name init` from the `server/` directory. If PostgreSQL is not installed locally, start it via Docker: `docker run -d --name spcs-pg -e POSTGRES_USER=spcs -e POSTGRES_PASSWORD=password -e POSTGRES_DB=spcs_transcripts -p 5432:5432 postgres:16`, wait 5 seconds, then run the migration.

    5. **server/src/lib/prisma.ts** — export a module-level PrismaClient singleton named `prisma`. Use the hot-reload-safe global pattern: declare `const globalForPrisma = globalThis as unknown as { prisma: PrismaClient }`, then `export const prisma = globalForPrisma.prisma ?? new PrismaClient()`. Assign `globalForPrisma.prisma = prisma` to prevent multiple instances during hot reload. Import `PrismaClient` from `@prisma/client`.

    6. **server/src/types/express.d.ts** — augment the Express `Request` type to add `user?: import('@prisma/client').User`. The file must begin with `export {}` to be treated as a TypeScript module (not a script). Use `declare global { namespace Express { interface Request { user?: import('@prisma/client').User } } }`.

    7. **server/src/app.ts** — create a minimal stub that exports the Express app. Import `express` from `express`. Export `export const app = express()`. Add `app.use(express.json())`. Do NOT call `app.listen()` here (anti-pattern in RESEARCH.md — Supertest binds its own port). Do NOT add any auth middleware here yet — that is Plan 03's job.
  </action>
  <verify>
    <automated>cd server && npx prisma validate && npx tsc --noEmit && echo "schema+types valid"</automated>
  </verify>
  <acceptance_criteria>
    - server/package.json contains `"express":` and `"express-jwt":` and `"prisma":` and `"vitest":` entries
    - server/package.json contains `"type": "module"` at top level
    - server/tsconfig.json contains `"moduleResolution": "NodeNext"` and `"strict": true`
    - server/.env.example contains `DATABASE_URL=postgresql://spcs:password@127.0.0.1:5432/` (note: 127.0.0.1 not localhost)
    - server/.env.example contains `AZURE_TENANT_ID=` and `AZURE_CLIENT_ID=` and `BOOTSTRAP_ADMIN_EMAIL=` and `TEST_JWT_SECRET=`
    - server/.gitignore contains `.env`
    - server/prisma/schema.prisma contains `model User` and `model AuditLog` and `enum Role` and `enum AuditAction`
    - server/prisma/schema.prisma contains `@default(uuid())` for both model id fields
    - server/prisma/migrations/ directory exists with at least one `.sql` migration file
    - server/src/lib/prisma.ts exports `prisma` and contains `new PrismaClient`
    - server/src/types/express.d.ts contains `user?: import('@prisma/client').User`
    - server/src/app.ts exports `app` and does NOT contain `app.listen(`
    - `cd server && npx prisma validate` exits 0
    - `cd server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Server npm project scaffolded, Prisma schema valid, first migration generated, PrismaClient singleton exported, Express types augmented with req.user, app stub exported without listen().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create failing test suite (RED phase — defines auth contract for AUTH-01, AUTH-02, AUTH-03)</name>
  <read_first>
    - server/package.json — confirm vitest, supertest, jsonwebtoken, nock are in devDependencies
    - server/src/app.ts — read the current stub (needed for import path in test files)
    - server/prisma/schema.prisma — read User and AuditLog field names (needed for test assertions)
    - .planning/phases/01-infrastructure-auth/01-RESEARCH.md — read: Vitest + Supertest Auth Test example, Pitfall 5 (preferred_username fallback to upn/email), Validation Architecture section, Wave 0 Gaps list
    - .planning/phases/01-infrastructure-auth/01-VALIDATION.md — read: Per-Task Verification Map table (all 7 test cases), Wave 0 Requirements checklist
  </read_first>
  <files>
    server/vitest.config.ts,
    server/src/__tests__/helpers/makeTestToken.ts,
    server/src/__tests__/helpers/testDb.ts,
    server/src/__tests__/auth.test.ts,
    server/src/__tests__/requireRole.test.ts,
    server/src/__tests__/audit.test.ts
  </files>
  <behavior>
    auth.test.ts — 5 test cases:
    - GET /api/auth/me without Authorization header → 401 (test ID: auth-01-401)
    - GET /api/auth/me with valid Bearer token (preferred_username: staff@school.edu) → 200, body has { id, email: 'staff@school.edu', displayName, role: 'STAFF' } (test ID: auth-01-200)
    - GET /api/auth/me with new user token → User record created in DB (test ID: auth-01-upsert)
    - GET /api/auth/me with token preferred_username matching BOOTSTRAP_ADMIN_EMAIL → role is 'ADMIN' in response and DB (test ID: auth-02-bootstrap)
    - Admin-only route (e.g., GET /api/admin/test added temporarily) called with Staff role token → 403 (test ID: auth-02-admin-route)

    requireRole.test.ts — 2 test cases:
    - requireRole(Role.ADMIN) called with req.user.role === STAFF → responds 403 (test ID: auth-02-403)
    - requireRole(Role.ADMIN) called with req.user.role === ADMIN → calls next() (test ID: auth-02-admin-pass)

    audit.test.ts — 1 test case:
    - logAudit(prisma, { userId, action: 'CREATE', model: 'Student', recordId: uuid }) → exactly one AuditLog record written with matching fields (test ID: auth-03-audit)
  </behavior>
  <action>
    1. **server/vitest.config.ts** — configure vitest: `environment: 'node'`, `globals: true`, `testTimeout: 15000`, `include: ['src/__tests__/**/*.test.ts']`. Set env vars for test environment: `AZURE_TENANT_ID: 'test-tenant-id'`, `AZURE_CLIENT_ID: 'test-client-id'`, `BOOTSTRAP_ADMIN_EMAIL: 'admin@school.edu'`, `TEST_JWT_SECRET: 'test-secret'`, `DATABASE_URL: process.env.DATABASE_URL || 'postgresql://spcs:password@127.0.0.1:5432/spcs_transcripts'`. Load dotenv at the top of the config for local dev convenience.

    2. **server/src/__tests__/helpers/makeTestToken.ts** — export `makeTestToken(overrides: Record<string, unknown> = {}): string`. Sign with `process.env.TEST_JWT_SECRET || 'test-secret'` using HS256 algorithm via `jsonwebtoken`. Default payload must include: `preferred_username: 'staff@school.edu'`, `name: 'Test Staff'`, `aud: \`api://${process.env.AZURE_CLIENT_ID || 'test-client-id'}\``, `iss: \`https://login.microsoftonline.com/${process.env.AZURE_TENANT_ID || 'test-tenant-id'}/v2.0\``. Spread `overrides` into the payload after defaults. Set `expiresIn: '1h'`. Note: makeTestToken uses HS256 — Plan 03's auth middleware must check `NODE_ENV === 'test'` and switch to HS256 validation using `TEST_JWT_SECRET` instead of JWKS. This avoids requiring a live JWKS endpoint in tests (per Assumption A4 in RESEARCH.md).

    3. **server/src/__tests__/helpers/testDb.ts** — import `prisma` from `../../lib/prisma`. Export `clearDb` async function that deletes all AuditLog records then all User records (AuditLog first due to FK). Export a `beforeEach` call to `clearDb()`. Export `prisma` re-export for test assertion use.

    4. **server/src/__tests__/auth.test.ts** — import `{ app }` from `../../app`, `request` from `supertest`, `makeTestToken` from `./helpers/makeTestToken`, `{ prisma }` from `./helpers/testDb`. Call `clearDb` in `beforeEach`. Write all 5 test cases from `<behavior>`. The admin-only route test requires adding a temporary admin test route to app.ts (add `app.get('/api/admin/test', requireRole(Role.ADMIN), (req, res) => res.json({ ok: true }))` — this import will fail until Plan 03 creates requireRole, which is acceptable for the RED phase). Note: tests import `requireRole` and `resolveUser` which do not exist yet — TypeScript import errors are acceptable here since vitest will simply report these as test failures.

    5. **server/src/__tests__/requireRole.test.ts** — import `requireRole` from `../../middleware/requireRole` and `Role` from `@prisma/client`. Write 2 unit test cases: mock `req.user = { role: Role.STAFF }` and call `requireRole(Role.ADMIN)(mockReq, mockRes, mockNext)`, assert `res.status` called with 403; second test mocks `req.user = { role: Role.ADMIN }` and asserts `next()` was called. Use vitest `vi.fn()` for mocking.

    6. **server/src/__tests__/audit.test.ts** — import `logAudit` from `../../services/audit` and `{ prisma }` from `./helpers/testDb`. Write 1 test case: call `await logAudit(prisma, { userId: 'test-user-id', action: 'CREATE', model: 'Student', recordId: 'test-record-id' })`, then `const logs = await prisma.auditLog.findMany()`, assert `logs.length === 1`, assert `logs[0].action === 'CREATE'`, assert `logs[0].model === 'Student'`, assert `logs[0].actingUserId === 'test-user-id'`.

    All three test files will report import/module-not-found errors or test failures — this is the RED phase. Do NOT fix the failing tests here; they are intentionally failing.
  </action>
  <verify>
    <automated>cd server && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - server/vitest.config.ts exists and contains `environment: 'node'` and `globals: true`
    - server/src/__tests__/helpers/makeTestToken.ts exists and contains `jwt.sign` and `'HS256'`
    - server/src/__tests__/helpers/testDb.ts exists and contains `prisma.auditLog.deleteMany` and `prisma.user.deleteMany`
    - server/src/__tests__/auth.test.ts contains at minimum the strings: `GET /api/auth/me`, `401`, `200`, `BOOTSTRAP_ADMIN_EMAIL`, `403`
    - server/src/__tests__/requireRole.test.ts contains `requireRole` and `Role.ADMIN` and `Role.STAFF`
    - server/src/__tests__/audit.test.ts contains `logAudit` and `auditLog.findMany`
    - `cd server && npx vitest run` exits with non-zero exit code (tests are RED — correct for Walking Skeleton Wave 1)
    - No test file contains `it.skip(` or `test.skip(` — all tests must be active and failing, not skipped
  </acceptance_criteria>
  <done>Test infrastructure created. Tests are deliberately RED — they define the auth contract. Tests will become GREEN in Plan 03 when middleware and services are implemented. RED state is the correct outcome for this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → npm registry | Package installation pulls code from public registry |
| server/.env → PostgreSQL | DATABASE_URL credential grants DB access for migrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm install (server) | mitigate | All packages pre-audited in RESEARCH.md Package Legitimacy Audit — no SLOP verdicts; all packages Approved; no blocking human checkpoint required |
| T-01-02 | Information Disclosure | server/.env | mitigate | .env.example committed with placeholder values only; actual .env added to server/.gitignore immediately; DATABASE_URL and JWT secrets never committed |
| T-01-03 | Information Disclosure | TEST_JWT_SECRET | mitigate | Named `TEST_JWT_SECRET` to signal test-only use; .env.example includes comment warning against production use; test helper only activates HS256 mode when `NODE_ENV === 'test'` |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck — Package Legitimacy Audit in RESEARCH.md is complete; all packages are OK or approved-SUS (Microsoft MSAL packages, confirmed official); no [SLOP] verdicts; no blocking human checkpoint required |
</threat_model>

<verification>
After completing both tasks:
1. `cd server && npx prisma validate` → exits 0
2. `cd server && npx tsc --noEmit` → exits 0 (TypeScript compiles cleanly)
3. `cd server && npx vitest run` → exits non-zero (RED tests — correct for Wave 1)
4. `cat server/.gitignore | grep '\.env'` → returns `.env`
5. `cat server/prisma/schema.prisma | grep -v '^//' | grep -c 'model '` → returns 2
6. `ls server/prisma/migrations/` → lists at least one directory with `migration.sql`
</verification>

<success_criteria>
- server/ is a valid TypeScript npm project with all Standard Stack dependencies installed
- Prisma schema defines User + AuditLog models per Pattern 7; first migration generated and applied
- 3 test files exist declaring 8 test cases covering AUTH-01, AUTH-02, AUTH-03
- Tests are RED (failing) — this is the deliberate Walking Skeleton contract-first state
- No secrets in git; .env.example has all required placeholder keys
</success_criteria>

## Artifacts This Plan Produces

| Artifact | Type | Path | Description |
|----------|------|------|-------------|
| `prisma` | `const PrismaClient` | `server/src/lib/prisma.ts` | Singleton Prisma client for all server-side DB operations; re-exported by testDb helper |
| `app` | Express Application (stub) | `server/src/app.ts` | Minimal Express stub; expanded to full implementation in Plan 03 |
| `makeTestToken` | `function(overrides?) => string` | `server/src/__tests__/helpers/makeTestToken.ts` | Signs HS256 JWT for test use; never used outside `NODE_ENV === 'test'` |
| `clearDb` | `async function` | `server/src/__tests__/helpers/testDb.ts` | Truncates AuditLog + User between test runs |
| `User` | Prisma model | `server/prisma/schema.prisma` | Fields: id (uuid PK), email (unique), displayName, role (Role), createdAt, updatedAt |
| `AuditLog` | Prisma model | `server/prisma/schema.prisma` | Fields: id (uuid PK), actingUserId (FK→User), action (AuditAction), model (string), recordId (string), details (string?), timestamp |
| `Role` | Prisma enum | `server/prisma/schema.prisma` | Values: ADMIN, STAFF |
| `AuditAction` | Prisma enum | `server/prisma/schema.prisma` | Values: CREATE, UPDATE, DELETE |
| `DATABASE_URL` | env var | `server/.env.example` | PostgreSQL connection string using 127.0.0.1 (not localhost) |
| `AZURE_TENANT_ID` | env var | `server/.env.example` | Azure AD tenant ID |
| `AZURE_CLIENT_ID` | env var | `server/.env.example` | Azure AD application client ID |
| `BOOTSTRAP_ADMIN_EMAIL` | env var | `server/.env.example` | Email of first Admin user; matched against JWT preferred_username claim |
| `TEST_JWT_SECRET` | env var | `server/.env.example` | HS256 secret for test token generation; test-environment only |

<output>
Create `.planning/phases/01-infrastructure-auth/01-server-scaffold-SUMMARY.md` when done
</output>
