DATABASE_URL=postgresql://spcs:password@127.0.0.1:5432/spcs_transcripts
AZURE_TENANT_ID=your-azure-tenant-id
AZURE_CLIENT_ID=your-azure-client-id
BOOTSTRAP_ADMIN_EMAIL=admin@school.edu
PORT=3001
CORS_ORIGIN=http://localhost:5173
NODE_ENV=development
TEST_JWT_SECRET=test-secret-for-vitest-only-never-production # Used by makeTestToken.ts in test environment only — never set in production
